name: maplibre_native_ffi_smoke
description: Build-only Flutter host for the MapLibre Native Dart binding.
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  maplibre_native_ffi:
    path: ../..

flutter:
  uses-material-design: false
