name: maplibre_native_ffi
description: Low-level Dart bindings for the MapLibre Native C API.
version: 0.0.0
repository: https://github.com/maplibre/maplibre-native-ffi
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  ffi: ^2.1.4

dev_dependencies:
  ffigen: ">=19.0.0 <21.0.0"
  lints: ^6.0.0
  test: ^1.26.3
