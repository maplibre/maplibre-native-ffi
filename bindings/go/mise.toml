[tools]
"core:go" = "{{vars.go_version}}"
"aqua:mvdan/gofumpt" = "{{vars.gofumpt_version}}"

[tasks._gofumpt-check]
description = "List Go files that require gofumpt."
hide = true
raw_args = true
dir = "../.."
run = "gofumpt -l"

[tasks._gofumpt-fix]
description = "Format Go files with gofumpt."
hide = true
raw_args = true
dir = "../.."
run = "gofumpt -w"

[tasks.build]
usage = 'arg "[target]" default="{{vars.host_native_target}}"'
depends = [{ task = "//:build", args = ["{{usage.target}}"] }]
run = '''
PKG_CONFIG_PATH="$MISE_MONOREPO_ROOT/build/$usage_target/install/share/pkgconfig${PKG_CONFIG_PATH:+:${PKG_CONFIG_PATH}}" \
  go test ./...
'''
run_windows = '''
native_install_dir="$MISE_MONOREPO_ROOT/build/$usage_target/install"
export PATH="$(cygpath -u "$native_install_dir/bin")${PATH:+:${PATH}}"
PKG_CONFIG_PATH="$native_install_dir/share/pkgconfig${PKG_CONFIG_PATH:+:${PKG_CONFIG_PATH}}" \
  go test ./...
'''

[tasks.test]
usage = 'arg "[target]" default="{{vars.host_native_target}}"'
depends = [{ task = "//:build", args = ["{{usage.target}}"] }]
run = '''
native_install_dir="$MISE_MONOREPO_ROOT/build/$usage_target/install"
export PKG_CONFIG_PATH="$native_install_dir/share/pkgconfig${PKG_CONFIG_PATH:+:${PKG_CONFIG_PATH}}"
go test ./...
go vet ./...
'''
run_windows = '''
native_install_dir="$MISE_MONOREPO_ROOT/build/$usage_target/install"
export PATH="$(cygpath -u "$native_install_dir/bin")${PATH:+:${PATH}}"
export PKG_CONFIG_PATH="$native_install_dir/share/pkgconfig${PKG_CONFIG_PATH:+:${PKG_CONFIG_PATH}}"
go test ./...
go vet ./...
'''
