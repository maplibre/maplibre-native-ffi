// Targeted by JavaCPP version 1.5.11: DO NOT EDIT THIS FILE

package org.maplibre.nativejni.internal.javacpp;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

public class MaplibreNativeC extends org.maplibre.nativejni.internal.javacpp.MaplibreNativeCConfig {
  static {
    Loader.load();
  }

  // Parsed from maplibre_native_c/base.h

  /**
   * \file maplibre_native_c/base.h Public C API declarations for base ABI types and status values.
   */

  // #ifndef MAPLIBRE_NATIVE_C_BASE_H
  // #define MAPLIBRE_NATIVE_C_BASE_H

  // #ifndef __cplusplus
  // #include <stdbool.h>
  // #endif

  // #include <stddef.h>
  // #include <stdint.h>

  // #ifdef _WIN32
  // #if defined(MLN_BUILDING_C)
  // #define MLN_API __declspec(dllexport)
  // #else
  // #define MLN_API __declspec(dllimport)
  // #endif
  // #else
  // #define MLN_API __attribute__((visibility("default")))
  // #endif

  // #ifdef __cplusplus
  // #else
  // #define MLN_NOEXCEPT
  // #endif

  // #ifdef __cplusplus
  // #endif

  /** Status values returned by status-returning functions. */
  /** enum mln_status */
  public static final int MLN_STATUS_OK = 0,
      /** A pointer, size field, mask, or handle argument was invalid. */
      MLN_STATUS_INVALID_ARGUMENT = -1,
      /** The object is valid but not currently in a state that permits the call. */
      MLN_STATUS_INVALID_STATE = -2,
      /** The handle is thread-affine and the call was made from the wrong thread. */
      MLN_STATUS_WRONG_THREAD = -3,
      /** The entry point or requested behavior is unavailable in this build. */
      MLN_STATUS_UNSUPPORTED = -4,
      /** A native MapLibre error or C++ exception was converted to status. */
      MLN_STATUS_NATIVE_ERROR = -5;

  /** Render backend support flags reported by this native library build. */
  /** enum mln_render_backend_flag */
  public static final int MLN_RENDER_BACKEND_FLAG_METAL = 1 << 0,
      MLN_RENDER_BACKEND_FLAG_VULKAN = 1 << 1;

  @Opaque
  public static class mln_runtime extends Pointer {
    /** Empty constructor. Calls {@code super((Pointer)null)}. */
    public mln_runtime() {
      super((Pointer) null);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_runtime(Pointer p) {
      super(p);
    }
  }

  @Opaque
  public static class mln_map extends Pointer {
    /** Empty constructor. Calls {@code super((Pointer)null)}. */
    public mln_map() {
      super((Pointer) null);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_map(Pointer p) {
      super(p);
    }
  }

  @Opaque
  public static class mln_map_projection extends Pointer {
    /** Empty constructor. Calls {@code super((Pointer)null)}. */
    public mln_map_projection() {
      super((Pointer) null);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_map_projection(Pointer p) {
      super(p);
    }
  }

  @Opaque
  public static class mln_offline_region_snapshot extends Pointer {
    /** Empty constructor. Calls {@code super((Pointer)null)}. */
    public mln_offline_region_snapshot() {
      super((Pointer) null);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_offline_region_snapshot(Pointer p) {
      super(p);
    }
  }

  @Opaque
  public static class mln_offline_region_list extends Pointer {
    /** Empty constructor. Calls {@code super((Pointer)null)}. */
    public mln_offline_region_list() {
      super((Pointer) null);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_offline_region_list(Pointer p) {
      super(p);
    }
  }

  @Opaque
  public static class mln_json_snapshot extends Pointer {
    /** Empty constructor. Calls {@code super((Pointer)null)}. */
    public mln_json_snapshot() {
      super((Pointer) null);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_json_snapshot(Pointer p) {
      super(p);
    }
  }

  @Opaque
  public static class mln_resource_request_handle extends Pointer {
    /** Empty constructor. Calls {@code super((Pointer)null)}. */
    public mln_resource_request_handle() {
      super((Pointer) null);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_resource_request_handle(Pointer p) {
      super(p);
    }
  }

  @Opaque
  public static class mln_render_session extends Pointer {
    /** Empty constructor. Calls {@code super((Pointer)null)}. */
    public mln_render_session() {
      super((Pointer) null);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_render_session(Pointer p) {
      super(p);
    }
  }

  /**
   * Reports the C ABI contract version. The value is 0 while the ABI is unstable, and will
   * increment on each SemVer major release.
   */
  @NoException
  public static native @Cast("uint32_t") int mln_c_version();

  /**
   * Reports the render backends available in this native library build.
   *
   * <p>The return value is a mask of mln_render_backend_flag values.
   */
  @NoException
  public static native @Cast("uint32_t") int mln_supported_render_backend_mask();

  // #ifdef __cplusplus
  // #endif

  // #endif  // MAPLIBRE_NATIVE_C_BASE_H

  // Parsed from maplibre_native_c/diagnostics.h

  /** \file maplibre_native_c/diagnostics.h Public C API declarations for diagnostics. */

  // #ifndef MAPLIBRE_NATIVE_C_DIAGNOSTICS_H
  // #define MAPLIBRE_NATIVE_C_DIAGNOSTICS_H

  // #include "base.h"

  // #ifdef __cplusplus
  // #endif

  /**
   * Returns the last thread-local diagnostic message.
   *
   * <p>The returned string is empty when no diagnostic is available. The pointer is owned by the C
   * API and remains valid until the next C API call on the same thread that writes a thread-local
   * diagnostic.
   */
  @NoException
  public static native @Cast("const char*") BytePointer mln_thread_last_error_message();

  // #ifdef __cplusplus
  // #endif

  // #endif  // MAPLIBRE_NATIVE_C_DIAGNOSTICS_H

  // Parsed from maplibre_native_c/logging.h

  /** \file maplibre_native_c/logging.h Public C API declarations for logging. */

  // #ifndef MAPLIBRE_NATIVE_C_LOGGING_H
  // #define MAPLIBRE_NATIVE_C_LOGGING_H

  // #include <stdint.h>

  // #include "base.h"

  // #ifdef __cplusplus
  // #endif

  /** Log severity values emitted by MapLibre Native. */
  /** enum mln_log_severity */
  public static final int MLN_LOG_SEVERITY_INFO = 1,
      MLN_LOG_SEVERITY_WARNING = 2,
      MLN_LOG_SEVERITY_ERROR = 3;

  /** Bitmask values for log severities dispatched asynchronously. */
  /** enum mln_log_severity_mask */
  public static final int MLN_LOG_SEVERITY_MASK_INFO = 1 << MLN_LOG_SEVERITY_INFO,
      MLN_LOG_SEVERITY_MASK_WARNING = 1 << MLN_LOG_SEVERITY_WARNING,
      MLN_LOG_SEVERITY_MASK_ERROR = 1 << MLN_LOG_SEVERITY_ERROR,
      MLN_LOG_SEVERITY_MASK_DEFAULT = MLN_LOG_SEVERITY_MASK_INFO | MLN_LOG_SEVERITY_MASK_WARNING,
      MLN_LOG_SEVERITY_MASK_ALL =
          MLN_LOG_SEVERITY_MASK_INFO | MLN_LOG_SEVERITY_MASK_WARNING | MLN_LOG_SEVERITY_MASK_ERROR;

  /** Log event categories emitted by MapLibre Native. */
  /** enum mln_log_event */
  public static final int MLN_LOG_EVENT_GENERAL = 0,
      MLN_LOG_EVENT_SETUP = 1,
      MLN_LOG_EVENT_SHADER = 2,
      MLN_LOG_EVENT_PARSE_STYLE = 3,
      MLN_LOG_EVENT_PARSE_TILE = 4,
      MLN_LOG_EVENT_RENDER = 5,
      MLN_LOG_EVENT_STYLE = 6,
      MLN_LOG_EVENT_DATABASE = 7,
      MLN_LOG_EVENT_HTTP_REQUEST = 8,
      MLN_LOG_EVENT_SPRITE = 9,
      MLN_LOG_EVENT_IMAGE = 10,
      MLN_LOG_EVENT_OPENGL = 11,
      MLN_LOG_EVENT_JNI = 12,
      MLN_LOG_EVENT_ANDROID = 13,
      MLN_LOG_EVENT_CRASH = 14,
      MLN_LOG_EVENT_GLYPH = 15,
      MLN_LOG_EVENT_TIMING = 16;

  /**
   * Receives a MapLibre Native log record.
   *
   * <p>The message pointer is borrowed for the callback duration. Returning non-zero consumes the
   * record. Returning zero lets MapLibre Native's platform logger handle it.
   */
  public static class mln_log_callback extends FunctionPointer {
    static {
      Loader.load();
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_log_callback(Pointer p) {
      super(p);
    }

    protected mln_log_callback() {
      allocate();
    }

    private native void allocate();

    public native @Cast("uint32_t") int call(
        Pointer user_data,
        @Cast("uint32_t") int severity,
        @Cast("uint32_t") int event,
        @Cast("int64_t") long code,
        @Cast("const char*") BytePointer message);
  }

  /**
   * Installs a process-global MapLibre Native log callback.
   *
   * <p>Passing null clears the current callback. The callback and user_data are stored by reference
   * and must remain valid until the callback is replaced or cleared.
   *
   * <p>The callback is a low-level native callback:
   *
   * <p>- MapLibre may invoke it from logging or worker threads selected by the async severity mask.
   * - MapLibre may invoke it while holding internal logging locks. - The callback must be
   * thread-safe, return quickly, and must not call this C API or MapLibre Native APIs. - Language
   * adapters for runtimes that restrict native-thread callbacks can marshal records into
   * host-managed logging facilities before invoking user code.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_log_set_callback(
      mln_log_callback callback, Pointer user_data);

  /**
   * Clears the process-global log callback.
   *
   * <p>After this call succeeds, future log dispatches no longer use the callback that was
   * previously registered through mln_log_set_callback().
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_log_clear_callback();

  /**
   * Controls which log severities MapLibre Native may dispatch asynchronously.
   *
   * <p>MLN_LOG_SEVERITY_MASK_DEFAULT restores MapLibre Native's default behavior: info and warning
   * records may be asynchronous, while error records remain synchronous.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when mask contains
   * unknown bits. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_log_set_async_severity_mask(
      @Cast("uint32_t") int mask);

  // #ifdef __cplusplus
  // #endif

  // #endif  // MAPLIBRE_NATIVE_C_LOGGING_H

  // Parsed from maplibre_native_c/runtime.h

  /**
   * \file maplibre_native_c/runtime.h Public C API declarations for runtime, resources, and events.
   */

  // #ifndef MAPLIBRE_NATIVE_C_RUNTIME_H
  // #define MAPLIBRE_NATIVE_C_RUNTIME_H

  // #ifndef __cplusplus
  // #include <stdbool.h>
  // #endif

  // #include <stddef.h>
  // #include <stdint.h>

  // #include "base.h"

  // #ifdef __cplusplus
  // #endif

  /** enum mln_network_status */
  public static final int MLN_NETWORK_STATUS_ONLINE = 1, MLN_NETWORK_STATUS_OFFLINE = 2;

  /** enum mln_runtime_option_flag */
  public static final int MLN_RUNTIME_OPTION_MAXIMUM_CACHE_SIZE = 1 << 0;

  /** enum mln_ambient_cache_operation */
  public static final int MLN_AMBIENT_CACHE_OPERATION_RESET_DATABASE = 1,
      MLN_AMBIENT_CACHE_OPERATION_PACK_DATABASE = 2,
      MLN_AMBIENT_CACHE_OPERATION_INVALIDATE = 3,
      MLN_AMBIENT_CACHE_OPERATION_CLEAR = 4;

  /** enum mln_offline_region_definition_type */
  public static final int MLN_OFFLINE_REGION_DEFINITION_TILE_PYRAMID = 1,
      MLN_OFFLINE_REGION_DEFINITION_GEOMETRY = 2;

  /** enum mln_offline_region_download_state */
  public static final int MLN_OFFLINE_REGION_DOWNLOAD_INACTIVE = 0,
      MLN_OFFLINE_REGION_DOWNLOAD_ACTIVE = 1;

  /** Offline database operation token. Zero is never a valid operation ID. */

  /** Offline database operation kinds reported by completion events. */
  /** enum mln_offline_operation_kind */
  public static final int MLN_OFFLINE_OPERATION_AMBIENT_CACHE = 1,
      MLN_OFFLINE_OPERATION_REGION_CREATE = 2,
      MLN_OFFLINE_OPERATION_REGION_GET = 3,
      MLN_OFFLINE_OPERATION_REGIONS_LIST = 4,
      MLN_OFFLINE_OPERATION_REGIONS_MERGE_DATABASE = 5,
      MLN_OFFLINE_OPERATION_REGION_UPDATE_METADATA = 6,
      MLN_OFFLINE_OPERATION_REGION_GET_STATUS = 7,
      MLN_OFFLINE_OPERATION_REGION_SET_OBSERVED = 8,
      MLN_OFFLINE_OPERATION_REGION_SET_DOWNLOAD_STATE = 9,
      MLN_OFFLINE_OPERATION_REGION_INVALIDATE = 10,
      MLN_OFFLINE_OPERATION_REGION_DELETE = 11;

  /** Offline database operation result kinds reported by completion events. */
  /** enum mln_offline_operation_result_kind */
  public static final int MLN_OFFLINE_OPERATION_RESULT_NONE = 0,
      MLN_OFFLINE_OPERATION_RESULT_REGION = 1,
      MLN_OFFLINE_OPERATION_RESULT_OPTIONAL_REGION = 2,
      MLN_OFFLINE_OPERATION_RESULT_REGION_LIST = 3,
      MLN_OFFLINE_OPERATION_RESULT_REGION_STATUS = 4;

  /** Offline region status snapshot. */
  public static class mln_offline_region_status extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_offline_region_status() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_offline_region_status(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_offline_region_status(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_offline_region_status position(long position) {
      return (mln_offline_region_status) super.position(position);
    }

    @Override
    public mln_offline_region_status getPointer(long i) {
      return new mln_offline_region_status((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_offline_region_status size(int setter);

    /** One of mln_offline_region_download_state. */
    public native @Cast("uint32_t") int download_state();

    public native mln_offline_region_status download_state(int setter);

    public native @Cast("uint64_t") long completed_resource_count();

    public native mln_offline_region_status completed_resource_count(long setter);

    public native @Cast("uint64_t") long completed_resource_size();

    public native mln_offline_region_status completed_resource_size(long setter);

    public native @Cast("uint64_t") long completed_tile_count();

    public native mln_offline_region_status completed_tile_count(long setter);

    public native @Cast("uint64_t") long required_tile_count();

    public native mln_offline_region_status required_tile_count(long setter);

    public native @Cast("uint64_t") long completed_tile_size();

    public native mln_offline_region_status completed_tile_size(long setter);

    public native @Cast("uint64_t") long required_resource_count();

    public native mln_offline_region_status required_resource_count(long setter);

    public native @Cast("bool") boolean required_resource_count_is_precise();

    public native mln_offline_region_status required_resource_count_is_precise(boolean setter);

    public native @Cast("bool") boolean complete();

    public native mln_offline_region_status complete(boolean setter);
  }

  /** Runtime event types returned by mln_runtime_poll_event(). */
  /** enum mln_runtime_event_type */
  public static final int MLN_RUNTIME_EVENT_MAP_CAMERA_WILL_CHANGE = 1,
      MLN_RUNTIME_EVENT_MAP_CAMERA_IS_CHANGING = 2,
      MLN_RUNTIME_EVENT_MAP_CAMERA_DID_CHANGE = 3,
      MLN_RUNTIME_EVENT_MAP_STYLE_LOADED = 4,
      MLN_RUNTIME_EVENT_MAP_LOADING_STARTED = 5,
      MLN_RUNTIME_EVENT_MAP_LOADING_FINISHED = 6,
      MLN_RUNTIME_EVENT_MAP_LOADING_FAILED = 7,
      MLN_RUNTIME_EVENT_MAP_IDLE = 8,
      MLN_RUNTIME_EVENT_MAP_RENDER_UPDATE_AVAILABLE = 9,
      MLN_RUNTIME_EVENT_MAP_RENDER_ERROR = 10,
      MLN_RUNTIME_EVENT_MAP_STILL_IMAGE_FINISHED = 11,
      MLN_RUNTIME_EVENT_MAP_STILL_IMAGE_FAILED = 12,
      MLN_RUNTIME_EVENT_MAP_RENDER_FRAME_STARTED = 13,
      MLN_RUNTIME_EVENT_MAP_RENDER_FRAME_FINISHED = 14,
      MLN_RUNTIME_EVENT_MAP_RENDER_MAP_STARTED = 15,
      MLN_RUNTIME_EVENT_MAP_RENDER_MAP_FINISHED = 16,
      MLN_RUNTIME_EVENT_MAP_STYLE_IMAGE_MISSING = 17,
      MLN_RUNTIME_EVENT_MAP_TILE_ACTION = 18,
      MLN_RUNTIME_EVENT_OFFLINE_REGION_STATUS_CHANGED = 19,
      MLN_RUNTIME_EVENT_OFFLINE_REGION_RESPONSE_ERROR = 20,
      MLN_RUNTIME_EVENT_OFFLINE_REGION_TILE_COUNT_LIMIT_EXCEEDED = 21,
      MLN_RUNTIME_EVENT_OFFLINE_OPERATION_COMPLETED = 22;

  /** Source kinds used by mln_runtime_event.source_type. */
  /** enum mln_runtime_event_source_type */
  public static final int MLN_RUNTIME_EVENT_SOURCE_RUNTIME = 0, MLN_RUNTIME_EVENT_SOURCE_MAP = 1;

  /** Payload kinds used by mln_runtime_event.payload_type. */
  /** enum mln_runtime_event_payload_type */
  public static final int MLN_RUNTIME_EVENT_PAYLOAD_NONE = 0,
      MLN_RUNTIME_EVENT_PAYLOAD_RENDER_FRAME = 1,
      MLN_RUNTIME_EVENT_PAYLOAD_RENDER_MAP = 2,
      MLN_RUNTIME_EVENT_PAYLOAD_STYLE_IMAGE_MISSING = 3,
      MLN_RUNTIME_EVENT_PAYLOAD_TILE_ACTION = 4,
      MLN_RUNTIME_EVENT_PAYLOAD_OFFLINE_REGION_STATUS = 5,
      MLN_RUNTIME_EVENT_PAYLOAD_OFFLINE_REGION_RESPONSE_ERROR = 6,
      MLN_RUNTIME_EVENT_PAYLOAD_OFFLINE_REGION_TILE_COUNT_LIMIT = 7,
      MLN_RUNTIME_EVENT_PAYLOAD_OFFLINE_OPERATION_COMPLETED = 8;

  /** Render modes reported by render observer events. */
  /** enum mln_render_mode */
  public static final int MLN_RENDER_MODE_PARTIAL = 0, MLN_RENDER_MODE_FULL = 1;

  /** Tile operations reported by tile observer events. */
  /** enum mln_tile_operation */
  public static final int MLN_TILE_OPERATION_REQUESTED_FROM_CACHE = 0,
      MLN_TILE_OPERATION_REQUESTED_FROM_NETWORK = 1,
      MLN_TILE_OPERATION_LOAD_FROM_NETWORK = 2,
      MLN_TILE_OPERATION_LOAD_FROM_CACHE = 3,
      MLN_TILE_OPERATION_START_PARSE = 4,
      MLN_TILE_OPERATION_END_PARSE = 5,
      MLN_TILE_OPERATION_ERROR = 6,
      MLN_TILE_OPERATION_CANCELLED = 7,
      MLN_TILE_OPERATION_NULL = 8;

  /** enum mln_resource_kind */
  public static final int MLN_RESOURCE_KIND_UNKNOWN = 0,
      MLN_RESOURCE_KIND_STYLE = 1,
      MLN_RESOURCE_KIND_SOURCE = 2,
      MLN_RESOURCE_KIND_TILE = 3,
      MLN_RESOURCE_KIND_GLYPHS = 4,
      MLN_RESOURCE_KIND_SPRITE_IMAGE = 5,
      MLN_RESOURCE_KIND_SPRITE_JSON = 6,
      MLN_RESOURCE_KIND_IMAGE = 7;

  /** enum mln_resource_loading_method */
  public static final int MLN_RESOURCE_LOADING_METHOD_ALL = 0,
      MLN_RESOURCE_LOADING_METHOD_CACHE_ONLY = 1,
      MLN_RESOURCE_LOADING_METHOD_NETWORK_ONLY = 2;

  /** enum mln_resource_priority */
  public static final int MLN_RESOURCE_PRIORITY_REGULAR = 0, MLN_RESOURCE_PRIORITY_LOW = 1;

  /** enum mln_resource_usage */
  public static final int MLN_RESOURCE_USAGE_ONLINE = 0, MLN_RESOURCE_USAGE_OFFLINE = 1;

  /** enum mln_resource_storage_policy */
  public static final int MLN_RESOURCE_STORAGE_POLICY_PERMANENT = 0,
      MLN_RESOURCE_STORAGE_POLICY_VOLATILE = 1;

  /** enum mln_resource_response_status */
  public static final int MLN_RESOURCE_RESPONSE_STATUS_OK = 0,
      MLN_RESOURCE_RESPONSE_STATUS_ERROR = 1,
      MLN_RESOURCE_RESPONSE_STATUS_NO_CONTENT = 2,
      MLN_RESOURCE_RESPONSE_STATUS_NOT_MODIFIED = 3;

  /** enum mln_resource_error_reason */
  public static final int MLN_RESOURCE_ERROR_REASON_NONE = 0,
      MLN_RESOURCE_ERROR_REASON_NOT_FOUND = 1,
      MLN_RESOURCE_ERROR_REASON_SERVER = 2,
      MLN_RESOURCE_ERROR_REASON_CONNECTION = 3,
      MLN_RESOURCE_ERROR_REASON_RATE_LIMIT = 4,
      MLN_RESOURCE_ERROR_REASON_OTHER = 5;

  /** enum mln_resource_provider_decision */
  public static final int MLN_RESOURCE_PROVIDER_DECISION_PASS_THROUGH = 0,
      MLN_RESOURCE_PROVIDER_DECISION_HANDLE = 1;

  /**
   * Reads MapLibre Native's process-global network status.
   *
   * <p>On success, out_status receives a mln_network_status value.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when out_status is null.
   * - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_network_status_get(
      @Cast("uint32_t*") IntPointer out_status);

  @NoException
  public static native @Cast("mln_status") int mln_network_status_get(
      @Cast("uint32_t*") IntBuffer out_status);

  @NoException
  public static native @Cast("mln_status") int mln_network_status_get(
      @Cast("uint32_t*") int[] out_status);

  /**
   * Sets MapLibre Native's process-global network status.
   *
   * <p>MLN_NETWORK_STATUS_ONLINE allows HTTP and HTTPS requests and wakes native subscribers when
   * transitioning from offline. MLN_NETWORK_STATUS_OFFLINE makes MapLibre's online source stop
   * starting network requests until reachability returns. Runtime-scoped resource configuration is
   * unchanged.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when status is not a
   * mln_network_status value. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to
   * status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_network_status_set(@Cast("uint32_t") int status);

  public static class mln_runtime_options extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_runtime_options() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_runtime_options(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_runtime_options(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_runtime_options position(long position) {
      return (mln_runtime_options) super.position(position);
    }

    @Override
    public mln_runtime_options getPointer(long i) {
      return new mln_runtime_options((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_runtime_options size(int setter);

    public native @Cast("uint32_t") int flags();

    public native mln_runtime_options flags(int setter);

    /** Filesystem root for asset:// URLs. Copied during runtime creation. */
    public native @Cast("const char*") BytePointer asset_path();

    public native mln_runtime_options asset_path(BytePointer setter);

    /** Cache database path. Copied during runtime creation. */
    public native @Cast("const char*") BytePointer cache_path();

    public native mln_runtime_options cache_path(BytePointer setter);

    /** Maximum ambient cache size in bytes when the matching flag is set. */
    public native @Cast("uint64_t") long maximum_cache_size();

    public native mln_runtime_options maximum_cache_size(long setter);
  }

  /** Rendering statistics reported in MLN_RUNTIME_EVENT_PAYLOAD_RENDER_FRAME. */
  public static class mln_rendering_stats extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_rendering_stats() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_rendering_stats(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_rendering_stats(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_rendering_stats position(long position) {
      return (mln_rendering_stats) super.position(position);
    }

    @Override
    public mln_rendering_stats getPointer(long i) {
      return new mln_rendering_stats((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_rendering_stats size(int setter);

    /** Frame CPU encoding time in seconds. */
    public native double encoding_time();

    public native mln_rendering_stats encoding_time(double setter);

    /** Frame CPU rendering time in seconds. */
    public native double rendering_time();

    public native mln_rendering_stats rendering_time(double setter);

    /** Number of frames rendered by the native renderer. */
    public native @Cast("int64_t") long frame_count();

    public native mln_rendering_stats frame_count(long setter);

    /** Draw calls executed during the most recent frame. */
    public native @Cast("int64_t") long draw_call_count();

    public native mln_rendering_stats draw_call_count(long setter);

    /** Total draw calls executed by the native renderer. */
    public native @Cast("int64_t") long total_draw_call_count();

    public native mln_rendering_stats total_draw_call_count(long setter);
  }

  /** Payload for MLN_RUNTIME_EVENT_MAP_RENDER_FRAME_FINISHED. */
  public static class mln_runtime_event_render_frame extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_runtime_event_render_frame() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_runtime_event_render_frame(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_runtime_event_render_frame(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_runtime_event_render_frame position(long position) {
      return (mln_runtime_event_render_frame) super.position(position);
    }

    @Override
    public mln_runtime_event_render_frame getPointer(long i) {
      return new mln_runtime_event_render_frame((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_runtime_event_render_frame size(int setter);

    /** One of mln_render_mode. */
    public native @Cast("uint32_t") int mode();

    public native mln_runtime_event_render_frame mode(int setter);

    /** Whether MapLibre needs another frame after this one. */
    public native @Cast("bool") boolean needs_repaint();

    public native mln_runtime_event_render_frame needs_repaint(boolean setter);

    /** Whether symbol placement changed during this frame. */
    public native @Cast("bool") boolean placement_changed();

    public native mln_runtime_event_render_frame placement_changed(boolean setter);

    public native @ByRef mln_rendering_stats stats();

    public native mln_runtime_event_render_frame stats(mln_rendering_stats setter);
  }

  /** Payload for MLN_RUNTIME_EVENT_MAP_RENDER_MAP_FINISHED. */
  public static class mln_runtime_event_render_map extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_runtime_event_render_map() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_runtime_event_render_map(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_runtime_event_render_map(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_runtime_event_render_map position(long position) {
      return (mln_runtime_event_render_map) super.position(position);
    }

    @Override
    public mln_runtime_event_render_map getPointer(long i) {
      return new mln_runtime_event_render_map((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_runtime_event_render_map size(int setter);

    /** One of mln_render_mode. */
    public native @Cast("uint32_t") int mode();

    public native mln_runtime_event_render_map mode(int setter);
  }

  /** Payload for MLN_RUNTIME_EVENT_MAP_STYLE_IMAGE_MISSING. */
  public static class mln_runtime_event_style_image_missing extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_runtime_event_style_image_missing() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_runtime_event_style_image_missing(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_runtime_event_style_image_missing(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_runtime_event_style_image_missing position(long position) {
      return (mln_runtime_event_style_image_missing) super.position(position);
    }

    @Override
    public mln_runtime_event_style_image_missing getPointer(long i) {
      return new mln_runtime_event_style_image_missing((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_runtime_event_style_image_missing size(int setter);

    /**
     * Borrowed image ID bytes. Valid until the next poll for this runtime or until the runtime is
     * destroyed.
     */
    public native @Cast("const char*") BytePointer image_id();

    public native mln_runtime_event_style_image_missing image_id(BytePointer setter);

    /** Number of bytes in image_id, excluding the trailing null terminator. */
    public native @Cast("size_t") long image_id_size();

    public native mln_runtime_event_style_image_missing image_id_size(long setter);
  }

  /** Overscaled tile identity reported in tile observer events. */
  public static class mln_tile_id extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_tile_id() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_tile_id(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_tile_id(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_tile_id position(long position) {
      return (mln_tile_id) super.position(position);
    }

    @Override
    public mln_tile_id getPointer(long i) {
      return new mln_tile_id((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int overscaled_z();

    public native mln_tile_id overscaled_z(int setter);

    public native int wrap();

    public native mln_tile_id wrap(int setter);

    public native @Cast("uint32_t") int canonical_z();

    public native mln_tile_id canonical_z(int setter);

    public native @Cast("uint32_t") int canonical_x();

    public native mln_tile_id canonical_x(int setter);

    public native @Cast("uint32_t") int canonical_y();

    public native mln_tile_id canonical_y(int setter);
  }

  /** Payload for MLN_RUNTIME_EVENT_MAP_TILE_ACTION. */
  public static class mln_runtime_event_tile_action extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_runtime_event_tile_action() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_runtime_event_tile_action(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_runtime_event_tile_action(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_runtime_event_tile_action position(long position) {
      return (mln_runtime_event_tile_action) super.position(position);
    }

    @Override
    public mln_runtime_event_tile_action getPointer(long i) {
      return new mln_runtime_event_tile_action((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_runtime_event_tile_action size(int setter);

    /** One of mln_tile_operation. */
    public native @Cast("uint32_t") int operation();

    public native mln_runtime_event_tile_action operation(int setter);

    public native @ByRef mln_tile_id tile_id();

    public native mln_runtime_event_tile_action tile_id(mln_tile_id setter);

    /**
     * Borrowed source ID bytes. Valid until the next poll for this runtime or until the runtime is
     * destroyed.
     */
    public native @Cast("const char*") BytePointer source_id();

    public native mln_runtime_event_tile_action source_id(BytePointer setter);

    /** Number of bytes in source_id, excluding the trailing null terminator. */
    public native @Cast("size_t") long source_id_size();

    public native mln_runtime_event_tile_action source_id_size(long setter);
  }

  /** Payload for MLN_RUNTIME_EVENT_OFFLINE_REGION_STATUS_CHANGED. */
  public static class mln_runtime_event_offline_region_status extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_runtime_event_offline_region_status() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_runtime_event_offline_region_status(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_runtime_event_offline_region_status(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_runtime_event_offline_region_status position(long position) {
      return (mln_runtime_event_offline_region_status) super.position(position);
    }

    @Override
    public mln_runtime_event_offline_region_status getPointer(long i) {
      return new mln_runtime_event_offline_region_status((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_runtime_event_offline_region_status size(int setter);

    public native @Cast("mln_offline_region_id") long region_id();

    public native mln_runtime_event_offline_region_status region_id(long setter);

    public native @ByRef mln_offline_region_status status();

    public native mln_runtime_event_offline_region_status status(mln_offline_region_status setter);
  }

  /** Payload for MLN_RUNTIME_EVENT_OFFLINE_REGION_RESPONSE_ERROR. */
  public static class mln_runtime_event_offline_region_response_error extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_runtime_event_offline_region_response_error() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_runtime_event_offline_region_response_error(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_runtime_event_offline_region_response_error(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_runtime_event_offline_region_response_error position(long position) {
      return (mln_runtime_event_offline_region_response_error) super.position(position);
    }

    @Override
    public mln_runtime_event_offline_region_response_error getPointer(long i) {
      return new mln_runtime_event_offline_region_response_error((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_runtime_event_offline_region_response_error size(int setter);

    public native @Cast("mln_offline_region_id") long region_id();

    public native mln_runtime_event_offline_region_response_error region_id(long setter);

    /** One of mln_resource_error_reason. */
    public native @Cast("uint32_t") int reason();

    public native mln_runtime_event_offline_region_response_error reason(int setter);
  }

  /** Payload for MLN_RUNTIME_EVENT_OFFLINE_REGION_TILE_COUNT_LIMIT_EXCEEDED. */
  public static class mln_runtime_event_offline_region_tile_count_limit extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_runtime_event_offline_region_tile_count_limit() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_runtime_event_offline_region_tile_count_limit(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_runtime_event_offline_region_tile_count_limit(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_runtime_event_offline_region_tile_count_limit position(long position) {
      return (mln_runtime_event_offline_region_tile_count_limit) super.position(position);
    }

    @Override
    public mln_runtime_event_offline_region_tile_count_limit getPointer(long i) {
      return new mln_runtime_event_offline_region_tile_count_limit((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_runtime_event_offline_region_tile_count_limit size(int setter);

    public native @Cast("mln_offline_region_id") long region_id();

    public native mln_runtime_event_offline_region_tile_count_limit region_id(long setter);

    public native @Cast("uint64_t") @Name("limit") long _limit();

    public native mln_runtime_event_offline_region_tile_count_limit _limit(long setter);
  }

  /** Payload for MLN_RUNTIME_EVENT_OFFLINE_OPERATION_COMPLETED. */
  public static class mln_runtime_event_offline_operation_completed extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_runtime_event_offline_operation_completed() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_runtime_event_offline_operation_completed(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_runtime_event_offline_operation_completed(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_runtime_event_offline_operation_completed position(long position) {
      return (mln_runtime_event_offline_operation_completed) super.position(position);
    }

    @Override
    public mln_runtime_event_offline_operation_completed getPointer(long i) {
      return new mln_runtime_event_offline_operation_completed((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_runtime_event_offline_operation_completed size(int setter);

    public native @Cast("mln_offline_operation_id") long operation_id();

    public native mln_runtime_event_offline_operation_completed operation_id(long setter);

    /** One of mln_offline_operation_kind. */
    public native @Cast("uint32_t") int operation_kind();

    public native mln_runtime_event_offline_operation_completed operation_kind(int setter);

    /** One of mln_offline_operation_result_kind. */
    public native @Cast("uint32_t") int result_kind();

    public native mln_runtime_event_offline_operation_completed result_kind(int setter);

    /** Async result status as a mln_status value. */
    public native int result_status();

    public native mln_runtime_event_offline_operation_completed result_status(int setter);

    /** Meaningful for MLN_OFFLINE_OPERATION_REGION_GET. */
    public native @Cast("bool") boolean found();

    public native mln_runtime_event_offline_operation_completed found(boolean setter);
  }

  /** Event payload returned by mln_runtime_poll_event(). */
  public static class mln_runtime_event extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_runtime_event() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_runtime_event(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_runtime_event(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_runtime_event position(long position) {
      return (mln_runtime_event) super.position(position);
    }

    @Override
    public mln_runtime_event getPointer(long i) {
      return new mln_runtime_event((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_runtime_event size(int setter);

    public native @Cast("uint32_t") int type();

    public native mln_runtime_event type(int setter);

    /** One of mln_runtime_event_source_type. */
    public native @Cast("uint32_t") int source_type();

    public native mln_runtime_event source_type(int setter);

    /**
     * Source handle for this event. For map-originated events, this is an mln_map*. For
     * runtime-originated events, this is an mln_runtime*. Borrowed; valid while the source handle
     * remains live.
     */
    public native Pointer source();

    public native mln_runtime_event source(Pointer setter);

    public native int code();

    public native mln_runtime_event code(int setter);

    /** One of mln_runtime_event_payload_type. */
    public native @Cast("uint32_t") int payload_type();

    public native mln_runtime_event payload_type(int setter);

    /** Borrowed payload selected by payload_type. Null when payload_size is 0. */
    public native @Const Pointer payload();

    public native mln_runtime_event payload(Pointer setter);

    /** Number of bytes in payload. */
    public native @Cast("size_t") long payload_size();

    public native mln_runtime_event payload_size(long setter);

    /** Borrowed event message bytes. Null when message_size is 0. */
    public native @Cast("const char*") BytePointer message();

    public native mln_runtime_event message(BytePointer setter);

    /** Number of bytes in message, excluding the trailing null terminator. */
    public native @Cast("size_t") long message_size();

    public native mln_runtime_event message_size(long setter);
  }

  public static class mln_resource_transform_response extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_resource_transform_response() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_resource_transform_response(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_resource_transform_response(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_resource_transform_response position(long position) {
      return (mln_resource_transform_response) super.position(position);
    }

    @Override
    public mln_resource_transform_response getPointer(long i) {
      return new mln_resource_transform_response((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_resource_transform_response size(int setter);

    /** Replacement URL. Null or empty keeps the original URL. Copied on return. */
    public native @Cast("const char*") BytePointer url();

    public native mln_resource_transform_response url(BytePointer setter);
  }

  /**
   * Rewrites a network resource URL.
   *
   * <p>This callback can only replace the request URL. It cannot mutate headers, bodies, cache
   * policy, or convert a request into an error.
   *
   * <p>Callback invocations follow these rules:
   *
   * <p>- MapLibre may invoke the callback on a worker or network thread instead of the runtime
   * owner thread. - The callback must be thread-safe, return quickly, and must not call this C API.
   * - url and out_response are borrowed for the callback duration. - When a replacement URL is set,
   * out_response->url must point to null-terminated storage that remains valid after the callback
   * returns until the C API copies it during the current transform invocation. Bindings usually
   * keep per-thread response storage alive until the next callback, transform replacement,
   * transform clear, or runtime teardown. - A non-OK return status is treated as no rewrite and
   * does not fail the resource request. - The callback and user_data must remain valid until the
   * transform is replaced, cleared, or the runtime is destroyed. Those calls wait for in-flight
   * transform callbacks before returning.
   */
  public static class mln_resource_transform_callback extends FunctionPointer {
    static {
      Loader.load();
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_resource_transform_callback(Pointer p) {
      super(p);
    }

    protected mln_resource_transform_callback() {
      allocate();
    }

    private native void allocate();

    public native @Cast("mln_status") int call(
        Pointer user_data,
        @Cast("uint32_t") int kind,
        @Cast("const char*") BytePointer url,
        mln_resource_transform_response out_response);
  }

  public static class mln_resource_transform extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_resource_transform() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_resource_transform(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_resource_transform(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_resource_transform position(long position) {
      return (mln_resource_transform) super.position(position);
    }

    @Override
    public mln_resource_transform getPointer(long i) {
      return new mln_resource_transform((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_resource_transform size(int setter);

    public native mln_resource_transform_callback callback();

    public native mln_resource_transform callback(mln_resource_transform_callback setter);

    public native Pointer user_data();

    public native mln_resource_transform user_data(Pointer setter);
  }

  public static class mln_resource_request extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_resource_request() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_resource_request(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_resource_request(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_resource_request position(long position) {
      return (mln_resource_request) super.position(position);
    }

    @Override
    public mln_resource_request getPointer(long i) {
      return new mln_resource_request((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_resource_request size(int setter);

    public native @Cast("const char*") BytePointer url();

    public native mln_resource_request url(BytePointer setter);

    public native @Cast("uint32_t") int kind();

    public native mln_resource_request kind(int setter);

    public native @Cast("uint32_t") int loading_method();

    public native mln_resource_request loading_method(int setter);

    public native @Cast("uint32_t") int priority();

    public native mln_resource_request priority(int setter);

    public native @Cast("uint32_t") int usage();

    public native mln_resource_request usage(int setter);

    public native @Cast("uint32_t") int storage_policy();

    public native mln_resource_request storage_policy(int setter);

    public native @Cast("bool") boolean has_range();

    public native mln_resource_request has_range(boolean setter);

    public native @Cast("uint64_t") long range_start();

    public native mln_resource_request range_start(long setter);

    public native @Cast("uint64_t") long range_end();

    public native mln_resource_request range_end(long setter);

    public native @Cast("bool") boolean has_prior_modified();

    public native mln_resource_request has_prior_modified(boolean setter);

    public native @Cast("int64_t") long prior_modified_unix_ms();

    public native mln_resource_request prior_modified_unix_ms(long setter);

    public native @Cast("bool") boolean has_prior_expires();

    public native mln_resource_request has_prior_expires(boolean setter);

    public native @Cast("int64_t") long prior_expires_unix_ms();

    public native mln_resource_request prior_expires_unix_ms(long setter);

    public native @Cast("const char*") BytePointer prior_etag();

    public native mln_resource_request prior_etag(BytePointer setter);

    public native @Cast("const uint8_t*") BytePointer prior_data();

    public native mln_resource_request prior_data(BytePointer setter);

    public native @Cast("size_t") long prior_data_size();

    public native mln_resource_request prior_data_size(long setter);
  }

  public static class mln_resource_response extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_resource_response() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_resource_response(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_resource_response(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_resource_response position(long position) {
      return (mln_resource_response) super.position(position);
    }

    @Override
    public mln_resource_response getPointer(long i) {
      return new mln_resource_response((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_resource_response size(int setter);

    public native @Cast("uint32_t") int status();

    public native mln_resource_response status(int setter);

    public native @Cast("uint32_t") int error_reason();

    public native mln_resource_response error_reason(int setter);

    /** Response bytes. May be null only when byte_count is 0. */
    public native @Cast("const uint8_t*") BytePointer bytes();

    public native mln_resource_response bytes(BytePointer setter);

    public native @Cast("size_t") long byte_count();

    public native mln_resource_response byte_count(long setter);

    public native @Cast("const char*") BytePointer error_message();

    public native mln_resource_response error_message(BytePointer setter);

    public native @Cast("bool") boolean must_revalidate();

    public native mln_resource_response must_revalidate(boolean setter);

    public native @Cast("bool") boolean has_modified();

    public native mln_resource_response has_modified(boolean setter);

    public native @Cast("int64_t") long modified_unix_ms();

    public native mln_resource_response modified_unix_ms(long setter);

    public native @Cast("bool") boolean has_expires();

    public native mln_resource_response has_expires(boolean setter);

    public native @Cast("int64_t") long expires_unix_ms();

    public native mln_resource_response expires_unix_ms(long setter);

    public native @Cast("const char*") BytePointer etag();

    public native mln_resource_response etag(BytePointer setter);

    public native @Cast("bool") boolean has_retry_after();

    public native mln_resource_response has_retry_after(boolean setter);

    public native @Cast("int64_t") long retry_after_unix_ms();

    public native mln_resource_response retry_after_unix_ms(long setter);
  }

  /**
   * Intercepts a network resource request.
   *
   * <p>The callback runs synchronously on the thread that reaches the C API network file source.
   * That thread may be a MapLibre worker or network thread instead of the runtime owner thread.
   *
   * <p>Request handling follows these rules:
   *
   * <p>- request and its pointed-to fields are borrowed for the callback duration. -
   * MLN_RESOURCE_PROVIDER_DECISION_PASS_THROUGH lets native OnlineFileSource handle the request. -
   * After returning PASS_THROUGH, the provider must not retain, complete, or release the handle. -
   * MLN_RESOURCE_PROVIDER_DECISION_HANDLE lets the provider complete the request through the handle
   * inline or later. - Unknown decision values produce a provider error response. The C API
   * releases the provided handle and does not pass the request through. - The C API copies
   * completion data, and mln_resource_request_complete() may be called from any thread. - Providers
   * must release handled request handles after they no longer need to complete or observe
   * cancellation. - The callback must be thread-safe, return quickly, and must not call map or
   * runtime C API functions. - The callback may call resource request handle functions for the
   * provided handle.
   */
  public static class mln_resource_provider_callback extends FunctionPointer {
    static {
      Loader.load();
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_resource_provider_callback(Pointer p) {
      super(p);
    }

    protected mln_resource_provider_callback() {
      allocate();
    }

    private native void allocate();

    public native @Cast("uint32_t") int call(
        Pointer user_data, @Const mln_resource_request request, mln_resource_request_handle handle);
  }

  public static class mln_resource_provider extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_resource_provider() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_resource_provider(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_resource_provider(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_resource_provider position(long position) {
      return (mln_resource_provider) super.position(position);
    }

    @Override
    public mln_resource_provider getPointer(long i) {
      return new mln_resource_provider((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_resource_provider size(int setter);

    public native mln_resource_provider_callback callback();

    public native mln_resource_provider callback(mln_resource_provider_callback setter);

    public native Pointer user_data();

    public native mln_resource_provider user_data(Pointer setter);
  }

  /** Returns runtime options initialized for this C API version. */
  @NoException
  public static native @ByVal mln_runtime_options mln_runtime_options_default();

  /**
   * Creates a runtime handle.
   *
   * <p>The creating thread becomes the runtime owner thread. Each owner thread may hold one live
   * runtime.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when out_runtime is null,
   * *out_runtime is not null, or options has an unsupported size or flags. -
   * MLN_STATUS_INVALID_STATE when the current thread already owns a live runtime. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_create(
      @Const mln_runtime_options options, @Cast("mln_runtime**") PointerPointer out_runtime);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_create(
      @Const mln_runtime_options options, @ByPtrPtr mln_runtime out_runtime);

  /**
   * Sets a runtime-scoped network resource provider.
   *
   * <p>The provider must be set before any map is created from the runtime. It is invoked for
   * requests that reach the C API network file source. Built-in non-network schemes such as file,
   * asset, mbtiles, and pmtiles are handled by native MainResourceLoader before this extension
   * point. The callback and user_data are stored by reference and must remain valid until the
   * runtime is destroyed.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when runtime is null or
   * not live, provider is null, provider->size is too small, or callback is null. -
   * MLN_STATUS_INVALID_STATE when runtime already owns live maps. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the runtime owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_set_resource_provider(
      mln_runtime runtime, @Const mln_resource_provider provider);

  /**
   * Completes a C API resource provider request.
   *
   * <p>This function may be called inline from the provider callback or later from any thread. The
   * C API copies all response bytes and strings before returning.
   *
   * <p>Completion is one-shot. A second completion, completion after cancellation, or completion
   * with null arguments returns a non-OK status and does not invoke MapLibre's resource callback.
   * Malformed response contents are converted to provider error responses and still consume the
   * completion.
   *
   * <p>Returns: - MLN_STATUS_OK when the response was accepted for asynchronous delivery. -
   * MLN_STATUS_INVALID_ARGUMENT when handle or response is null. - MLN_STATUS_INVALID_STATE when
   * the request was cancelled, already completed, or can no longer accept a response. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_resource_request_complete(
      mln_resource_request_handle handle, @Const mln_resource_response response);

  /**
   * Reports whether MapLibre has cancelled a C API resource provider request.
   *
   * <p>This function may be called from any thread while the provider still owns the handle. A
   * cancelled request no longer wants a response; later completion is ignored with
   * MLN_STATUS_INVALID_STATE.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when handle or
   * out_cancelled is null.
   */
  @NoException
  public static native @Cast("mln_status") int mln_resource_request_cancelled(
      @Const mln_resource_request_handle handle, @Cast("bool*") BoolPointer out_cancelled);

  @NoException
  public static native @Cast("mln_status") int mln_resource_request_cancelled(
      @Const mln_resource_request_handle handle, @Cast("bool*") boolean[] out_cancelled);

  /**
   * Releases the provider's reference to a resource request handle.
   *
   * <p>Providers own a releasable handle only after returning MLN_RESOURCE_PROVIDER_DECISION_HANDLE
   * from the callback. Release the handle exactly once after completing the request or deciding not
   * to complete it. Passing null is a no-op. A released handle must not be used again.
   */
  @NoException
  public static native void mln_resource_request_release(mln_resource_request_handle handle);

  /**
   * Registers or updates a runtime-scoped URL transform for network resources.
   *
   * <p>It is forwarded to MapLibre's OnlineFileSource, so it applies wherever native
   * OnlineFileSource applies transforms, including nested PMTiles network range requests. It does
   * not apply to file, asset, database, MBTiles, or registered C API provider responses intercepted
   * before OnlineFileSource.
   *
   * <p>This call may replace an existing transform while maps exist. When it returns, no in-flight
   * request can still invoke the previous transform.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when runtime is null or
   * not live, transform is null, transform->size is too small, or callback is null. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the runtime owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_set_resource_transform(
      mln_runtime runtime, @Const mln_resource_transform transform);

  /**
   * Clears the runtime-scoped URL transform for network resources.
   *
   * <p>After this call succeeds, network resource URLs pass through unchanged. When it returns, no
   * in-flight request can still invoke the previous transform.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when runtime is null or
   * not live. - MLN_STATUS_WRONG_THREAD when called from a thread other than the runtime owner
   * thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_clear_resource_transform(
      mln_runtime runtime);

  /**
   * Starts a MapLibre ambient cache maintenance operation for this runtime.
   *
   * <p>When runtime options omit cache_path, this operates on MapLibre's default in-memory database
   * and its effects are not durable beyond the native database lifetime. Completion is reported
   * through MLN_RUNTIME_EVENT_OFFLINE_OPERATION_COMPLETED.
   *
   * <p>Returns: - MLN_STATUS_OK when the operation was accepted and out_operation_id was set. -
   * MLN_STATUS_INVALID_ARGUMENT when runtime is null or not live, or operation is not a
   * mln_ambient_cache_operation value, or out_operation_id is null. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the runtime owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_run_ambient_cache_operation_start(
      mln_runtime runtime,
      @Cast("uint32_t") int operation,
      @Cast("mln_offline_operation_id*") LongPointer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_run_ambient_cache_operation_start(
      mln_runtime runtime,
      @Cast("uint32_t") int operation,
      @Cast("mln_offline_operation_id*") LongBuffer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_run_ambient_cache_operation_start(
      mln_runtime runtime,
      @Cast("uint32_t") int operation,
      @Cast("mln_offline_operation_id*") long[] out_operation_id);

  /**
   * Discards runtime-owned state for an offline database operation.
   *
   * <p>Discarding does not cancel native database work. It drops stored results, removes queued
   * completion events for the operation, and suppresses later completion delivery when the native
   * operation is still pending.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when runtime is null or
   * not live, or operation_id is zero or unknown. - MLN_STATUS_WRONG_THREAD when called from a
   * thread other than the runtime owner thread. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_operation_discard(
      mln_runtime runtime, @Cast("mln_offline_operation_id") long operation_id);

  /**
   * Destroys a runtime handle.
   *
   * <p>The runtime must no longer own live maps.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when runtime is null or
   * not a live runtime handle. - MLN_STATUS_INVALID_STATE when runtime still owns live maps. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the creating thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_destroy(mln_runtime runtime);

  /**
   * Runs one pending owner-thread task for this runtime.
   *
   * <p>If no task is pending, the call returns MLN_STATUS_OK without doing work.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when runtime is null or
   * not a live runtime handle. - MLN_STATUS_WRONG_THREAD when called from a thread other than the
   * runtime owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to
   * status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_run_once(mln_runtime runtime);

  /**
   * Pops the next queued runtime event.
   *
   * <p>On success, *out_event is reset and *out_has_event indicates whether an event was available.
   * When an event is available, *out_event receives it. Map-originated events set
   * out_event->source_type to MLN_RUNTIME_EVENT_SOURCE_MAP and out_event->source to the source map.
   * Runtime-originated events set out_event->source_type to MLN_RUNTIME_EVENT_SOURCE_RUNTIME.
   *
   * <p>When an event is available, out_event->payload points to runtime-owned storage containing a
   * struct selected by out_event->payload_type, or null when the payload type is
   * MLN_RUNTIME_EVENT_PAYLOAD_NONE. String pointers inside typed payloads and out_event->message
   * remain valid until the next mln_runtime_poll_event() call for the same runtime or until the
   * runtime is destroyed. Copy those bytes before then when they must outlive that window. For
   * style-image-missing and tile-action events, out_event->message contains the same ID string
   * exposed by the typed payload.
   *
   * <p>Returns: - MLN_STATUS_OK when the poll completed; out_has_event indicates whether an event
   * was written to out_event. - MLN_STATUS_INVALID_ARGUMENT when runtime is null or not live,
   * out_event is null, out_has_event is null, or out_event->size is too small. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the runtime owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_poll_event(
      mln_runtime runtime, mln_runtime_event out_event, @Cast("bool*") BoolPointer out_has_event);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_poll_event(
      mln_runtime runtime, mln_runtime_event out_event, @Cast("bool*") boolean[] out_has_event);

  // #ifdef __cplusplus
  // #endif

  // #endif  // MAPLIBRE_NATIVE_C_RUNTIME_H

  // Parsed from maplibre_native_c/camera.h

  /**
   * \file maplibre_native_c/camera.h Public C API declarations for map camera and coordinate
   * conversion.
   */

  // #ifndef MAPLIBRE_NATIVE_C_CAMERA_H
  // #define MAPLIBRE_NATIVE_C_CAMERA_H

  // #include <stddef.h>
  // #include <stdint.h>

  // #include "base.h"
  // #include "map.h"

  // #ifdef __cplusplus
  // #endif

  /** Returns empty camera options initialized for this C API version. */
  @NoException
  public static native @ByVal mln_camera_options mln_camera_options_default();

  /** Returns empty animation options initialized for this C API version. */
  @NoException
  public static native @ByVal mln_animation_options mln_animation_options_default();

  /** Returns empty camera fitting options initialized for this C API version. */
  @NoException
  public static native @ByVal mln_camera_fit_options mln_camera_fit_options_default();

  /** Returns empty map bound options initialized for this C API version. */
  @NoException
  public static native @ByVal mln_bound_options mln_bound_options_default();

  /** Returns empty free camera options initialized for this C API version. */
  @NoException
  public static native @ByVal mln_free_camera_options mln_free_camera_options_default();

  /** Returns empty axonometric rendering options initialized for this C API version. */
  @NoException
  public static native @ByVal mln_projection_mode mln_projection_mode_default();

  /** Returns empty viewport options initialized for this C API version. */
  @NoException
  public static native @ByVal mln_map_viewport_options mln_map_viewport_options_default();

  /** Returns empty tile tuning options initialized for this C API version. */
  @NoException
  public static native @ByVal mln_map_tile_options mln_map_tile_options_default();

  /**
   * Applies MapLibre debug overlay mask bits to a map.
   *
   * <p>Pass 0 to disable all debug overlays.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, or options contains unknown bits. - MLN_STATUS_WRONG_THREAD when called from a thread
   * other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_debug_options(
      mln_map map, @Cast("uint32_t") int options);

  /**
   * Copies the current MapLibre debug overlay mask bits.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, or out_options is null. - MLN_STATUS_WRONG_THREAD when called from a thread other than
   * the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to
   * status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_get_debug_options(
      mln_map map, @Cast("uint32_t*") IntPointer out_options);

  @NoException
  public static native @Cast("mln_status") int mln_map_get_debug_options(
      mln_map map, @Cast("uint32_t*") IntBuffer out_options);

  @NoException
  public static native @Cast("mln_status") int mln_map_get_debug_options(
      mln_map map, @Cast("uint32_t*") int[] out_options);

  /**
   * Enables or disables MapLibre's rendering stats overlay view.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_rendering_stats_view_enabled(
      mln_map map, @Cast("bool") boolean enabled);

  /**
   * Copies whether MapLibre's rendering stats overlay view is enabled.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, or out_enabled is null. - MLN_STATUS_WRONG_THREAD when called from a thread other than
   * the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to
   * status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_get_rendering_stats_view_enabled(
      mln_map map, @Cast("bool*") BoolPointer out_enabled);

  @NoException
  public static native @Cast("mln_status") int mln_map_get_rendering_stats_view_enabled(
      mln_map map, @Cast("bool*") boolean[] out_enabled);

  /**
   * Copies whether MapLibre currently considers the map fully loaded.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, or out_loaded is null. - MLN_STATUS_WRONG_THREAD when called from a thread other than the
   * map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_is_fully_loaded(
      mln_map map, @Cast("bool*") BoolPointer out_loaded);

  @NoException
  public static native @Cast("mln_status") int mln_map_is_fully_loaded(
      mln_map map, @Cast("bool*") boolean[] out_loaded);

  /**
   * Dumps map debug logs through MapLibre Native logging.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_dump_debug_logs(mln_map map);

  /**
   * Copies live map viewport and render-transform controls.
   *
   * <p>On success, *out_options is overwritten and all known fields are marked.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, out_options is null, or out_options->size is too small. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_get_viewport_options(
      mln_map map, mln_map_viewport_options out_options);

  /**
   * Applies selected live map viewport and render-transform controls.
   *
   * <p>Only fields indicated by options->fields affect the map.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, options is null, options->size is too small, options->fields contains unknown bits, an
   * enum value is unknown, or an enabled frustum offset value is invalid. - MLN_STATUS_WRONG_THREAD
   * when called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_viewport_options(
      mln_map map, @Const mln_map_viewport_options options);

  /**
   * Copies tile prefetch and LOD tuning controls.
   *
   * <p>On success, *out_options is overwritten and all known fields are marked.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, out_options is null, or out_options->size is too small. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_get_tile_options(
      mln_map map, mln_map_tile_options out_options);

  /**
   * Applies selected tile prefetch and LOD tuning controls.
   *
   * <p>Only fields indicated by options->fields affect the map.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, options is null, options->size is too small, options->fields contains unknown bits,
   * prefetch_zoom_delta is greater than 255, a double field is non-finite, or lod_mode is unknown.
   * - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_tile_options(
      mln_map map, @Const mln_map_tile_options options);

  /**
   * Copies the current camera snapshot.
   *
   * <p>On success, *out_camera is overwritten.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, out_camera is null, or out_camera->size is too small. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_get_camera(
      mln_map map, mln_camera_options out_camera);

  /**
   * Applies a camera jump command.
   *
   * <p>Only fields indicated by camera->fields affect the map.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, camera is null, camera->size is too small, camera->fields contains unknown bits, or an
   * enabled camera field is invalid. - MLN_STATUS_WRONG_THREAD when called from a thread other than
   * the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to
   * status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_jump_to(
      mln_map map, @Const mln_camera_options camera);

  /**
   * Applies a camera ease transition command.
   *
   * <p>Only fields indicated by camera->fields affect the map. Passing a null animation uses
   * MapLibre Native's default animation options.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, camera is null, camera->size is too small, camera->fields contains unknown bits, an
   * enabled camera field is invalid, animation->size is too small, animation->fields contains
   * unknown bits, or an enabled animation field is invalid. - MLN_STATUS_WRONG_THREAD when called
   * from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_ease_to(
      mln_map map, @Const mln_camera_options camera, @Const mln_animation_options animation);

  /**
   * Applies a camera fly transition command.
   *
   * <p>Only fields indicated by camera->fields affect the map. Passing a null animation uses
   * MapLibre Native's default animation options.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, camera is null, camera->size is too small, camera->fields contains unknown bits, an
   * enabled camera field is invalid, animation->size is too small, animation->fields contains
   * unknown bits, or an enabled animation field is invalid. - MLN_STATUS_WRONG_THREAD when called
   * from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_fly_to(
      mln_map map, @Const mln_camera_options camera, @Const mln_animation_options animation);

  /**
   * Applies a screen-space pan command.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, or a delta value is non-finite. - MLN_STATUS_WRONG_THREAD when called from a thread other
   * than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to
   * status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_move_by(
      mln_map map, double delta_x, double delta_y);

  /**
   * Applies an animated screen-space pan command.
   *
   * <p>Passing a null animation uses MapLibre Native's default animation options.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, a delta value is non-finite, animation->size is too small, animation->fields contains
   * unknown bits, or an enabled animation field is invalid. - MLN_STATUS_WRONG_THREAD when called
   * from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_move_by_animated(
      mln_map map, double delta_x, double delta_y, @Const mln_animation_options animation);

  /**
   * Applies a screen-space zoom command.
   *
   * <p>Passing a null anchor uses MapLibre Native's default zoom anchor.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, scale is non-positive or non-finite, or anchor contains non-finite values. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_scale_by(
      mln_map map, double scale, @Const mln_screen_point anchor);

  /**
   * Applies an animated screen-space zoom command.
   *
   * <p>Passing a null anchor uses MapLibre Native's default zoom anchor. Passing a null animation
   * uses MapLibre Native's default animation options.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, scale is non-positive or non-finite, anchor contains non-finite values, animation->size
   * is too small, animation->fields contains unknown bits, or an enabled animation field is
   * invalid. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_scale_by_animated(
      mln_map map,
      double scale,
      @Const mln_screen_point anchor,
      @Const mln_animation_options animation);

  /**
   * Applies a screen-space rotate command.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, or a point contains non-finite values. - MLN_STATUS_WRONG_THREAD when called from a
   * thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_rotate_by(
      mln_map map, @ByVal mln_screen_point first, @ByVal mln_screen_point second);

  /**
   * Applies an animated screen-space rotate command.
   *
   * <p>Passing a null animation uses MapLibre Native's default animation options.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, a point contains non-finite values, animation->size is too small, animation->fields
   * contains unknown bits, or an enabled animation field is invalid. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_rotate_by_animated(
      mln_map map,
      @ByVal mln_screen_point first,
      @ByVal mln_screen_point second,
      @Const mln_animation_options animation);

  /**
   * Applies a pitch delta command.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, or pitch is non-finite. - MLN_STATUS_WRONG_THREAD when called from a thread other than
   * the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to
   * status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_pitch_by(mln_map map, double pitch);

  /**
   * Applies an animated pitch delta command.
   *
   * <p>Passing a null animation uses MapLibre Native's default animation options.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, pitch is non-finite, animation->size is too small, animation->fields contains unknown
   * bits, or an enabled animation field is invalid. - MLN_STATUS_WRONG_THREAD when called from a
   * thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_pitch_by_animated(
      mln_map map, double pitch, @Const mln_animation_options animation);

  /**
   * Cancels active camera transitions.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_cancel_transitions(mln_map map);

  /**
   * Computes a camera that fits geographic bounds in the current viewport.
   *
   * <p>Passing null fit_options uses zero padding with no bearing or pitch override. On success,
   * *out_camera is overwritten.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, bounds are invalid, fit_options is invalid, out_camera is null, or out_camera->size is
   * too small. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread.
   * - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_camera_for_lat_lng_bounds(
      mln_map map,
      @ByVal mln_lat_lng_bounds bounds,
      @Const mln_camera_fit_options fit_options,
      mln_camera_options out_camera);

  /**
   * Computes a camera that fits geographic coordinates in the current viewport.
   *
   * <p>The coordinates array is borrowed for the duration of this call and is not retained. Passing
   * null fit_options uses zero padding with no bearing or pitch override. On success, *out_camera
   * is overwritten.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, coordinates is null, coordinate_count is 0, any coordinate is invalid, fit_options is
   * invalid, out_camera is null, or out_camera->size is too small. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_camera_for_lat_lngs(
      mln_map map,
      @Const mln_lat_lng coordinates,
      @Cast("size_t") long coordinate_count,
      @Const mln_camera_fit_options fit_options,
      mln_camera_options out_camera);

  /**
   * Computes a camera that fits a geometry in the current viewport.
   *
   * <p>The geometry descriptor graph is borrowed for the duration of this call and is not retained.
   * Empty geometry objects and geometry collections with no coordinates are invalid for camera
   * fitting. Passing null fit_options uses zero padding with no bearing or pitch override. On
   * success, *out_camera is overwritten.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, geometry is null or invalid, geometry contains no coordinates, fit_options is invalid,
   * out_camera is null, or out_camera->size is too small. - MLN_STATUS_WRONG_THREAD when called
   * from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_camera_for_geometry(
      mln_map map,
      @Const mln_geometry geometry,
      @Const mln_camera_fit_options fit_options,
      mln_camera_options out_camera);

  /**
   * Computes wrapped geographic bounds for a camera in the current viewport.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, camera is null or invalid, or out_bounds is null. - MLN_STATUS_WRONG_THREAD when called
   * from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_lat_lng_bounds_for_camera(
      mln_map map, @Const mln_camera_options camera, mln_lat_lng_bounds out_bounds);

  /**
   * Computes unwrapped geographic bounds for a camera in the current viewport.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, camera is null or invalid, or out_bounds is null. - MLN_STATUS_WRONG_THREAD when called
   * from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_lat_lng_bounds_for_camera_unwrapped(
      mln_map map, @Const mln_camera_options camera, mln_lat_lng_bounds out_bounds);

  /**
   * Copies map camera constraint options.
   *
   * <p>On success, *out_options is overwritten and all known fields are marked.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, out_options is null, or out_options->size is too small. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_get_bounds(
      mln_map map, mln_bound_options out_options);

  /**
   * Applies selected map camera constraint options.
   *
   * <p>Only fields indicated by options->fields affect the map.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, options is null, options->size is too small, options->fields contains unknown bits,
   * bounds are invalid, a numeric field is non-finite, or paired min/max fields are inconsistent. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_bounds(
      mln_map map, @Const mln_bound_options options);

  /**
   * Copies the current free camera position and orientation.
   *
   * <p>On success, *out_options is overwritten.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, out_options is null, or out_options->size is too small. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_get_free_camera_options(
      mln_map map, mln_free_camera_options out_options);

  /**
   * Applies selected free camera position and orientation fields.
   *
   * <p>Position uses MapLibre Native's modified Web Mercator camera space. Orientation is a
   * quaternion stored as x, y, z, w. Only fields indicated by options->fields affect the map.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, options is null, options->size is too small, options->fields contains unknown bits,
   * position contains non-finite values, or orientation contains non-finite values or is zero
   * length. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_free_camera_options(
      mln_map map, @Const mln_free_camera_options options);

  /**
   * Copies the current axonometric rendering options.
   *
   * <p>On success, *out_mode is overwritten. MapLibre currently reports all fields.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, out_mode is null, or out_mode->size is too small. - MLN_STATUS_WRONG_THREAD when called
   * from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_get_projection_mode(
      mln_map map, mln_projection_mode out_mode);

  /**
   * Applies axonometric rendering option fields to a map.
   *
   * <p>Only fields indicated by mode->fields affect the map. Unspecified fields keep their current
   * native values. These options mutate the live map render transform and do not change coordinate
   * conversion units or formulas.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, mode is null, mode->size is too small, mode->fields contains unknown bits, or an enabled
   * skew value is non-finite. - MLN_STATUS_WRONG_THREAD when called from a thread other than the
   * map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_projection_mode(
      mln_map map, @Const mln_projection_mode mode);

  /**
   * Converts a geographic world coordinate to a screen point for the current map.
   *
   * <p>The output point uses logical map pixels with an origin at the top-left of the map viewport.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, out_point is null, or coordinate contains invalid latitude or longitude values. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_pixel_for_lat_lng(
      mln_map map, @ByVal mln_lat_lng coordinate, mln_screen_point out_point);

  /**
   * Converts a screen point to a geographic world coordinate for the current map.
   *
   * <p>The input point uses logical map pixels with an origin at the top-left of the map viewport.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, out_coordinate is null, or point contains non-finite values. - MLN_STATUS_WRONG_THREAD
   * when called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_lat_lng_for_pixel(
      mln_map map, @ByVal mln_screen_point point, mln_lat_lng out_coordinate);

  /**
   * Converts geographic world coordinates to screen points for the current map.
   *
   * <p>The caller owns both arrays. On success, out_points receives coordinate_count entries.
   * coordinates and out_points may be null only when coordinate_count is 0.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, a required array is null, or any coordinate contains invalid latitude or longitude
   * values. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_pixels_for_lat_lngs(
      mln_map map,
      @Const mln_lat_lng coordinates,
      @Cast("size_t") long coordinate_count,
      mln_screen_point out_points);

  /**
   * Converts screen points to geographic world coordinates for the current map.
   *
   * <p>The caller owns both arrays. On success, out_coordinates receives point_count entries.
   * points and out_coordinates may be null only when point_count is 0.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, a required array is null, or any point contains non-finite values. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_lat_lngs_for_pixels(
      mln_map map,
      @Const mln_screen_point points,
      @Cast("size_t") long point_count,
      mln_lat_lng out_coordinates);

  // #ifdef __cplusplus
  // #endif

  // #endif  // MAPLIBRE_NATIVE_C_CAMERA_H

  // Parsed from maplibre_native_c/render_target.h

  /**
   * \file maplibre_native_c/render_target.h Shared public C API declarations for render target
   * descriptors.
   */

  // #ifndef MAPLIBRE_NATIVE_C_RENDER_TARGET_H
  // #define MAPLIBRE_NATIVE_C_RENDER_TARGET_H

  // #include <stdint.h>

  // #include "base.h"

  // #ifdef __cplusplus
  // #endif

  /** Logical render target extent in UI pixels. */
  public static class mln_render_target_extent extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_render_target_extent() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_render_target_extent(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_render_target_extent(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_render_target_extent position(long position) {
      return (mln_render_target_extent) super.position(position);
    }

    @Override
    public mln_render_target_extent getPointer(long i) {
      return new mln_render_target_extent((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_render_target_extent size(int setter);

    /** Logical map width in UI pixels. */
    public native @Cast("uint32_t") int width();

    public native mln_render_target_extent width(int setter);

    /** Logical map height in UI pixels. */
    public native @Cast("uint32_t") int height();

    public native mln_render_target_extent height(int setter);

    /** UI-to-device pixel scale. Must be positive and finite. */
    public native double scale_factor();

    public native mln_render_target_extent scale_factor(double setter);
  }

  /** Metal backend context fields shared by Metal render targets. */
  public static class mln_metal_context_descriptor extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_metal_context_descriptor() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_metal_context_descriptor(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_metal_context_descriptor(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_metal_context_descriptor position(long position) {
      return (mln_metal_context_descriptor) super.position(position);
    }

    @Override
    public mln_metal_context_descriptor getPointer(long i) {
      return new mln_metal_context_descriptor((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_metal_context_descriptor size(int setter);

    /** id<MTLDevice> / MTL::Device*. Retained when the target requires it. */
    public native Pointer device();

    public native mln_metal_context_descriptor device(Pointer setter);
  }

  /** Vulkan backend context fields shared by Vulkan render targets. */
  public static class mln_vulkan_context_descriptor extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_vulkan_context_descriptor() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_vulkan_context_descriptor(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_vulkan_context_descriptor(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_vulkan_context_descriptor position(long position) {
      return (mln_vulkan_context_descriptor) super.position(position);
    }

    @Override
    public mln_vulkan_context_descriptor getPointer(long i) {
      return new mln_vulkan_context_descriptor((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_vulkan_context_descriptor size(int setter);

    /** Borrowed VkInstance. Required. */
    public native Pointer instance();

    public native mln_vulkan_context_descriptor instance(Pointer setter);

    /** Borrowed VkPhysicalDevice. Required. */
    public native Pointer physical_device();

    public native mln_vulkan_context_descriptor physical_device(Pointer setter);

    /** Borrowed VkDevice. Required. */
    public native Pointer device();

    public native mln_vulkan_context_descriptor device(Pointer setter);

    /** Borrowed graphics VkQueue. Required. */
    public native Pointer graphics_queue();

    public native mln_vulkan_context_descriptor graphics_queue(Pointer setter);

    /** Queue family index for graphics_queue. Must support graphics commands. */
    public native @Cast("uint32_t") int graphics_queue_family_index();

    public native mln_vulkan_context_descriptor graphics_queue_family_index(int setter);
  }

  // #ifdef __cplusplus
  // #endif

  // #endif  // MAPLIBRE_NATIVE_C_RENDER_TARGET_H

  // Parsed from maplibre_native_c/texture.h

  /** \file maplibre_native_c/texture.h Public C API declarations for texture render targets. */

  // #ifndef MAPLIBRE_NATIVE_C_TEXTURE_H
  // #define MAPLIBRE_NATIVE_C_TEXTURE_H

  // #include <stddef.h>
  // #include <stdint.h>

  // #include "base.h"
  // #include "render_target.h"

  // #ifdef __cplusplus
  // #endif

  /** Metal texture session attachment options for a session-owned target. */
  public static class mln_metal_owned_texture_descriptor extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_metal_owned_texture_descriptor() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_metal_owned_texture_descriptor(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_metal_owned_texture_descriptor(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_metal_owned_texture_descriptor position(long position) {
      return (mln_metal_owned_texture_descriptor) super.position(position);
    }

    @Override
    public mln_metal_owned_texture_descriptor getPointer(long i) {
      return new mln_metal_owned_texture_descriptor((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_metal_owned_texture_descriptor size(int setter);

    /** Logical texture extent. */
    public native @ByRef mln_render_target_extent extent();

    public native mln_metal_owned_texture_descriptor extent(mln_render_target_extent setter);

    /** Metal backend context. device is required. */
    public native @ByRef mln_metal_context_descriptor context();

    public native mln_metal_owned_texture_descriptor context(mln_metal_context_descriptor setter);
  }

  /** Metal caller-owned texture session attachment options. */
  public static class mln_metal_borrowed_texture_descriptor extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_metal_borrowed_texture_descriptor() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_metal_borrowed_texture_descriptor(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_metal_borrowed_texture_descriptor(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_metal_borrowed_texture_descriptor position(long position) {
      return (mln_metal_borrowed_texture_descriptor) super.position(position);
    }

    @Override
    public mln_metal_borrowed_texture_descriptor getPointer(long i) {
      return new mln_metal_borrowed_texture_descriptor((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_metal_borrowed_texture_descriptor size(int setter);

    /** Logical texture extent. */
    public native @ByRef mln_render_target_extent extent();

    public native mln_metal_borrowed_texture_descriptor extent(mln_render_target_extent setter);

    /**
     * Borrowed id<MTLTexture> / MTL::Texture*. Required.
     *
     * <p>The texture's physical pixel dimensions must match extent, and the texture must allow
     * render-target usage. The caller owns the texture and must keep it valid until detach or
     * destroy.
     */
    public native Pointer texture();

    public native mln_metal_borrowed_texture_descriptor texture(Pointer setter);
  }

  /** Metal frame acquired from a session-owned texture target. */
  public static class mln_metal_owned_texture_frame extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_metal_owned_texture_frame() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_metal_owned_texture_frame(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_metal_owned_texture_frame(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_metal_owned_texture_frame position(long position) {
      return (mln_metal_owned_texture_frame) super.position(position);
    }

    @Override
    public mln_metal_owned_texture_frame getPointer(long i) {
      return new mln_metal_owned_texture_frame((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_metal_owned_texture_frame size(int setter);

    /** Session generation that produced this frame. */
    public native @Cast("uint64_t") long generation();

    public native mln_metal_owned_texture_frame generation(long setter);

    /** Physical Metal texture width in device pixels. */
    public native @Cast("uint32_t") int width();

    public native mln_metal_owned_texture_frame width(int setter);

    /** Physical Metal texture height in device pixels. */
    public native @Cast("uint32_t") int height();

    public native mln_metal_owned_texture_frame height(int setter);

    /** UI-to-device pixel scale used for this frame. */
    public native double scale_factor();

    public native mln_metal_owned_texture_frame scale_factor(double setter);

    /** Opaque frame identity used to reject stale releases. */
    public native @Cast("uint64_t") long frame_id();

    public native mln_metal_owned_texture_frame frame_id(long setter);

    /** Borrowed id<MTLTexture> / MTL::Texture*. Valid until frame release. */
    public native Pointer texture();

    public native mln_metal_owned_texture_frame texture(Pointer setter);

    /** Borrowed id<MTLDevice> / MTL::Device*. Valid until frame release. */
    public native Pointer device();

    public native mln_metal_owned_texture_frame device(Pointer setter);

    /** Backend-native pixel format value. Metal uses MTLPixelFormat. */
    public native @Cast("uint64_t") long pixel_format();

    public native mln_metal_owned_texture_frame pixel_format(long setter);
  }

  /** Vulkan texture session attachment options for a session-owned target. */
  public static class mln_vulkan_owned_texture_descriptor extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_vulkan_owned_texture_descriptor() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_vulkan_owned_texture_descriptor(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_vulkan_owned_texture_descriptor(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_vulkan_owned_texture_descriptor position(long position) {
      return (mln_vulkan_owned_texture_descriptor) super.position(position);
    }

    @Override
    public mln_vulkan_owned_texture_descriptor getPointer(long i) {
      return new mln_vulkan_owned_texture_descriptor((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_vulkan_owned_texture_descriptor size(int setter);

    /** Logical texture extent. */
    public native @ByRef mln_render_target_extent extent();

    public native mln_vulkan_owned_texture_descriptor extent(mln_render_target_extent setter);

    /** Borrowed Vulkan context. All handles are required. */
    public native @ByRef mln_vulkan_context_descriptor context();

    public native mln_vulkan_owned_texture_descriptor context(mln_vulkan_context_descriptor setter);
  }

  /** Vulkan caller-owned texture session attachment options. */
  public static class mln_vulkan_borrowed_texture_descriptor extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_vulkan_borrowed_texture_descriptor() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_vulkan_borrowed_texture_descriptor(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_vulkan_borrowed_texture_descriptor(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_vulkan_borrowed_texture_descriptor position(long position) {
      return (mln_vulkan_borrowed_texture_descriptor) super.position(position);
    }

    @Override
    public mln_vulkan_borrowed_texture_descriptor getPointer(long i) {
      return new mln_vulkan_borrowed_texture_descriptor((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_vulkan_borrowed_texture_descriptor size(int setter);

    /** Logical texture extent. */
    public native @ByRef mln_render_target_extent extent();

    public native mln_vulkan_borrowed_texture_descriptor extent(mln_render_target_extent setter);

    /** Borrowed Vulkan context. All handles are required. */
    public native @ByRef mln_vulkan_context_descriptor context();

    public native mln_vulkan_borrowed_texture_descriptor context(
        mln_vulkan_context_descriptor setter);

    /**
     * Borrowed VkImage. Required.
     *
     * <p>The image must be a 2D, single-sample color image with
     * VK_IMAGE_USAGE_COLOR_ATTACHMENT_BIT. Its physical dimensions must match extent. Include
     * VK_IMAGE_USAGE_SAMPLED_BIT when the host will sample from the image after rendering.
     */
    public native Pointer image();

    public native mln_vulkan_borrowed_texture_descriptor image(Pointer setter);

    /**
     * Borrowed VkImageView for image. Required.
     *
     * <p>The view must be a 2D color view that matches image and format.
     */
    public native Pointer image_view();

    public native mln_vulkan_borrowed_texture_descriptor image_view(Pointer setter);

    /** Backend-native VkFormat value for image. VK_FORMAT_UNDEFINED is invalid. */
    public native @Cast("uint32_t") int format();

    public native mln_vulkan_borrowed_texture_descriptor format(int setter);

    /**
     * Backend-native VkImageLayout value expected at render-pass begin.
     *
     * <p>Use VK_IMAGE_LAYOUT_UNDEFINED when the previous image contents may be discarded.
     */
    public native @Cast("uint32_t") int initial_layout();

    public native mln_vulkan_borrowed_texture_descriptor initial_layout(int setter);

    /** Backend-native VkImageLayout value left after rendering succeeds. */
    public native @Cast("uint32_t") int final_layout();

    public native mln_vulkan_borrowed_texture_descriptor final_layout(int setter);
  }

  /** Vulkan frame acquired from a session-owned texture target. */
  public static class mln_vulkan_owned_texture_frame extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_vulkan_owned_texture_frame() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_vulkan_owned_texture_frame(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_vulkan_owned_texture_frame(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_vulkan_owned_texture_frame position(long position) {
      return (mln_vulkan_owned_texture_frame) super.position(position);
    }

    @Override
    public mln_vulkan_owned_texture_frame getPointer(long i) {
      return new mln_vulkan_owned_texture_frame((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_vulkan_owned_texture_frame size(int setter);

    /** Session generation that produced this frame. */
    public native @Cast("uint64_t") long generation();

    public native mln_vulkan_owned_texture_frame generation(long setter);

    /** Physical Vulkan image width in device pixels. */
    public native @Cast("uint32_t") int width();

    public native mln_vulkan_owned_texture_frame width(int setter);

    /** Physical Vulkan image height in device pixels. */
    public native @Cast("uint32_t") int height();

    public native mln_vulkan_owned_texture_frame height(int setter);

    /** UI-to-device pixel scale used for this frame. */
    public native double scale_factor();

    public native mln_vulkan_owned_texture_frame scale_factor(double setter);

    /** Opaque frame identity used to reject stale releases. */
    public native @Cast("uint64_t") long frame_id();

    public native mln_vulkan_owned_texture_frame frame_id(long setter);

    /** Borrowed VkImage. Valid until frame release. */
    public native Pointer image();

    public native mln_vulkan_owned_texture_frame image(Pointer setter);

    /** Borrowed VkImageView. Valid until frame release. */
    public native Pointer image_view();

    public native mln_vulkan_owned_texture_frame image_view(Pointer setter);

    /** Borrowed VkDevice. Valid until frame release. */
    public native Pointer device();

    public native mln_vulkan_owned_texture_frame device(Pointer setter);

    /** Backend-native VkFormat value. */
    public native @Cast("uint32_t") int format();

    public native mln_vulkan_owned_texture_frame format(int setter);

    /** Backend-native VkImageLayout value; Vulkan frames are host-sampleable. */
    public native @Cast("uint32_t") int layout();

    public native mln_vulkan_owned_texture_frame layout(int setter);
  }

  /** CPU image readback metadata for a texture session frame. */
  public static class mln_texture_image_info extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_texture_image_info() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_texture_image_info(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_texture_image_info(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_texture_image_info position(long position) {
      return (mln_texture_image_info) super.position(position);
    }

    @Override
    public mln_texture_image_info getPointer(long i) {
      return new mln_texture_image_info((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_texture_image_info size(int setter);

    /** Physical image width in device pixels. */
    public native @Cast("uint32_t") int width();

    public native mln_texture_image_info width(int setter);

    /** Physical image height in device pixels. */
    public native @Cast("uint32_t") int height();

    public native mln_texture_image_info height(int setter);

    /** Bytes per image row. */
    public native @Cast("uint32_t") int stride();

    public native mln_texture_image_info stride(int setter);

    /** Required output buffer byte length. */
    public native @Cast("size_t") long byte_length();

    public native mln_texture_image_info byte_length(long setter);
  }

  /**
   * Returns Metal owned-texture descriptor defaults for this C API version.
   *
   * <p>
   */
  @NoException
  public static native @ByVal mln_metal_owned_texture_descriptor
      mln_metal_owned_texture_descriptor_default();

  /** Returns Metal borrowed-texture descriptor defaults for this C API version. */
  @NoException
  public static native @ByVal mln_metal_borrowed_texture_descriptor
      mln_metal_borrowed_texture_descriptor_default();

  /** Returns Vulkan owned-texture descriptor defaults for this C API version. */
  @NoException
  public static native @ByVal mln_vulkan_owned_texture_descriptor
      mln_vulkan_owned_texture_descriptor_default();

  /** Returns Vulkan borrowed-texture descriptor defaults for this C API version. */
  @NoException
  public static native @ByVal mln_vulkan_borrowed_texture_descriptor
      mln_vulkan_borrowed_texture_descriptor_default();

  /** Returns texture image info defaults for this C API version. */
  @NoException
  public static native @ByVal mln_texture_image_info mln_texture_image_info_default();

  /**
   * Attaches a Metal texture render target owned by the session to a map.
   *
   * <p>The map may have at most one live render session. The session and every texture-session call
   * are owner-thread affine to the map owner thread. The session renders into a session-owned
   * texture created on descriptor->context.device. On success, *out_session receives a handle the
   * caller destroys with mln_render_session_destroy().
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, descriptor is null or invalid, out_session is null, or *out_session is not null. -
   * MLN_STATUS_INVALID_STATE when the map already has a render session. - MLN_STATUS_WRONG_THREAD
   * when called from a thread other than the map owner thread. - MLN_STATUS_UNSUPPORTED when Metal
   * texture sessions are not supported by this build. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_metal_owned_texture_attach(
      mln_map map,
      @Const mln_metal_owned_texture_descriptor descriptor,
      @Cast("mln_render_session**") PointerPointer out_session);

  @NoException
  public static native @Cast("mln_status") int mln_metal_owned_texture_attach(
      mln_map map,
      @Const mln_metal_owned_texture_descriptor descriptor,
      @ByPtrPtr mln_render_session out_session);

  /**
   * Attaches a Metal caller-owned texture render target to a map.
   *
   * <p>The map may have at most one live render session. The session and every texture-session call
   * are owner-thread affine to the map owner thread. The session renders into descriptor->texture.
   * The caller owns the texture, keeps it valid until detach or destroy, and synchronizes any use
   * outside this session. On success, *out_session receives a handle the caller destroys with
   * mln_render_session_destroy().
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, descriptor is null or invalid, out_session is null, or *out_session is not null. -
   * MLN_STATUS_INVALID_STATE when the map already has a render session. - MLN_STATUS_WRONG_THREAD
   * when called from a thread other than the map owner thread. - MLN_STATUS_UNSUPPORTED when Metal
   * borrowed texture sessions are not supported by this build. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_metal_borrowed_texture_attach(
      mln_map map,
      @Const mln_metal_borrowed_texture_descriptor descriptor,
      @Cast("mln_render_session**") PointerPointer out_session);

  @NoException
  public static native @Cast("mln_status") int mln_metal_borrowed_texture_attach(
      mln_map map,
      @Const mln_metal_borrowed_texture_descriptor descriptor,
      @ByPtrPtr mln_render_session out_session);

  /**
   * Attaches a Vulkan texture render target owned by the session to a map.
   *
   * <p>The map may have at most one live render session. The session and every texture-session call
   * are owner-thread affine to the map owner thread. The session renders into a session-owned image
   * created on descriptor->context.device. Vulkan handles are borrowed and must remain valid until
   * detach or destroy. On success, *out_session receives a handle the caller destroys with
   * mln_render_session_destroy().
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, descriptor is null or invalid, out_session is null, or *out_session is not null. -
   * MLN_STATUS_INVALID_STATE when the map already has a render session. - MLN_STATUS_WRONG_THREAD
   * when called from a thread other than the map owner thread. - MLN_STATUS_UNSUPPORTED when Vulkan
   * texture sessions are not supported by this build. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_vulkan_owned_texture_attach(
      mln_map map,
      @Const mln_vulkan_owned_texture_descriptor descriptor,
      @Cast("mln_render_session**") PointerPointer out_session);

  @NoException
  public static native @Cast("mln_status") int mln_vulkan_owned_texture_attach(
      mln_map map,
      @Const mln_vulkan_owned_texture_descriptor descriptor,
      @ByPtrPtr mln_render_session out_session);

  /**
   * Attaches a Vulkan caller-owned texture render target to a map.
   *
   * <p>The map may have at most one live render session. The session and every texture-session call
   * are owner-thread affine to the map owner thread. The session renders into descriptor->image
   * through descriptor->image_view. The caller owns the image and view, keeps them valid until
   * detach or destroy, and handles queue-family ownership and synchronization outside this session.
   * On success, *out_session receives a handle the caller destroys with
   * mln_render_session_destroy().
   *
   * <p>Before each mln_render_session_render_update(), make the image available on
   * descriptor->context.graphics_queue in descriptor->initial_layout and keep it out of concurrent
   * use. The session submits rendering on that queue, waits for the submitted work to finish, and
   * leaves the image in descriptor->final_layout before mln_render_session_render_update() returns.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, descriptor is null or invalid, out_session is null, or *out_session is not null. -
   * MLN_STATUS_INVALID_STATE when the map already has a render session. - MLN_STATUS_WRONG_THREAD
   * when called from a thread other than the map owner thread. - MLN_STATUS_UNSUPPORTED when Vulkan
   * borrowed texture sessions are not supported by this build. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_vulkan_borrowed_texture_attach(
      mln_map map,
      @Const mln_vulkan_borrowed_texture_descriptor descriptor,
      @Cast("mln_render_session**") PointerPointer out_session);

  @NoException
  public static native @Cast("mln_status") int mln_vulkan_borrowed_texture_attach(
      mln_map map,
      @Const mln_vulkan_borrowed_texture_descriptor descriptor,
      @ByPtrPtr mln_render_session out_session);

  /**
   * Reads the most recently rendered session-owned texture frame into caller-owned storage.
   *
   * <p>The copied image is premultiplied RGBA8 in physical pixels. The function fills out_info with
   * the required byte length and image layout metadata. When out_data is null or out_data_capacity
   * is too small, out_info is still filled and the function returns MLN_STATUS_INVALID_ARGUMENT.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live, out_info is null, out_info->size is too small, out_data is null, or out_data_capacity
   * is too small. - MLN_STATUS_INVALID_STATE when no rendered frame is available, the session is
   * detached, a frame is currently acquired, or readback produces no image. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the session owner thread. -
   * MLN_STATUS_UNSUPPORTED when session is not a texture session or when the texture session uses a
   * caller-owned target. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to
   * status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_texture_read_premultiplied_rgba8(
      mln_render_session session,
      @Cast("uint8_t*") BytePointer out_data,
      @Cast("size_t") long out_data_capacity,
      mln_texture_image_info out_info);

  @NoException
  public static native @Cast("mln_status") int mln_texture_read_premultiplied_rgba8(
      mln_render_session session,
      @Cast("uint8_t*") ByteBuffer out_data,
      @Cast("size_t") long out_data_capacity,
      mln_texture_image_info out_info);

  @NoException
  public static native @Cast("mln_status") int mln_texture_read_premultiplied_rgba8(
      mln_render_session session,
      @Cast("uint8_t*") byte[] out_data,
      @Cast("size_t") long out_data_capacity,
      mln_texture_image_info out_info);

  /**
   * Acquires the most recently rendered Metal texture frame.
   *
   * <p>Use this function with sessions created by mln_metal_owned_texture_attach().
   *
   * <p>The returned texture and device pointers are borrowed and remain valid only until
   * mln_metal_owned_texture_release_frame() is called for the same frame. While acquired, resize,
   * render update, detach, destroy, and a second acquire return MLN_STATUS_INVALID_STATE.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live, out_frame is null, or out_frame->size is too small. - MLN_STATUS_INVALID_STATE when
   * the session is detached, no rendered frame is available, or a texture frame is already
   * acquired. - MLN_STATUS_WRONG_THREAD when called from a thread other than the session owner
   * thread. - MLN_STATUS_UNSUPPORTED when session cannot expose a Metal texture frame. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_metal_owned_texture_acquire_frame(
      mln_render_session session, mln_metal_owned_texture_frame out_frame);

  /**
   * Releases a Metal texture frame acquired from a session-owned texture target.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live, frame is null, frame->size is too small, or frame identity does not match the
   * acquired frame. - MLN_STATUS_INVALID_STATE when no texture frame is currently acquired. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the session owner thread. -
   * MLN_STATUS_UNSUPPORTED when session cannot release a Metal texture frame. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_metal_owned_texture_release_frame(
      mln_render_session session, @Const mln_metal_owned_texture_frame frame);

  /**
   * Acquires the most recently rendered Vulkan texture frame.
   *
   * <p>Use this function with sessions created by mln_vulkan_owned_texture_attach().
   *
   * <p>The returned image, image view, and device pointers are borrowed and remain valid only until
   * mln_vulkan_owned_texture_release_frame() is called for the same frame. While acquired, resize,
   * render update, detach, destroy, and a second acquire return MLN_STATUS_INVALID_STATE.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live, out_frame is null, or out_frame->size is too small. - MLN_STATUS_INVALID_STATE when
   * the session is detached, no rendered frame is available, or a texture frame is already
   * acquired. - MLN_STATUS_WRONG_THREAD when called from a thread other than the session owner
   * thread. - MLN_STATUS_UNSUPPORTED when session cannot expose a Vulkan texture frame. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_vulkan_owned_texture_acquire_frame(
      mln_render_session session, mln_vulkan_owned_texture_frame out_frame);

  /**
   * Releases a Vulkan texture frame acquired from a session-owned texture target.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live, frame is null, frame->size is too small, or frame identity does not match the
   * acquired frame. - MLN_STATUS_INVALID_STATE when no texture frame is currently acquired. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the session owner thread. -
   * MLN_STATUS_UNSUPPORTED when session cannot release a Vulkan texture frame. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_vulkan_owned_texture_release_frame(
      mln_render_session session, @Const mln_vulkan_owned_texture_frame frame);

  // #ifdef __cplusplus
  // #endif

  // #endif  // MAPLIBRE_NATIVE_C_TEXTURE_H

  // Parsed from maplibre_native_c/surface.h

  /** \file maplibre_native_c/surface.h Public C API declarations for surface render targets. */

  // #ifndef MAPLIBRE_NATIVE_C_SURFACE_H
  // #define MAPLIBRE_NATIVE_C_SURFACE_H

  // #include <stdint.h>

  // #include "base.h"
  // #include "render_target.h"

  // #ifdef __cplusplus
  // #endif

  /** Metal native surface session attachment options. */
  public static class mln_metal_surface_descriptor extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_metal_surface_descriptor() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_metal_surface_descriptor(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_metal_surface_descriptor(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_metal_surface_descriptor position(long position) {
      return (mln_metal_surface_descriptor) super.position(position);
    }

    @Override
    public mln_metal_surface_descriptor getPointer(long i) {
      return new mln_metal_surface_descriptor((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_metal_surface_descriptor size(int setter);

    /** Logical surface extent. */
    public native @ByRef mln_render_target_extent extent();

    public native mln_metal_surface_descriptor extent(mln_render_target_extent setter);

    /** Metal backend context. device is optional for Metal surfaces. */
    public native @ByRef mln_metal_context_descriptor context();

    public native mln_metal_surface_descriptor context(mln_metal_context_descriptor setter);

    /** CAMetalLayer* / CA::MetalLayer* retained by the session. Required. */
    public native Pointer layer();

    public native mln_metal_surface_descriptor layer(Pointer setter);
  }

  /** Vulkan native surface session attachment options. */
  public static class mln_vulkan_surface_descriptor extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_vulkan_surface_descriptor() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_vulkan_surface_descriptor(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_vulkan_surface_descriptor(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_vulkan_surface_descriptor position(long position) {
      return (mln_vulkan_surface_descriptor) super.position(position);
    }

    @Override
    public mln_vulkan_surface_descriptor getPointer(long i) {
      return new mln_vulkan_surface_descriptor((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_vulkan_surface_descriptor size(int setter);

    /** Logical surface extent. */
    public native @ByRef mln_render_target_extent extent();

    public native mln_vulkan_surface_descriptor extent(mln_render_target_extent setter);

    /**
     * Borrowed Vulkan context. All handles are required. The device must support VK_KHR_swapchain,
     * and the queue family must support graphics and presentation to this descriptor's surface.
     */
    public native @ByRef mln_vulkan_context_descriptor context();

    public native mln_vulkan_surface_descriptor context(mln_vulkan_context_descriptor setter);

    /** Borrowed VkSurfaceKHR. Required. */
    public native Pointer surface();

    public native mln_vulkan_surface_descriptor surface(Pointer setter);
  }

  /** Returns Metal surface descriptor defaults for this C API version. */
  @NoException
  public static native @ByVal mln_metal_surface_descriptor mln_metal_surface_descriptor_default();

  /** Returns Vulkan surface descriptor defaults for this C API version. */
  @NoException
  public static native @ByVal mln_vulkan_surface_descriptor mln_vulkan_surface_descriptor_default();

  /**
   * Attaches a Metal native surface render target to a map.
   *
   * <p>The map may have at most one live render session. The session and every surface-session call
   * are owner-thread affine to the map owner thread. The session retains descriptor->layer and
   * optional descriptor->context.device. It renders into the layer and presents through it. On
   * success, *out_session receives a handle the caller destroys with mln_render_session_destroy().
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, descriptor is null or invalid, out_session is null, or *out_session is not null. -
   * MLN_STATUS_INVALID_STATE when the map already has a render session. - MLN_STATUS_WRONG_THREAD
   * when called from a thread other than the map owner thread. - MLN_STATUS_UNSUPPORTED when Metal
   * surface sessions are not supported by this build. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_metal_surface_attach(
      mln_map map,
      @Const mln_metal_surface_descriptor descriptor,
      @Cast("mln_render_session**") PointerPointer out_session);

  @NoException
  public static native @Cast("mln_status") int mln_metal_surface_attach(
      mln_map map,
      @Const mln_metal_surface_descriptor descriptor,
      @ByPtrPtr mln_render_session out_session);

  /**
   * Attaches a Vulkan native surface render target to a map.
   *
   * <p>The map may have at most one live render session. The session and every surface-session call
   * are owner-thread affine to the map owner thread. The session renders to descriptor->surface and
   * presents through it. The Vulkan device must support VK_KHR_swapchain, and the queue family must
   * support graphics and presentation to descriptor->surface. Vulkan handles are borrowed and must
   * remain valid until the session is detached or destroyed. On success, *out_session receives a
   * handle the caller destroys with mln_render_session_destroy().
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, descriptor is null or invalid, out_session is null, or *out_session is not null. -
   * MLN_STATUS_INVALID_STATE when the map already has a render session. - MLN_STATUS_WRONG_THREAD
   * when called from a thread other than the map owner thread. - MLN_STATUS_UNSUPPORTED when Vulkan
   * surface sessions are not supported by this build. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_vulkan_surface_attach(
      mln_map map,
      @Const mln_vulkan_surface_descriptor descriptor,
      @Cast("mln_render_session**") PointerPointer out_session);

  @NoException
  public static native @Cast("mln_status") int mln_vulkan_surface_attach(
      mln_map map,
      @Const mln_vulkan_surface_descriptor descriptor,
      @ByPtrPtr mln_render_session out_session);

  // #ifdef __cplusplus
  // #endif

  // #endif  // MAPLIBRE_NATIVE_C_SURFACE_H

  // Parsed from maplibre_native_c/render_session.h

  /** \file maplibre_native_c/render_session.h Public C API declarations for render sessions. */

  // #ifndef MAPLIBRE_NATIVE_C_RENDER_SESSION_H
  // #define MAPLIBRE_NATIVE_C_RENDER_SESSION_H

  // #include <stdint.h>

  // #include "base.h"
  // #include "map.h"

  // #ifdef __cplusplus
  // #endif

  /**
   * Resizes an attached render session.
   *
   * <p>Width and height are logical map dimensions. The scale_factor value maps them to physical
   * backend pixels.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live, dimensions are zero, scale_factor is non-positive or non-finite, or scaled dimensions
   * are too large. - MLN_STATUS_INVALID_STATE when the session is detached or a texture frame is
   * currently acquired. - MLN_STATUS_UNSUPPORTED when resizing is not supported by the session kind
   * or mode, such as a caller-owned borrowed texture target. - MLN_STATUS_WRONG_THREAD when called
   * from a thread other than the session owner thread. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_render_session_resize(
      mln_render_session session,
      @Cast("uint32_t") int width,
      @Cast("uint32_t") int height,
      double scale_factor);

  /**
   * Processes the latest map render update for an attached render session.
   *
   * <p>Surface sessions render and present through their native surface. Texture sessions render
   * into their texture target.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live. - MLN_STATUS_INVALID_STATE when no render update is available or the session is
   * detached, or a texture frame is currently acquired. - MLN_STATUS_WRONG_THREAD when called from
   * a thread other than the session owner thread. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_render_session_render_update(
      mln_render_session session);

  /**
   * Detaches backend-bound render resources from the map while keeping the session handle live for
   * destruction.
   *
   * <p>After detach, resize, render, readback, acquire, and renderer maintenance operations return
   * MLN_STATUS_INVALID_STATE.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live. - MLN_STATUS_INVALID_STATE when already detached or a texture frame is acquired. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the session owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_render_session_detach(
      mln_render_session session);

  /**
   * Destroys a render session handle.
   *
   * <p>If the session is still attached, this function detaches it first.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live. - MLN_STATUS_INVALID_STATE when a texture frame is acquired. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the session owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_render_session_destroy(
      mln_render_session session);

  /**
   * Asks the session renderer to release cached resources where possible.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live. - MLN_STATUS_INVALID_STATE when the session is detached or no renderer has been
   * created for the session yet. - MLN_STATUS_WRONG_THREAD when called from a thread other than the
   * session owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to
   * status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_render_session_reduce_memory_use(
      mln_render_session session);

  /**
   * Clears renderer data for the session.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live. - MLN_STATUS_INVALID_STATE when the session is detached or no renderer has been
   * created for the session yet. - MLN_STATUS_WRONG_THREAD when called from a thread other than the
   * session owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to
   * status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_render_session_clear_data(
      mln_render_session session);

  /**
   * Dumps renderer debug logs for the session through MapLibre Native logging.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live. - MLN_STATUS_INVALID_STATE when the session is detached or no renderer has been
   * created for the session yet. - MLN_STATUS_WRONG_THREAD when called from a thread other than the
   * session owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to
   * status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_render_session_dump_debug_logs(
      mln_render_session session);

  /**
   * Sets per-feature state on a render source for this render session.
   *
   * <p>The session renderer must already exist; call mln_render_session_render_update() once after
   * loading style data before using feature state. selector->source_id and selector->feature_id are
   * borrowed for the duration of the call. state must be a JSON object descriptor and is copied
   * before return. The accepted command requests a map repaint.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live, selector is null or invalid, selector lacks MLN_FEATURE_STATE_SELECTOR_FEATURE_ID,
   * state is null or not an object, state contains invalid descriptor data, or state contains
   * non-finite numbers. - MLN_STATUS_INVALID_STATE when the session is detached or no renderer has
   * been created for the session yet. - MLN_STATUS_WRONG_THREAD when called from a thread other
   * than the session owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_render_session_set_feature_state(
      mln_render_session session,
      @Const mln_feature_state_selector selector,
      @Const mln_json_value state);

  /**
   * Copies per-feature state from a render source in this render session.
   *
   * <p>The session renderer must already exist. selector->source_id and selector->feature_id are
   * borrowed for the duration of the call. On success, *out_state receives an owned snapshot
   * handle. Use mln_json_snapshot_get() to borrow its root JSON object value, and destroy it with
   * mln_json_snapshot_destroy(). Missing native source or feature state is reported as an empty
   * object snapshot.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live, selector is null or invalid, selector lacks MLN_FEATURE_STATE_SELECTOR_FEATURE_ID,
   * out_state is null, or *out_state is not null. - MLN_STATUS_INVALID_STATE when the session is
   * detached or no renderer has been created for the session yet. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the session owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_render_session_get_feature_state(
      mln_render_session session,
      @Const mln_feature_state_selector selector,
      @Cast("mln_json_snapshot**") PointerPointer out_state);

  @NoException
  public static native @Cast("mln_status") int mln_render_session_get_feature_state(
      mln_render_session session,
      @Const mln_feature_state_selector selector,
      @ByPtrPtr mln_json_snapshot out_state);

  /**
   * Removes per-feature state from a render source in this render session.
   *
   * <p>The session renderer must already exist. selector->source_id is required.
   * selector->feature_id and selector->state_key are optional. Passing both removes one state key
   * from one feature. Passing only feature_id removes all state for that feature. Passing neither
   * removes all feature state for the source/source-layer. The accepted command requests a map
   * repaint.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live, selector is null or invalid, or selector has MLN_FEATURE_STATE_SELECTOR_STATE_KEY
   * without MLN_FEATURE_STATE_SELECTOR_FEATURE_ID. - MLN_STATUS_INVALID_STATE when the session is
   * detached or no renderer has been created for the session yet. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the session owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_render_session_remove_feature_state(
      mln_render_session session, @Const mln_feature_state_selector selector);

  /**
   * Borrows the root JSON value from a snapshot handle.
   *
   * <p>The returned pointer and all nested pointers remain valid until the snapshot is destroyed.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when snapshot is null or
   * not live, or out_value is null. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_json_snapshot_get(
      @Const mln_json_snapshot snapshot, @Cast("const mln_json_value**") PointerPointer out_value);

  @NoException
  public static native @Cast("mln_status") int mln_json_snapshot_get(
      @Const mln_json_snapshot snapshot, @Const @ByPtrPtr mln_json_value out_value);

  /** Destroys a JSON snapshot handle. Null is accepted as a no-op. */
  @NoException
  public static native void mln_json_snapshot_destroy(mln_json_snapshot snapshot);

  // #ifdef __cplusplus
  // #endif

  // #endif  // MAPLIBRE_NATIVE_C_RENDER_SESSION_H

  // Parsed from maplibre_native_c/projection.h

  /** \file maplibre_native_c/projection.h Public C API declarations for projection helpers. */

  // #ifndef MAPLIBRE_NATIVE_C_PROJECTION_H
  // #define MAPLIBRE_NATIVE_C_PROJECTION_H

  // #include <stddef.h>

  // #include "base.h"
  // #include "map.h"

  // #ifdef __cplusplus
  // #endif

  /**
   * Creates a standalone projection helper from the current map transform.
   *
   * <p>The helper owns projection and camera transform state only. It does not own style,
   * resources, render targets, or runtime events. Use it to convert coordinates or compute camera
   * fitting without changing the source map.
   *
   * <p>Creation snapshots the map's transform. Later map camera or projection changes do not update
   * the helper. The creating thread owns the helper and must call projection functions on that
   * thread.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, out_projection is null, or *out_projection is not null. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_projection_create(
      mln_map map, @Cast("mln_map_projection**") PointerPointer out_projection);

  @NoException
  public static native @Cast("mln_status") int mln_map_projection_create(
      mln_map map, @ByPtrPtr mln_map_projection out_projection);

  /**
   * Destroys a standalone projection helper.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when projection is null
   * or not live. - MLN_STATUS_WRONG_THREAD when called from a thread other than the projection
   * owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_projection_destroy(
      mln_map_projection projection);

  /**
   * Copies the current camera snapshot from a standalone projection helper.
   *
   * <p>On success, *out_camera is overwritten.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when projection is null
   * or not live, out_camera is null, or out_camera->size is too small. - MLN_STATUS_WRONG_THREAD
   * when called from a thread other than the projection owner thread. - MLN_STATUS_NATIVE_ERROR
   * when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_projection_get_camera(
      mln_map_projection projection, mln_camera_options out_camera);

  /**
   * Applies camera fields to a standalone projection helper.
   *
   * <p>Only fields indicated by camera->fields affect the helper.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when projection is null
   * or not live, camera is null, camera->size is too small, or camera->fields contains unknown
   * bits. - MLN_STATUS_WRONG_THREAD when called from a thread other than the projection owner
   * thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_projection_set_camera(
      mln_map_projection projection, @Const mln_camera_options camera);

  /**
   * Updates a projection helper camera so coordinates are visible within padding.
   *
   * <p>The coordinates array is borrowed for the duration of this call and is not retained. Use
   * mln_map_projection_get_camera() after this call to read the computed camera.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when projection is null
   * or not live, coordinates is null, coordinate_count is 0, padding contains negative or
   * non-finite values, or any coordinate contains invalid latitude or longitude values. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the projection owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_projection_set_visible_coordinates(
      mln_map_projection projection,
      @Const mln_lat_lng coordinates,
      @Cast("size_t") long coordinate_count,
      @ByVal mln_edge_insets padding);

  /**
   * Updates a projection helper camera so geometry coordinates are visible.
   *
   * <p>The geometry descriptor graph, including all nested pointers, is borrowed for the duration
   * of this call and is not retained. Use mln_map_projection_get_camera() after this call to read
   * the computed camera. Empty geometry objects and geometry collections with no coordinates are
   * invalid for camera fitting.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when projection is null
   * or not live, geometry is null or invalid, padding contains negative or non-finite values, or
   * the geometry contains no coordinates. - MLN_STATUS_WRONG_THREAD when called from a thread other
   * than the projection owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_projection_set_visible_geometry(
      mln_map_projection projection, @Const mln_geometry geometry, @ByVal mln_edge_insets padding);

  /**
   * Converts a geographic world coordinate using a standalone projection helper.
   *
   * <p>The output point uses logical map pixels with an origin at the top-left of the helper
   * viewport.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when projection is null
   * or not live, out_point is null, or coordinate contains invalid latitude or longitude values. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the projection owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_projection_pixel_for_lat_lng(
      mln_map_projection projection, @ByVal mln_lat_lng coordinate, mln_screen_point out_point);

  /**
   * Converts a screen point using a standalone projection helper.
   *
   * <p>The input point uses logical map pixels with an origin at the top-left of the helper
   * viewport.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when projection is null
   * or not live, out_coordinate is null, or point contains non-finite values. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the projection owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_projection_lat_lng_for_pixel(
      mln_map_projection projection, @ByVal mln_screen_point point, mln_lat_lng out_coordinate);

  /**
   * Converts a geographic coordinate to spherical Mercator projected meters.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when out_meters is null
   * or coordinate contains invalid latitude or longitude values. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_projected_meters_for_lat_lng(
      @ByVal mln_lat_lng coordinate, mln_projected_meters out_meters);

  /**
   * Converts spherical Mercator projected meters to a geographic coordinate.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when out_coordinate is
   * null or meters contains non-finite values. - MLN_STATUS_NATIVE_ERROR when an internal exception
   * is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_lat_lng_for_projected_meters(
      @ByVal mln_projected_meters meters, mln_lat_lng out_coordinate);

  // #ifdef __cplusplus
  // #endif

  // #endif  // MAPLIBRE_NATIVE_C_PROJECTION_H

  // Parsed from maplibre_native_c/query.h

  /** \file maplibre_native_c/query.h Public C API declarations for feature queries. */

  // #ifndef MAPLIBRE_NATIVE_C_QUERY_H
  // #define MAPLIBRE_NATIVE_C_QUERY_H

  // #include <stddef.h>
  // #include <stdint.h>

  // #include "base.h"
  // #include "map.h"

  // #ifdef __cplusplus
  // #endif

  @Opaque
  public static class mln_feature_query_result extends Pointer {
    /** Empty constructor. Calls {@code super((Pointer)null)}. */
    public mln_feature_query_result() {
      super((Pointer) null);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_feature_query_result(Pointer p) {
      super(p);
    }
  }

  @Opaque
  public static class mln_feature_extension_result extends Pointer {
    /** Empty constructor. Calls {@code super((Pointer)null)}. */
    public mln_feature_extension_result() {
      super((Pointer) null);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_feature_extension_result(Pointer p) {
      super(p);
    }
  }

  /** Rendered feature query geometry variants. */
  /** enum mln_rendered_query_geometry_type */
  public static final int MLN_RENDERED_QUERY_GEOMETRY_TYPE_POINT = 1,
      MLN_RENDERED_QUERY_GEOMETRY_TYPE_BOX = 2,
      MLN_RENDERED_QUERY_GEOMETRY_TYPE_LINE_STRING = 3;

  /** Screen-space box in logical map pixels. */
  public static class mln_screen_box extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_screen_box() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_screen_box(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_screen_box(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_screen_box position(long position) {
      return (mln_screen_box) super.position(position);
    }

    @Override
    public mln_screen_box getPointer(long i) {
      return new mln_screen_box((Pointer) this).offsetAddress(i);
    }

    public native @ByRef mln_screen_point min();

    public native mln_screen_box min(mln_screen_point setter);

    public native @ByRef mln_screen_point max();

    public native mln_screen_box max(mln_screen_point setter);
  }

  /** Screen-space line string in logical map pixels. */
  public static class mln_screen_line_string extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_screen_line_string() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_screen_line_string(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_screen_line_string(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_screen_line_string position(long position) {
      return (mln_screen_line_string) super.position(position);
    }

    @Override
    public mln_screen_line_string getPointer(long i) {
      return new mln_screen_line_string((Pointer) this).offsetAddress(i);
    }

    /** Points. Null only when point_count is 0. */
    public native @Const mln_screen_point points();

    public native mln_screen_line_string points(mln_screen_point setter);

    public native @Cast("size_t") long point_count();

    public native mln_screen_line_string point_count(long setter);
  }

  /** Rendered feature query geometry descriptor. */
  public static class mln_rendered_query_geometry extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_rendered_query_geometry() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_rendered_query_geometry(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_rendered_query_geometry(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_rendered_query_geometry position(long position) {
      return (mln_rendered_query_geometry) super.position(position);
    }

    @Override
    public mln_rendered_query_geometry getPointer(long i) {
      return new mln_rendered_query_geometry((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_rendered_query_geometry size(int setter);

    /** One of mln_rendered_query_geometry_type. */
    public native @Cast("uint32_t") int type();

    public native mln_rendered_query_geometry type(int setter);

    @Name("data.point")
    public native @ByRef mln_screen_point data_point();

    public native mln_rendered_query_geometry data_point(mln_screen_point setter);

    @Name("data.box")
    public native @ByRef mln_screen_box data_box();

    public native mln_rendered_query_geometry data_box(mln_screen_box setter);

    @Name("data.line_string")
    public native @ByRef mln_screen_line_string data_line_string();

    public native mln_rendered_query_geometry data_line_string(mln_screen_line_string setter);
  }

  /** Optional fields for mln_rendered_feature_query_options. */
  /** enum mln_rendered_feature_query_option_field */
  public static final int MLN_RENDERED_FEATURE_QUERY_OPTION_LAYER_IDS = 1 << 0;

  /** Options for rendered feature queries. */
  public static class mln_rendered_feature_query_options extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_rendered_feature_query_options() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_rendered_feature_query_options(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_rendered_feature_query_options(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_rendered_feature_query_options position(long position) {
      return (mln_rendered_feature_query_options) super.position(position);
    }

    @Override
    public mln_rendered_feature_query_options getPointer(long i) {
      return new mln_rendered_feature_query_options((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_rendered_feature_query_options size(int setter);

    public native @Cast("uint32_t") int fields();

    public native mln_rendered_feature_query_options fields(int setter);

    /** Optional style layer IDs. When absent, all rendered layers are queried. */
    public native @Const mln_string_view layer_ids();

    public native mln_rendered_feature_query_options layer_ids(mln_string_view setter);

    public native @Cast("size_t") long layer_id_count();

    public native mln_rendered_feature_query_options layer_id_count(long setter);

    /** Optional MapLibre style-spec filter JSON. Null means no filter. */
    public native @Const mln_json_value filter();

    public native mln_rendered_feature_query_options filter(mln_json_value setter);
  }

  /** Optional fields for mln_source_feature_query_options. */
  /** enum mln_source_feature_query_option_field */
  public static final int MLN_SOURCE_FEATURE_QUERY_OPTION_SOURCE_LAYER_IDS = 1 << 0;

  /** Options for source feature queries. */
  public static class mln_source_feature_query_options extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_source_feature_query_options() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_source_feature_query_options(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_source_feature_query_options(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_source_feature_query_options position(long position) {
      return (mln_source_feature_query_options) super.position(position);
    }

    @Override
    public mln_source_feature_query_options getPointer(long i) {
      return new mln_source_feature_query_options((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_source_feature_query_options size(int setter);

    public native @Cast("uint32_t") int fields();

    public native mln_source_feature_query_options fields(int setter);

    /** Optional source-layer IDs. Required by vector sources; ignored by GeoJSON. */
    public native @Const mln_string_view source_layer_ids();

    public native mln_source_feature_query_options source_layer_ids(mln_string_view setter);

    public native @Cast("size_t") long source_layer_id_count();

    public native mln_source_feature_query_options source_layer_id_count(long setter);

    /** Optional MapLibre style-spec filter JSON. Null means no filter. */
    public native @Const mln_json_value filter();

    public native mln_source_feature_query_options filter(mln_json_value setter);
  }

  /** Optional fields returned by mln_queried_feature. */
  /** enum mln_queried_feature_field */
  public static final int MLN_QUERIED_FEATURE_SOURCE_ID = 1 << 0,
      MLN_QUERIED_FEATURE_SOURCE_LAYER_ID = 1 << 1,
      MLN_QUERIED_FEATURE_STATE = 1 << 2;

  /** One copied query result feature. */
  public static class mln_queried_feature extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_queried_feature() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_queried_feature(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_queried_feature(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_queried_feature position(long position) {
      return (mln_queried_feature) super.position(position);
    }

    @Override
    public mln_queried_feature getPointer(long i) {
      return new mln_queried_feature((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_queried_feature size(int setter);

    public native @Cast("uint32_t") int fields();

    public native mln_queried_feature fields(int setter);

    /** GeoJSON feature descriptor. Nested pointers are result-owned. */
    public native @ByRef mln_feature feature();

    public native mln_queried_feature feature(mln_feature setter);

    /** Native render source ID when available. */
    public native @ByRef mln_string_view source_id();

    public native mln_queried_feature source_id(mln_string_view setter);

    /** Native source layer ID when available. */
    public native @ByRef mln_string_view source_layer_id();

    public native mln_queried_feature source_layer_id(mln_string_view setter);

    /** Rendered feature state when available. */
    public native @Const mln_json_value state();

    public native mln_queried_feature state(mln_json_value setter);
  }

  /** Feature extension query result variants. */
  /** enum mln_feature_extension_result_type */
  public static final int MLN_FEATURE_EXTENSION_RESULT_TYPE_VALUE = 1,
      MLN_FEATURE_EXTENSION_RESULT_TYPE_FEATURE_COLLECTION = 2;

  /** Tagged feature extension query result view. */
  public static class mln_feature_extension_result_info extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_feature_extension_result_info() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_feature_extension_result_info(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_feature_extension_result_info(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_feature_extension_result_info position(long position) {
      return (mln_feature_extension_result_info) super.position(position);
    }

    @Override
    public mln_feature_extension_result_info getPointer(long i) {
      return new mln_feature_extension_result_info((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_feature_extension_result_info size(int setter);

    /** One of mln_feature_extension_result_type. */
    public native @Cast("uint32_t") int type();

    public native mln_feature_extension_result_info type(int setter);

    /** JSON-like value view selected by MLN_FEATURE_EXTENSION_RESULT_TYPE_VALUE. */
    @Name("data.value")
    public native @Const mln_json_value data_value();

    public native mln_feature_extension_result_info data_value(mln_json_value setter);

    /** Feature collection view selected by MLN_FEATURE_EXTENSION_RESULT_TYPE_FEATURE_COLLECTION. */
    @Name("data.feature_collection")
    public native @ByRef mln_feature_collection data_feature_collection();

    public native mln_feature_extension_result_info data_feature_collection(
        mln_feature_collection setter);
  }

  /** Returns default rendered feature query options. */
  @NoException
  public static native @ByVal mln_rendered_feature_query_options
      mln_rendered_feature_query_options_default();

  /** Returns default source feature query options. */
  @NoException
  public static native @ByVal mln_source_feature_query_options
      mln_source_feature_query_options_default();

  /** Returns a rendered point query geometry descriptor. */
  @NoException
  public static native @ByVal mln_rendered_query_geometry mln_rendered_query_geometry_point(
      @ByVal mln_screen_point point);

  /** Returns a rendered box query geometry descriptor. */
  @NoException
  public static native @ByVal mln_rendered_query_geometry mln_rendered_query_geometry_box(
      @ByVal mln_screen_box box);

  /** Returns a rendered line-string query geometry descriptor. */
  @NoException
  public static native @ByVal mln_rendered_query_geometry mln_rendered_query_geometry_line_string(
      @Const mln_screen_point points, @Cast("size_t") long point_count);

  /**
   * Queries rendered features from the latest render session state.
   *
   * <p>The session renderer must already exist. geometry and options are borrowed for the duration
   * of the call. Passing null for options uses default options. On success, *out_result receives an
   * owned result handle. Destroy it with mln_feature_query_result_destroy().
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live, geometry is null or invalid, options are invalid, out_result is null, or *out_result
   * is not null. - MLN_STATUS_INVALID_STATE when the session is detached or no renderer has been
   * created for the session yet. - MLN_STATUS_WRONG_THREAD when called from a thread other than the
   * session owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to
   * status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_render_session_query_rendered_features(
      mln_render_session session,
      @Const mln_rendered_query_geometry geometry,
      @Const mln_rendered_feature_query_options options,
      @Cast("mln_feature_query_result**") PointerPointer out_result);

  @NoException
  public static native @Cast("mln_status") int mln_render_session_query_rendered_features(
      mln_render_session session,
      @Const mln_rendered_query_geometry geometry,
      @Const mln_rendered_feature_query_options options,
      @ByPtrPtr mln_feature_query_result out_result);

  /**
   * Queries source features from the latest render session state.
   *
   * <p>The session renderer must already exist. source_id and options are borrowed for the duration
   * of the call. Passing null for options uses default options. On success, *out_result receives an
   * owned result handle. Destroy it with mln_feature_query_result_destroy().
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live, source_id is invalid or empty, options are invalid, out_result is null, or
   * *out_result is not null. - MLN_STATUS_INVALID_STATE when the session is detached or no renderer
   * has been created for the session yet. - MLN_STATUS_WRONG_THREAD when called from a thread other
   * than the session owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_render_session_query_source_features(
      mln_render_session session,
      @ByVal mln_string_view source_id,
      @Const mln_source_feature_query_options options,
      @Cast("mln_feature_query_result**") PointerPointer out_result);

  @NoException
  public static native @Cast("mln_status") int mln_render_session_query_source_features(
      mln_render_session session,
      @ByVal mln_string_view source_id,
      @Const mln_source_feature_query_options options,
      @ByPtrPtr mln_feature_query_result out_result);

  /**
   * Queries a feature extension from the latest render session state.
   *
   * <p>The session renderer must already exist. source_id, feature, extension, extension_field, and
   * arguments are borrowed for the duration of the call. arguments may be null. When non-null,
   * arguments must be a JSON object descriptor. On success, *out_result receives an owned result
   * handle. Destroy it with mln_feature_extension_result_destroy().
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when session is null or
   * not live, source_id, feature, extension, extension_field, or arguments are invalid, out_result
   * is null, or *out_result is not null. - MLN_STATUS_INVALID_STATE when the session is detached or
   * no renderer has been created for the session yet. - MLN_STATUS_WRONG_THREAD when called from a
   * thread other than the session owner thread. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_render_session_query_feature_extensions(
      mln_render_session session,
      @ByVal mln_string_view source_id,
      @Const mln_feature feature,
      @ByVal mln_string_view extension,
      @ByVal mln_string_view extension_field,
      @Const mln_json_value arguments,
      @Cast("mln_feature_extension_result**") PointerPointer out_result);

  @NoException
  public static native @Cast("mln_status") int mln_render_session_query_feature_extensions(
      mln_render_session session,
      @ByVal mln_string_view source_id,
      @Const mln_feature feature,
      @ByVal mln_string_view extension,
      @ByVal mln_string_view extension_field,
      @Const mln_json_value arguments,
      @ByPtrPtr mln_feature_extension_result out_result);

  /**
   * Gets the number of features in a query result handle.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when result is null or
   * not live, or out_count is null. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_feature_query_result_count(
      @Const mln_feature_query_result result, @Cast("size_t*") SizeTPointer out_count);

  /**
   * Borrows one feature from a query result handle.
   *
   * <p>On success, out_feature receives views into result-owned storage. Those views remain valid
   * until result is destroyed.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when result is null or
   * not live, index is out of range, out_feature is null, or out_feature->size is too small. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_feature_query_result_get(
      @Const mln_feature_query_result result,
      @Cast("size_t") long index,
      mln_queried_feature out_feature);

  /** Destroys a feature query result handle. Null is accepted as a no-op. */
  @NoException
  public static native void mln_feature_query_result_destroy(mln_feature_query_result result);

  /**
   * Borrows a feature extension query result view.
   *
   * <p>On success, out_info receives views into result-owned storage. Those views remain valid
   * until result is destroyed.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when result is null or
   * not live, out_info is null, or out_info->size is too small. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_feature_extension_result_get(
      @Const mln_feature_extension_result result, mln_feature_extension_result_info out_info);

  /** Destroys a feature extension result handle. Null is accepted as a no-op. */
  @NoException
  public static native void mln_feature_extension_result_destroy(
      mln_feature_extension_result result);

  // #ifdef __cplusplus
  // #endif

  // #endif  // MAPLIBRE_NATIVE_C_QUERY_H

  // Parsed from maplibre_native_c/style.h

  /**
   * \file maplibre_native_c/style.h Public C API declarations for style sources, layers, and
   * images.
   */

  // #ifndef MAPLIBRE_NATIVE_C_STYLE_H
  // #define MAPLIBRE_NATIVE_C_STYLE_H

  // #ifndef __cplusplus
  // #include <stdbool.h>
  // #endif

  // #include <stddef.h>
  // #include <stdint.h>

  // #include "base.h"
  // #include "map.h"

  // #ifdef __cplusplus
  // #endif

  @Opaque
  public static class mln_style_id_list extends Pointer {
    /** Empty constructor. Calls {@code super((Pointer)null)}. */
    public mln_style_id_list() {
      super((Pointer) null);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_style_id_list(Pointer p) {
      super(p);
    }
  }

  /** Style source type values returned by mln_map_get_style_source_type(). */
  /** enum mln_style_source_type */
  public static final int MLN_STYLE_SOURCE_TYPE_UNKNOWN = 0,
      MLN_STYLE_SOURCE_TYPE_VECTOR = 1,
      MLN_STYLE_SOURCE_TYPE_RASTER = 2,
      MLN_STYLE_SOURCE_TYPE_RASTER_DEM = 3,
      MLN_STYLE_SOURCE_TYPE_GEOJSON = 4,
      MLN_STYLE_SOURCE_TYPE_IMAGE = 5,
      MLN_STYLE_SOURCE_TYPE_VIDEO = 6,
      MLN_STYLE_SOURCE_TYPE_ANNOTATIONS = 7,
      MLN_STYLE_SOURCE_TYPE_CUSTOM_VECTOR = 8;

  /** Field mask values for mln_style_tile_source_options. */
  /** enum mln_style_tile_source_option_field */
  public static final int MLN_STYLE_TILE_SOURCE_OPTION_MIN_ZOOM = 1 << 0,
      MLN_STYLE_TILE_SOURCE_OPTION_MAX_ZOOM = 1 << 1,
      MLN_STYLE_TILE_SOURCE_OPTION_ATTRIBUTION = 1 << 2,
      MLN_STYLE_TILE_SOURCE_OPTION_SCHEME = 1 << 3,
      MLN_STYLE_TILE_SOURCE_OPTION_BOUNDS = 1 << 4,
      MLN_STYLE_TILE_SOURCE_OPTION_TILE_SIZE = 1 << 5,
      MLN_STYLE_TILE_SOURCE_OPTION_VECTOR_ENCODING = 1 << 6,
      MLN_STYLE_TILE_SOURCE_OPTION_RASTER_ENCODING = 1 << 7;

  /** Tile URL coordinate scheme values used by mln_style_tile_source_options. */
  /** enum mln_style_tile_scheme */
  public static final int MLN_STYLE_TILE_SCHEME_XYZ = 0, MLN_STYLE_TILE_SCHEME_TMS = 1;

  /** Vector tile encoding values used by mln_style_tile_source_options. */
  /** enum mln_style_vector_tile_encoding */
  public static final int MLN_STYLE_VECTOR_TILE_ENCODING_MVT = 0,
      MLN_STYLE_VECTOR_TILE_ENCODING_MLT = 1;

  /** DEM raster encoding values used by mln_style_tile_source_options. */
  /** enum mln_style_raster_dem_encoding */
  public static final int MLN_STYLE_RASTER_DEM_ENCODING_MAPBOX = 0,
      MLN_STYLE_RASTER_DEM_ENCODING_TERRARIUM = 1;

  /** Field mask values for mln_custom_geometry_source_options. */
  /** enum mln_custom_geometry_source_option_field */
  public static final int MLN_CUSTOM_GEOMETRY_SOURCE_OPTION_MIN_ZOOM = 1 << 0,
      MLN_CUSTOM_GEOMETRY_SOURCE_OPTION_MAX_ZOOM = 1 << 1,
      MLN_CUSTOM_GEOMETRY_SOURCE_OPTION_TOLERANCE = 1 << 2,
      MLN_CUSTOM_GEOMETRY_SOURCE_OPTION_TILE_SIZE = 1 << 3,
      MLN_CUSTOM_GEOMETRY_SOURCE_OPTION_BUFFER = 1 << 4,
      MLN_CUSTOM_GEOMETRY_SOURCE_OPTION_CLIP = 1 << 5,
      MLN_CUSTOM_GEOMETRY_SOURCE_OPTION_WRAP = 1 << 6;

  /** Field mask values for mln_style_image_options. */
  /** enum mln_style_image_option_field */
  public static final int MLN_STYLE_IMAGE_OPTION_PIXEL_RATIO = 1 << 0,
      MLN_STYLE_IMAGE_OPTION_SDF = 1 << 1;

  /** Location indicator image-name properties. */
  /** enum mln_location_indicator_image_kind */
  public static final int MLN_LOCATION_INDICATOR_IMAGE_KIND_TOP = 0,
      MLN_LOCATION_INDICATOR_IMAGE_KIND_BEARING = 1,
      MLN_LOCATION_INDICATOR_IMAGE_KIND_SHADOW = 2;

  /** Fixed source metadata returned by mln_map_get_style_source_info(). */
  public static class mln_style_source_info extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_style_source_info() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_style_source_info(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_style_source_info(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_style_source_info position(long position) {
      return (mln_style_source_info) super.position(position);
    }

    @Override
    public mln_style_source_info getPointer(long i) {
      return new mln_style_source_info((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_style_source_info size(int setter);

    /** One of mln_style_source_type. */
    public native @Cast("uint32_t") int type();

    public native mln_style_source_info type(int setter);

    /** Source ID byte length, excluding any null terminator. */
    public native @Cast("size_t") long id_size();

    public native mln_style_source_info id_size(long setter);

    public native @Cast("bool") boolean is_volatile();

    public native mln_style_source_info is_volatile(boolean setter);

    public native @Cast("bool") boolean has_attribution();

    public native mln_style_source_info has_attribution(boolean setter);

    /** Attribution byte length, excluding any null terminator. */
    public native @Cast("size_t") long attribution_size();

    public native mln_style_source_info attribution_size(long setter);
  }

  /** Options for vector and raster tile sources. */
  public static class mln_style_tile_source_options extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_style_tile_source_options() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_style_tile_source_options(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_style_tile_source_options(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_style_tile_source_options position(long position) {
      return (mln_style_tile_source_options) super.position(position);
    }

    @Override
    public mln_style_tile_source_options getPointer(long i) {
      return new mln_style_tile_source_options((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_style_tile_source_options size(int setter);

    public native @Cast("uint32_t") int fields();

    public native mln_style_tile_source_options fields(int setter);

    public native double min_zoom();

    public native mln_style_tile_source_options min_zoom(double setter);

    public native double max_zoom();

    public native mln_style_tile_source_options max_zoom(double setter);

    public native @ByRef mln_string_view attribution();

    public native mln_style_tile_source_options attribution(mln_string_view setter);

    /** One of mln_style_tile_scheme. Defaults to MLN_STYLE_TILE_SCHEME_XYZ. */
    public native @Cast("uint32_t") int scheme();

    public native mln_style_tile_source_options scheme(int setter);

    public native @ByRef mln_lat_lng_bounds bounds();

    public native mln_style_tile_source_options bounds(mln_lat_lng_bounds setter);

    /** Raster tile size in pixels. Defaults to 512. */
    public native @Cast("uint32_t") int tile_size();

    public native mln_style_tile_source_options tile_size(int setter);

    /** One of mln_style_vector_tile_encoding. Defaults to MVT. */
    public native @Cast("uint32_t") int vector_encoding();

    public native mln_style_tile_source_options vector_encoding(int setter);

    /** One of mln_style_raster_dem_encoding. Defaults to Mapbox. */
    public native @Cast("uint32_t") int raster_encoding();

    public native mln_style_tile_source_options raster_encoding(int setter);
  }

  /** Canonical tile identity used by custom geometry source callbacks. */
  public static class mln_canonical_tile_id extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_canonical_tile_id() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_canonical_tile_id(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_canonical_tile_id(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_canonical_tile_id position(long position) {
      return (mln_canonical_tile_id) super.position(position);
    }

    @Override
    public mln_canonical_tile_id getPointer(long i) {
      return new mln_canonical_tile_id((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int z();

    public native mln_canonical_tile_id z(int setter);

    public native @Cast("uint32_t") int x();

    public native mln_canonical_tile_id x(int setter);

    public native @Cast("uint32_t") int y();

    public native mln_canonical_tile_id y(int setter);
  }

  /** Callback invoked for custom geometry source tile requests and cancels. */
  public static class mln_custom_geometry_source_tile_callback extends FunctionPointer {
    static {
      Loader.load();
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_custom_geometry_source_tile_callback(Pointer p) {
      super(p);
    }

    protected mln_custom_geometry_source_tile_callback() {
      allocate();
    }

    private native void allocate();

    public native void call(Pointer user_data, @ByVal mln_canonical_tile_id tile_id);
  }

  /** Options for custom geometry sources. */
  public static class mln_custom_geometry_source_options extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_custom_geometry_source_options() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_custom_geometry_source_options(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_custom_geometry_source_options(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_custom_geometry_source_options position(long position) {
      return (mln_custom_geometry_source_options) super.position(position);
    }

    @Override
    public mln_custom_geometry_source_options getPointer(long i) {
      return new mln_custom_geometry_source_options((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_custom_geometry_source_options size(int setter);

    public native @Cast("uint32_t") int fields();

    public native mln_custom_geometry_source_options fields(int setter);

    /** Required tile fetch callback. */
    public native mln_custom_geometry_source_tile_callback fetch_tile();

    public native mln_custom_geometry_source_options fetch_tile(
        mln_custom_geometry_source_tile_callback setter);

    /** Optional best-effort tile cancel callback. */
    public native mln_custom_geometry_source_tile_callback cancel_tile();

    public native mln_custom_geometry_source_options cancel_tile(
        mln_custom_geometry_source_tile_callback setter);

    /** Caller-owned callback context retained by pointer. */
    public native Pointer user_data();

    public native mln_custom_geometry_source_options user_data(Pointer setter);

    public native double min_zoom();

    public native mln_custom_geometry_source_options min_zoom(double setter);

    public native double max_zoom();

    public native mln_custom_geometry_source_options max_zoom(double setter);

    public native double tolerance();

    public native mln_custom_geometry_source_options tolerance(double setter);

    public native @Cast("uint32_t") int tile_size();

    public native mln_custom_geometry_source_options tile_size(int setter);

    public native @Cast("uint32_t") int buffer();

    public native mln_custom_geometry_source_options buffer(int setter);

    public native @Cast("bool") boolean clip();

    public native mln_custom_geometry_source_options clip(boolean setter);

    public native @Cast("bool") boolean wrap();

    public native mln_custom_geometry_source_options wrap(boolean setter);
  }

  /** Caller-owned premultiplied RGBA8 image pixels. */
  public static class mln_premultiplied_rgba8_image extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_premultiplied_rgba8_image() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_premultiplied_rgba8_image(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_premultiplied_rgba8_image(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_premultiplied_rgba8_image position(long position) {
      return (mln_premultiplied_rgba8_image) super.position(position);
    }

    @Override
    public mln_premultiplied_rgba8_image getPointer(long i) {
      return new mln_premultiplied_rgba8_image((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_premultiplied_rgba8_image size(int setter);

    public native @Cast("uint32_t") int width();

    public native mln_premultiplied_rgba8_image width(int setter);

    public native @Cast("uint32_t") int height();

    public native mln_premultiplied_rgba8_image height(int setter);

    /** Bytes per image row. Must be at least width * 4. */
    public native @Cast("uint32_t") int stride();

    public native mln_premultiplied_rgba8_image stride(int setter);

    /** Premultiplied RGBA8 pixels. Must not be null for a non-empty image. */
    public native @Cast("const uint8_t*") BytePointer pixels();

    public native mln_premultiplied_rgba8_image pixels(BytePointer setter);

    /** Available bytes at pixels. */
    public native @Cast("size_t") long byte_length();

    public native mln_premultiplied_rgba8_image byte_length(long setter);
  }

  /** Options for runtime style images. */
  public static class mln_style_image_options extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_style_image_options() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_style_image_options(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_style_image_options(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_style_image_options position(long position) {
      return (mln_style_image_options) super.position(position);
    }

    @Override
    public mln_style_image_options getPointer(long i) {
      return new mln_style_image_options((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_style_image_options size(int setter);

    public native @Cast("uint32_t") int fields();

    public native mln_style_image_options fields(int setter);

    /** Sprite pixel ratio. Defaults to 1. */
    public native float pixel_ratio();

    public native mln_style_image_options pixel_ratio(float setter);

    /** Whether the image is a signed distance field icon. Defaults to false. */
    public native @Cast("bool") boolean sdf();

    public native mln_style_image_options sdf(boolean setter);
  }

  /** Fixed metadata for one runtime style image. */
  public static class mln_style_image_info extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_style_image_info() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_style_image_info(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_style_image_info(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_style_image_info position(long position) {
      return (mln_style_image_info) super.position(position);
    }

    @Override
    public mln_style_image_info getPointer(long i) {
      return new mln_style_image_info((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_style_image_info size(int setter);

    public native @Cast("uint32_t") int width();

    public native mln_style_image_info width(int setter);

    public native @Cast("uint32_t") int height();

    public native mln_style_image_info height(int setter);

    /** Native copied images are exposed as tightly packed premultiplied RGBA8. */
    public native @Cast("uint32_t") int stride();

    public native mln_style_image_info stride(int setter);

    public native @Cast("size_t") long byte_length();

    public native mln_style_image_info byte_length(long setter);

    public native float pixel_ratio();

    public native mln_style_image_info pixel_ratio(float setter);

    public native @Cast("bool") boolean sdf();

    public native mln_style_image_info sdf(boolean setter);
  }

  /** Returns default tile source options. */
  @NoException
  public static native @ByVal mln_style_tile_source_options mln_style_tile_source_options_default();

  /** Returns default custom geometry source options. */
  @NoException
  public static native @ByVal mln_custom_geometry_source_options
      mln_custom_geometry_source_options_default();

  /** Returns a default premultiplied RGBA8 image descriptor. */
  @NoException
  public static native @ByVal mln_premultiplied_rgba8_image mln_premultiplied_rgba8_image_default();

  /** Returns default runtime style image options. */
  @NoException
  public static native @ByVal mln_style_image_options mln_style_image_options_default();

  /** Returns default runtime style image metadata. */
  @NoException
  public static native @ByVal mln_style_image_info mln_style_image_info_default();

  /**
   * Gets the number of IDs in a style ID list handle.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when list is null or not
   * live, or out_count is null. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted
   * to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_style_id_list_count(
      @Const mln_style_id_list list, @Cast("size_t*") SizeTPointer out_count);

  /**
   * Borrows one ID from a style ID list handle.
   *
   * <p>On success, out_id receives a view into list-owned storage. The view remains valid until the
   * list is destroyed.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when list is null or not
   * live, index is out of range, or out_id is null. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_style_id_list_get(
      @Const mln_style_id_list list, @Cast("size_t") long index, mln_string_view out_id);

  /** Destroys a style ID list handle. Null is accepted as a no-op. */
  @NoException
  public static native void mln_style_id_list_destroy(mln_style_id_list list);

  /**
   * Adds one style source from a style-spec source JSON object.
   *
   * <p>source_id and source_json are borrowed for the call. source_json is the object that appears
   * under sources[source_id] in a style document. The function parses and copies the accepted
   * source into MapLibre Native before return.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, source_json is null or invalid, the source ID already
   * exists, or the source JSON cannot be converted. - MLN_STATUS_WRONG_THREAD when called from a
   * thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_add_style_source_json(
      mln_map map, @ByVal mln_string_view source_id, @Const mln_json_value source_json);

  /**
   * Removes one style source by ID.
   *
   * <p>source_id is borrowed for the call. On success, out_removed reports whether a source existed
   * and was removed.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, or out_removed is null. - MLN_STATUS_INVALID_STATE when
   * the source exists but a layer still uses it. - MLN_STATUS_WRONG_THREAD when called from a
   * thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_remove_style_source(
      mln_map map, @ByVal mln_string_view source_id, @Cast("bool*") BoolPointer out_removed);

  @NoException
  public static native @Cast("mln_status") int mln_map_remove_style_source(
      mln_map map, @ByVal mln_string_view source_id, @Cast("bool*") boolean[] out_removed);

  /**
   * Reports whether a style source ID exists.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, or out_exists is null. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_style_source_exists(
      mln_map map, @ByVal mln_string_view source_id, @Cast("bool*") BoolPointer out_exists);

  @NoException
  public static native @Cast("mln_status") int mln_map_style_source_exists(
      mln_map map, @ByVal mln_string_view source_id, @Cast("bool*") boolean[] out_exists);

  /**
   * Gets one style source type.
   *
   * <p>On success, out_found reports whether source_id exists. When found, out_source_type receives
   * one of mln_style_source_type.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, out_source_type is null, or out_found is null. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_get_style_source_type(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Cast("uint32_t*") IntPointer out_source_type,
      @Cast("bool*") BoolPointer out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_get_style_source_type(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Cast("uint32_t*") IntBuffer out_source_type,
      @Cast("bool*") boolean[] out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_get_style_source_type(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Cast("uint32_t*") int[] out_source_type,
      @Cast("bool*") BoolPointer out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_get_style_source_type(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Cast("uint32_t*") IntPointer out_source_type,
      @Cast("bool*") boolean[] out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_get_style_source_type(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Cast("uint32_t*") IntBuffer out_source_type,
      @Cast("bool*") BoolPointer out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_get_style_source_type(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Cast("uint32_t*") int[] out_source_type,
      @Cast("bool*") boolean[] out_found);

  /**
   * Copies fixed metadata for one style source.
   *
   * <p>The returned struct contains string lengths, not string contents. Use
   * mln_map_copy_style_source_attribution() to copy attribution bytes when has_attribution is true.
   * The source ID is the lookup key and is also available through style source ID lists.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, out_info is null, out_info->size is too small, or
   * out_found is null. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner
   * thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_get_style_source_info(
      mln_map map,
      @ByVal mln_string_view source_id,
      mln_style_source_info out_info,
      @Cast("bool*") BoolPointer out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_get_style_source_info(
      mln_map map,
      @ByVal mln_string_view source_id,
      mln_style_source_info out_info,
      @Cast("bool*") boolean[] out_found);

  /**
   * Copies one style source attribution string into caller-owned memory.
   *
   * <p>source_id is borrowed for the call. out_attribution may be null only when
   * attribution_capacity is 0. On success, out_attribution_size receives the byte length of the
   * attribution, excluding any null terminator. When out_found is false or the source has no
   * attribution, out_attribution_size receives 0.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, out_attribution is null with non-zero capacity,
   * attribution_capacity is too small for a present attribution, out_attribution_size is null, or
   * out_found is null. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner
   * thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_copy_style_source_attribution(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Cast("char*") BytePointer out_attribution,
      @Cast("size_t") long attribution_capacity,
      @Cast("size_t*") SizeTPointer out_attribution_size,
      @Cast("bool*") BoolPointer out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_copy_style_source_attribution(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Cast("char*") ByteBuffer out_attribution,
      @Cast("size_t") long attribution_capacity,
      @Cast("size_t*") SizeTPointer out_attribution_size,
      @Cast("bool*") boolean[] out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_copy_style_source_attribution(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Cast("char*") byte[] out_attribution,
      @Cast("size_t") long attribution_capacity,
      @Cast("size_t*") SizeTPointer out_attribution_size,
      @Cast("bool*") BoolPointer out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_copy_style_source_attribution(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Cast("char*") BytePointer out_attribution,
      @Cast("size_t") long attribution_capacity,
      @Cast("size_t*") SizeTPointer out_attribution_size,
      @Cast("bool*") boolean[] out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_copy_style_source_attribution(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Cast("char*") ByteBuffer out_attribution,
      @Cast("size_t") long attribution_capacity,
      @Cast("size_t*") SizeTPointer out_attribution_size,
      @Cast("bool*") BoolPointer out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_copy_style_source_attribution(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Cast("char*") byte[] out_attribution,
      @Cast("size_t") long attribution_capacity,
      @Cast("size_t*") SizeTPointer out_attribution_size,
      @Cast("bool*") boolean[] out_found);

  /**
   * Copies style source IDs in style order.
   *
   * <p>On success, *out_source_ids receives an owned list handle. Destroy it with
   * mln_style_id_list_destroy().
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, out_source_ids is null, or *out_source_ids is not null. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_list_style_source_ids(
      mln_map map, @Cast("mln_style_id_list**") PointerPointer out_source_ids);

  @NoException
  public static native @Cast("mln_status") int mln_map_list_style_source_ids(
      mln_map map, @ByPtrPtr mln_style_id_list out_source_ids);

  /**
   * Adds a GeoJSON source with URL data.
   *
   * <p>source_id and url are borrowed for the call. The source loads GeoJSON from url through
   * MapLibre Native's resource system.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id or url is invalid or empty, or the source ID already exists. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_add_geojson_source_url(
      mln_map map, @ByVal mln_string_view source_id, @ByVal mln_string_view url);

  /**
   * Adds a GeoJSON source with inline data.
   *
   * <p>source_id and data are borrowed for the call. The accepted GeoJSON descriptor is copied into
   * MapLibre Native before return.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, data is null or invalid, or the source ID already exists.
   * - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_add_geojson_source_data(
      mln_map map, @ByVal mln_string_view source_id, @Const mln_geojson data);

  /**
   * Updates one GeoJSON source to load data from a URL.
   *
   * <p>source_id and url are borrowed for the call.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id or url is invalid or empty, the source does not exist, or the source is not a
   * GeoJSON source. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner
   * thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_geojson_source_url(
      mln_map map, @ByVal mln_string_view source_id, @ByVal mln_string_view url);

  /**
   * Updates one GeoJSON source with inline data.
   *
   * <p>source_id and data are borrowed for the call. The accepted GeoJSON descriptor is copied into
   * MapLibre Native before return.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, data is null or invalid, the source does not exist, or the
   * source is not a GeoJSON source. - MLN_STATUS_WRONG_THREAD when called from a thread other than
   * the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to
   * status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_geojson_source_data(
      mln_map map, @ByVal mln_string_view source_id, @Const mln_geojson data);

  /**
   * Adds a vector source with a TileJSON URL.
   *
   * <p>source_id and url are borrowed for the call. options may be null for defaults. For URL
   * sources, min_zoom, max_zoom, and vector_encoding override values from the loaded TileJSON when
   * their field bits are set.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id or url is invalid or empty, options is invalid, or the source ID already
   * exists. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_add_vector_source_url(
      mln_map map,
      @ByVal mln_string_view source_id,
      @ByVal mln_string_view url,
      @Const mln_style_tile_source_options options);

  /**
   * Adds a vector source with inline tile URLs.
   *
   * <p>source_id and tile URL views are borrowed for the call. The function copies accepted strings
   * into MapLibre Native before return. options may be null for defaults.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, tile URLs are null, empty, or invalid, options is invalid,
   * or the source ID already exists. - MLN_STATUS_WRONG_THREAD when called from a thread other than
   * the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to
   * status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_add_vector_source_tiles(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Const mln_string_view tiles,
      @Cast("size_t") long tile_count,
      @Const mln_style_tile_source_options options);

  /**
   * Adds a raster source with a TileJSON URL.
   *
   * <p>source_id and url are borrowed for the call. options may be null for defaults. For URL
   * sources, only tile_size is used when its field bit is set.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id or url is invalid or empty, options is invalid, or the source ID already
   * exists. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_add_raster_source_url(
      mln_map map,
      @ByVal mln_string_view source_id,
      @ByVal mln_string_view url,
      @Const mln_style_tile_source_options options);

  /**
   * Adds a raster source with inline tile URLs.
   *
   * <p>source_id and tile URL views are borrowed for the call. The function copies accepted strings
   * into MapLibre Native before return. options may be null for defaults.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, tile URLs are null, empty, or invalid, options is invalid,
   * or the source ID already exists. - MLN_STATUS_WRONG_THREAD when called from a thread other than
   * the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to
   * status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_add_raster_source_tiles(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Const mln_string_view tiles,
      @Cast("size_t") long tile_count,
      @Const mln_style_tile_source_options options);

  /**
   * Adds a raster DEM source with a TileJSON URL.
   *
   * <p>source_id and url are borrowed for the call. options may be null for defaults. For URL
   * sources, tile_size and raster_encoding are used when their field bits are set.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id or url is invalid or empty, options is invalid, or the source ID already
   * exists. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_add_raster_dem_source_url(
      mln_map map,
      @ByVal mln_string_view source_id,
      @ByVal mln_string_view url,
      @Const mln_style_tile_source_options options);

  /**
   * Adds a raster DEM source with inline tile URLs.
   *
   * <p>source_id and tile URL views are borrowed for the call. The function copies accepted strings
   * into MapLibre Native before return. options may be null for defaults.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, tile URLs are null, empty, or invalid, options is invalid,
   * or the source ID already exists. - MLN_STATUS_WRONG_THREAD when called from a thread other than
   * the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to
   * status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_add_raster_dem_source_tiles(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Const mln_string_view tiles,
      @Cast("size_t") long tile_count,
      @Const mln_style_tile_source_options options);

  /**
   * Adds a custom geometry source.
   *
   * <p>source_id is borrowed for the call. options is borrowed for the call, but the callback
   * function pointers and user_data pointer are retained by value. The callback functions and
   * user_data must remain valid until the source is removed, the style is replaced, or the map is
   * destroyed, and until any in-flight callback invocation has returned. For URL loads, style
   * replacement occurs when the new style loads, not when the load request is accepted. For inline
   * JSON loads, style replacement completes before mln_map_set_style_json() returns successfully.
   *
   * <p>fetch_tile and cancel_tile may run on arbitrary native worker threads, may be concurrent
   * with owner-thread map calls, and must not call thread-affine map APIs directly. Queue work back
   * to the map owner thread before calling mln_map_set_custom_geometry_source_tile_data() or
   * invalidation functions. Callbacks must not throw, panic, longjmp, or otherwise unwind through
   * the C ABI. cancel_tile is best-effort and may be repeated or race with fetch_tile.
   *
   * <p>Custom geometry sources belong to the current style. Replacing the style drops sources that
   * were added to the previous style.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, options is null or invalid, fetch_tile is null, or the
   * source ID already exists. - MLN_STATUS_WRONG_THREAD when called from a thread other than the
   * map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_add_custom_geometry_source(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Const mln_custom_geometry_source_options options);

  /**
   * Sets custom geometry source data for one canonical tile.
   *
   * <p>source_id and data are borrowed for the call. The accepted GeoJSON descriptor is copied into
   * MapLibre Native before return.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, tile_id is invalid, data is null or invalid, the source
   * does not exist, or the source is not a custom geometry source. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_custom_geometry_source_tile_data(
      mln_map map,
      @ByVal mln_string_view source_id,
      @ByVal mln_canonical_tile_id tile_id,
      @Const mln_geojson data);

  /**
   * Invalidates custom geometry source data for one canonical tile.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, tile_id is invalid, the source does not exist, or the
   * source is not a custom geometry source. - MLN_STATUS_WRONG_THREAD when called from a thread
   * other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_invalidate_custom_geometry_source_tile(
      mln_map map, @ByVal mln_string_view source_id, @ByVal mln_canonical_tile_id tile_id);

  /**
   * Invalidates custom geometry source data inside one geographic region.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, bounds is invalid, the source does not exist, or the
   * source is not a custom geometry source. - MLN_STATUS_WRONG_THREAD when called from a thread
   * other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_invalidate_custom_geometry_source_region(
      mln_map map, @ByVal mln_string_view source_id, @ByVal mln_lat_lng_bounds bounds);

  /**
   * Sets one runtime style image.
   *
   * <p>image_id, image, and image pixels are borrowed for the call. The function copies accepted
   * pixel bytes into the current style before return. If image_id already exists, the native image
   * is replaced.
   *
   * <p>Runtime style images belong to the current style. Loading another style URL or JSON document
   * drops images that were added to the previous style.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, image_id is invalid or empty, image or options is invalid, image pixels are null, image
   * dimensions or stride are invalid, or image byte_length is too small. - MLN_STATUS_WRONG_THREAD
   * when called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_style_image(
      mln_map map,
      @ByVal mln_string_view image_id,
      @Const mln_premultiplied_rgba8_image image,
      @Const mln_style_image_options options);

  /**
   * Removes one runtime style image by ID.
   *
   * <p>image_id is borrowed for the call. On success, out_removed reports whether an image existed
   * and was removed.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, image_id is invalid or empty, or out_removed is null. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_remove_style_image(
      mln_map map, @ByVal mln_string_view image_id, @Cast("bool*") BoolPointer out_removed);

  @NoException
  public static native @Cast("mln_status") int mln_map_remove_style_image(
      mln_map map, @ByVal mln_string_view image_id, @Cast("bool*") boolean[] out_removed);

  /**
   * Reports whether a runtime style image ID exists.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, image_id is invalid or empty, or out_exists is null. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_style_image_exists(
      mln_map map, @ByVal mln_string_view image_id, @Cast("bool*") BoolPointer out_exists);

  @NoException
  public static native @Cast("mln_status") int mln_map_style_image_exists(
      mln_map map, @ByVal mln_string_view image_id, @Cast("bool*") boolean[] out_exists);

  /**
   * Copies fixed metadata for one runtime style image.
   *
   * <p>On success, out_found reports whether image_id exists. When not found, out_info receives
   * default image metadata.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, image_id is invalid or empty, out_info is null, out_info->size is too small, or out_found
   * is null. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_get_style_image_info(
      mln_map map,
      @ByVal mln_string_view image_id,
      mln_style_image_info out_info,
      @Cast("bool*") BoolPointer out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_get_style_image_info(
      mln_map map,
      @ByVal mln_string_view image_id,
      mln_style_image_info out_info,
      @Cast("bool*") boolean[] out_found);

  /**
   * Copies one runtime style image as tightly packed premultiplied RGBA8 pixels.
   *
   * <p>image_id is borrowed for the call. out_pixels may be null only when pixel_capacity is 0. On
   * success, out_byte_length receives the required byte length. When out_found is false,
   * out_byte_length receives 0. If pixel_capacity is too small for a present image, out_byte_length
   * still receives the required byte length and the function returns MLN_STATUS_INVALID_ARGUMENT.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, image_id is invalid or empty, out_pixels is null with non-zero capacity, pixel_capacity
   * is too small for a present image, out_byte_length is null, or out_found is null. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_copy_style_image_premultiplied_rgba8(
      mln_map map,
      @ByVal mln_string_view image_id,
      @Cast("uint8_t*") BytePointer out_pixels,
      @Cast("size_t") long pixel_capacity,
      @Cast("size_t*") SizeTPointer out_byte_length,
      @Cast("bool*") BoolPointer out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_copy_style_image_premultiplied_rgba8(
      mln_map map,
      @ByVal mln_string_view image_id,
      @Cast("uint8_t*") ByteBuffer out_pixels,
      @Cast("size_t") long pixel_capacity,
      @Cast("size_t*") SizeTPointer out_byte_length,
      @Cast("bool*") boolean[] out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_copy_style_image_premultiplied_rgba8(
      mln_map map,
      @ByVal mln_string_view image_id,
      @Cast("uint8_t*") byte[] out_pixels,
      @Cast("size_t") long pixel_capacity,
      @Cast("size_t*") SizeTPointer out_byte_length,
      @Cast("bool*") BoolPointer out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_copy_style_image_premultiplied_rgba8(
      mln_map map,
      @ByVal mln_string_view image_id,
      @Cast("uint8_t*") BytePointer out_pixels,
      @Cast("size_t") long pixel_capacity,
      @Cast("size_t*") SizeTPointer out_byte_length,
      @Cast("bool*") boolean[] out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_copy_style_image_premultiplied_rgba8(
      mln_map map,
      @ByVal mln_string_view image_id,
      @Cast("uint8_t*") ByteBuffer out_pixels,
      @Cast("size_t") long pixel_capacity,
      @Cast("size_t*") SizeTPointer out_byte_length,
      @Cast("bool*") BoolPointer out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_copy_style_image_premultiplied_rgba8(
      mln_map map,
      @ByVal mln_string_view image_id,
      @Cast("uint8_t*") byte[] out_pixels,
      @Cast("size_t") long pixel_capacity,
      @Cast("size_t*") SizeTPointer out_byte_length,
      @Cast("bool*") boolean[] out_found);

  /**
   * Adds an image source that loads its image from a URL.
   *
   * <p>source_id, coordinates, and url are borrowed for the call. coordinates must contain exactly
   * four coordinates in top-left, top-right, bottom-right, bottom-left order. The function copies
   * accepted strings and coordinates into the current style before return. Later URL load or decode
   * failures are reported through runtime events.
   *
   * <p>Image sources belong to the current style. Loading another style URL or JSON document drops
   * sources that were added to the previous style.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id or url is invalid or empty, coordinates is null or invalid, coordinate_count is
   * not 4, or the source ID already exists. - MLN_STATUS_WRONG_THREAD when called from a thread
   * other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_add_image_source_url(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Const mln_lat_lng coordinates,
      @Cast("size_t") long coordinate_count,
      @ByVal mln_string_view url);

  /**
   * Adds an image source with inline image pixels.
   *
   * <p>source_id, coordinates, image, and image pixels are borrowed for the call. coordinates must
   * contain exactly four coordinates in top-left, top-right, bottom-right, bottom-left order. The
   * function copies accepted coordinates and pixels into the current style before return.
   *
   * <p>Image sources belong to the current style. Loading another style URL or JSON document drops
   * sources that were added to the previous style.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, coordinates is null or invalid, coordinate_count is not 4,
   * image is invalid, image pixels are null, image dimensions or stride are invalid, image
   * byte_length is too small, or the source ID already exists. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_add_image_source_image(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Const mln_lat_lng coordinates,
      @Cast("size_t") long coordinate_count,
      @Const mln_premultiplied_rgba8_image image);

  /**
   * Updates an image source to load its image from a URL.
   *
   * <p>source_id and url are borrowed for the call. Later URL load or decode failures are reported
   * through runtime events.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id or url is invalid or empty, the source does not exist, or the source is not an
   * image source. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner
   * thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_image_source_url(
      mln_map map, @ByVal mln_string_view source_id, @ByVal mln_string_view url);

  /**
   * Updates an image source with inline image pixels.
   *
   * <p>source_id, image, and image pixels are borrowed for the call. The function copies accepted
   * pixels into MapLibre Native before return.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, image is invalid, image pixels are null, image dimensions
   * or stride are invalid, image byte_length is too small, the source does not exist, or the source
   * is not an image source. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map
   * owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_image_source_image(
      mln_map map, @ByVal mln_string_view source_id, @Const mln_premultiplied_rgba8_image image);

  /**
   * Updates image source coordinates.
   *
   * <p>coordinates is borrowed for the call and must contain exactly four coordinates in top-left,
   * top-right, bottom-right, bottom-left order. The function copies accepted coordinates into
   * MapLibre Native before return.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, coordinates is null or invalid, coordinate_count is not 4,
   * the source does not exist, or the source is not an image source. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_image_source_coordinates(
      mln_map map,
      @ByVal mln_string_view source_id,
      @Const mln_lat_lng coordinates,
      @Cast("size_t") long coordinate_count);

  /**
   * Copies image source coordinates.
   *
   * <p>On success, out_found reports whether source_id exists. When found, out_coordinate_count
   * receives 4. If coordinate_capacity is less than 4, out_coordinate_count still receives 4 and
   * the function returns MLN_STATUS_INVALID_ARGUMENT.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, source_id is invalid or empty, out_coordinates is null with non-zero capacity,
   * coordinate_capacity is too small for a found source, out_coordinate_count is null, out_found is
   * null, or the source exists and is not an image source. - MLN_STATUS_WRONG_THREAD when called
   * from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_get_image_source_coordinates(
      mln_map map,
      @ByVal mln_string_view source_id,
      mln_lat_lng out_coordinates,
      @Cast("size_t") long coordinate_capacity,
      @Cast("size_t*") SizeTPointer out_coordinate_count,
      @Cast("bool*") BoolPointer out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_get_image_source_coordinates(
      mln_map map,
      @ByVal mln_string_view source_id,
      mln_lat_lng out_coordinates,
      @Cast("size_t") long coordinate_capacity,
      @Cast("size_t*") SizeTPointer out_coordinate_count,
      @Cast("bool*") boolean[] out_found);

  /**
   * Adds a hillshade layer for a raster DEM source.
   *
   * <p>layer_id, source_id, and before_layer_id are borrowed for the call. Passing an empty
   * before_layer_id appends the layer; otherwise the layer is inserted before that existing layer.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, layer_id or source_id is invalid or empty, before_layer_id is invalid or does not exist,
   * layer_id already exists, source_id does not exist, or source_id is not a raster DEM source. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_add_hillshade_layer(
      mln_map map,
      @ByVal mln_string_view layer_id,
      @ByVal mln_string_view source_id,
      @ByVal mln_string_view before_layer_id);

  /**
   * Adds a color-relief layer for a raster DEM source.
   *
   * <p>layer_id, source_id, and before_layer_id are borrowed for the call. Passing an empty
   * before_layer_id appends the layer; otherwise the layer is inserted before that existing layer.
   * Use mln_map_set_layer_property() with color-relief-color to set the color ramp expression.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, layer_id or source_id is invalid or empty, before_layer_id is invalid or does not exist,
   * layer_id already exists, source_id does not exist, or source_id is not a raster DEM source. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_add_color_relief_layer(
      mln_map map,
      @ByVal mln_string_view layer_id,
      @ByVal mln_string_view source_id,
      @ByVal mln_string_view before_layer_id);

  /**
   * Adds a source-free location indicator layer.
   *
   * <p>layer_id and before_layer_id are borrowed for the call. Passing an empty before_layer_id
   * appends the layer; otherwise the layer is inserted before that existing layer.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, layer_id is invalid or empty, before_layer_id is invalid or does not exist, or layer_id
   * already exists. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner
   * thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_add_location_indicator_layer(
      mln_map map, @ByVal mln_string_view layer_id, @ByVal mln_string_view before_layer_id);

  /**
   * Sets a location indicator layer location.
   *
   * <p>coordinate uses normal C API latitude/longitude order. The underlying style property is
   * written as [longitude, latitude, altitude].
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, layer_id is invalid or empty, coordinate or altitude is invalid, the layer does not
   * exist, or the layer is not a location indicator layer. - MLN_STATUS_WRONG_THREAD when called
   * from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_location_indicator_location(
      mln_map map, @ByVal mln_string_view layer_id, @ByVal mln_lat_lng coordinate, double altitude);

  /**
   * Sets a location indicator layer bearing in degrees.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, layer_id is invalid or empty, bearing is not finite float32, the layer does not exist, or
   * the layer is not a location indicator layer. - MLN_STATUS_WRONG_THREAD when called from a
   * thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_location_indicator_bearing(
      mln_map map, @ByVal mln_string_view layer_id, double bearing);

  /**
   * Sets a location indicator layer accuracy radius in logical pixels.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, layer_id is invalid or empty, radius is negative or not finite float32, the layer does
   * not exist, or the layer is not a location indicator layer. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_location_indicator_accuracy_radius(
      mln_map map, @ByVal mln_string_view layer_id, double radius);

  /**
   * Sets one location indicator image-name property.
   *
   * <p>image_id is borrowed for the call and copied into native style storage. The named style
   * image does not need to exist when this function is called.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, layer_id or image_id is invalid or empty, image_kind is invalid, the layer does not
   * exist, or the layer is not a location indicator layer. - MLN_STATUS_WRONG_THREAD when called
   * from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_location_indicator_image_name(
      mln_map map,
      @ByVal mln_string_view layer_id,
      @Cast("uint32_t") int image_kind,
      @ByVal mln_string_view image_id);

  /**
   * Adds one style layer from a full style-spec layer JSON object.
   *
   * <p>layer_json and before_layer_id are borrowed for the call. layer_json must contain id and
   * type members. Passing an empty before_layer_id appends the layer; otherwise the layer is
   * inserted before that existing layer.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, layer_json is null or invalid, the layer ID already exists, before_layer_id is invalid or
   * does not exist, or the layer JSON cannot be converted. - MLN_STATUS_WRONG_THREAD when called
   * from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal
   * exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_add_style_layer_json(
      mln_map map, @Const mln_json_value layer_json, @ByVal mln_string_view before_layer_id);

  /**
   * Removes one style layer by ID.
   *
   * <p>layer_id is borrowed for the call. On success, out_removed reports whether a layer existed
   * and was removed.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, layer_id is invalid or empty, or out_removed is null. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_remove_style_layer(
      mln_map map, @ByVal mln_string_view layer_id, @Cast("bool*") BoolPointer out_removed);

  @NoException
  public static native @Cast("mln_status") int mln_map_remove_style_layer(
      mln_map map, @ByVal mln_string_view layer_id, @Cast("bool*") boolean[] out_removed);

  /**
   * Reports whether a style layer ID exists.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, layer_id is invalid or empty, or out_exists is null. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_style_layer_exists(
      mln_map map, @ByVal mln_string_view layer_id, @Cast("bool*") BoolPointer out_exists);

  @NoException
  public static native @Cast("mln_status") int mln_map_style_layer_exists(
      mln_map map, @ByVal mln_string_view layer_id, @Cast("bool*") boolean[] out_exists);

  /**
   * Borrows one style layer type string.
   *
   * <p>On success, out_found reports whether layer_id exists. When found, out_layer_type receives a
   * view of a static style-spec layer type string.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, layer_id is invalid or empty, out_layer_type is null, or out_found is null. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_get_style_layer_type(
      mln_map map,
      @ByVal mln_string_view layer_id,
      mln_string_view out_layer_type,
      @Cast("bool*") BoolPointer out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_get_style_layer_type(
      mln_map map,
      @ByVal mln_string_view layer_id,
      mln_string_view out_layer_type,
      @Cast("bool*") boolean[] out_found);

  /**
   * Copies style layer IDs in style order.
   *
   * <p>On success, *out_layer_ids receives an owned list handle. Destroy it with
   * mln_style_id_list_destroy().
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, out_layer_ids is null, or *out_layer_ids is not null. - MLN_STATUS_WRONG_THREAD when
   * called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_list_style_layer_ids(
      mln_map map, @Cast("mln_style_id_list**") PointerPointer out_layer_ids);

  @NoException
  public static native @Cast("mln_status") int mln_map_list_style_layer_ids(
      mln_map map, @ByPtrPtr mln_style_id_list out_layer_ids);

  /**
   * Moves one style layer before another layer or to the top.
   *
   * <p>layer_id and before_layer_id are borrowed for the call. Passing an empty before_layer_id
   * moves the layer to the top of the style order.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, layer_id is invalid or empty, before_layer_id is invalid, layer_id does not exist, or
   * before_layer_id is non-empty and does not exist. - MLN_STATUS_WRONG_THREAD when called from a
   * thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_move_style_layer(
      mln_map map, @ByVal mln_string_view layer_id, @ByVal mln_string_view before_layer_id);

  /**
   * Copies one style layer as a full style-spec layer JSON snapshot.
   *
   * <p>On success, out_found reports whether layer_id exists. When found, *out_layer receives an
   * owned snapshot handle.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, layer_id is invalid or empty, out_layer is null, *out_layer is not null, or out_found is
   * null. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_get_style_layer_json(
      mln_map map,
      @ByVal mln_string_view layer_id,
      @Cast("mln_json_snapshot**") PointerPointer out_layer,
      @Cast("bool*") BoolPointer out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_get_style_layer_json(
      mln_map map,
      @ByVal mln_string_view layer_id,
      @ByPtrPtr mln_json_snapshot out_layer,
      @Cast("bool*") BoolPointer out_found);

  @NoException
  public static native @Cast("mln_status") int mln_map_get_style_layer_json(
      mln_map map,
      @ByVal mln_string_view layer_id,
      @ByPtrPtr mln_json_snapshot out_layer,
      @Cast("bool*") boolean[] out_found);

  /**
   * Sets the style light from a style-spec light JSON object.
   *
   * <p>light_json is borrowed for the call. The function parses and copies the accepted light into
   * MapLibre Native before return.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, light_json is null or invalid, or the light JSON cannot be converted. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_style_light_json(
      mln_map map, @Const mln_json_value light_json);

  /**
   * Sets one style light property using its MapLibre style-spec property name.
   *
   * <p>property_name and value are borrowed for the call. value is a style-spec JSON value
   * descriptor. The function parses and copies the accepted value into MapLibre Native's typed
   * light property storage before return.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, property_name is invalid or empty, value is null or invalid, the property name is
   * unknown, or the property value cannot be converted for that property. - MLN_STATUS_WRONG_THREAD
   * when called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_style_light_property(
      mln_map map, @ByVal mln_string_view property_name, @Const mln_json_value value);

  /**
   * Copies one style light property as a style-spec JSON value snapshot.
   *
   * <p>On success, *out_value receives an owned snapshot handle. Use mln_json_snapshot_get() to
   * borrow its root JSON value. Destroy the snapshot with mln_json_snapshot_destroy(). Undefined
   * native style light properties return null snapshots.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, property_name is invalid or empty, out_value is null, or *out_value is not null. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_get_style_light_property(
      mln_map map,
      @ByVal mln_string_view property_name,
      @Cast("mln_json_snapshot**") PointerPointer out_value);

  @NoException
  public static native @Cast("mln_status") int mln_map_get_style_light_property(
      mln_map map, @ByVal mln_string_view property_name, @ByPtrPtr mln_json_snapshot out_value);

  /**
   * Sets one layer property using its MapLibre style-spec property name.
   *
   * <p>layer_id, property_name, and value are borrowed for the call. value is a style-spec JSON
   * value descriptor. Expressions use style-spec expression JSON arrays. The function parses and
   * copies the accepted value into MapLibre Native's typed style property storage before return.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, layer_id or property_name is invalid or empty, value is null or invalid, the layer does
   * not exist, the property name is unknown for that layer, or the property value cannot be
   * converted for that property. - MLN_STATUS_WRONG_THREAD when called from a thread other than the
   * map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_layer_property(
      mln_map map,
      @ByVal mln_string_view layer_id,
      @ByVal mln_string_view property_name,
      @Const mln_json_value value);

  /**
   * Copies one layer property as a style-spec JSON value snapshot.
   *
   * <p>On success, *out_value receives an owned snapshot handle. Use mln_json_snapshot_get() to
   * borrow its root JSON value. Destroy the snapshot with mln_json_snapshot_destroy(). Undefined
   * native style properties return null snapshots.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, layer_id or property_name is invalid or empty, out_value is null, *out_value is not null,
   * or the layer does not exist. - MLN_STATUS_WRONG_THREAD when called from a thread other than the
   * map owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_get_layer_property(
      mln_map map,
      @ByVal mln_string_view layer_id,
      @ByVal mln_string_view property_name,
      @Cast("mln_json_snapshot**") PointerPointer out_value);

  @NoException
  public static native @Cast("mln_status") int mln_map_get_layer_property(
      mln_map map,
      @ByVal mln_string_view layer_id,
      @ByVal mln_string_view property_name,
      @ByPtrPtr mln_json_snapshot out_value);

  /**
   * Sets or clears one layer filter.
   *
   * <p>layer_id and filter are borrowed for the call. Passing null for filter clears the layer
   * filter. Non-null filters use the MapLibre style-spec filter JSON representation. The function
   * parses and copies the accepted filter into MapLibre Native's typed filter expression storage
   * before return.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, layer_id is invalid or empty, filter is invalid, the layer does not exist, or the filter
   * cannot be converted. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map
   * owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_layer_filter(
      mln_map map, @ByVal mln_string_view layer_id, @Const mln_json_value filter);

  /**
   * Copies one layer filter as a style-spec JSON value snapshot.
   *
   * <p>On success, *out_filter receives an owned snapshot handle. Use mln_json_snapshot_get() to
   * borrow its root JSON value. Destroy the snapshot with mln_json_snapshot_destroy(). Missing
   * filters return null snapshots.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not
   * live, layer_id is invalid or empty, out_filter is null, *out_filter is not null, or the layer
   * does not exist. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner
   * thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_get_layer_filter(
      mln_map map,
      @ByVal mln_string_view layer_id,
      @Cast("mln_json_snapshot**") PointerPointer out_filter);

  @NoException
  public static native @Cast("mln_status") int mln_map_get_layer_filter(
      mln_map map, @ByVal mln_string_view layer_id, @ByPtrPtr mln_json_snapshot out_filter);

  // #ifdef __cplusplus
  // #endif

  // #endif  // MAPLIBRE_NATIVE_C_STYLE_H

  // Parsed from maplibre_native_c/map.h

  /**
   * \file maplibre_native_c/map.h Public C API declarations for map lifecycle, shared types, and
   * offline regions.
   */

  // #ifndef MAPLIBRE_NATIVE_C_MAP_H
  // #define MAPLIBRE_NATIVE_C_MAP_H

  // #ifndef __cplusplus
  // #include <stdbool.h>
  // #endif

  // #include <stddef.h>
  // #include <stdint.h>

  // #include "base.h"
  // #include "runtime.h"

  // #ifdef __cplusplus
  // #endif

  /** Field mask values for mln_camera_options. */
  /** enum mln_camera_option_field */
  public static final int MLN_CAMERA_OPTION_CENTER = 1 << 0,
      MLN_CAMERA_OPTION_ZOOM = 1 << 1,
      MLN_CAMERA_OPTION_BEARING = 1 << 2,
      MLN_CAMERA_OPTION_PITCH = 1 << 3,
      MLN_CAMERA_OPTION_CENTER_ALTITUDE = 1 << 4,
      MLN_CAMERA_OPTION_PADDING = 1 << 5,
      MLN_CAMERA_OPTION_ANCHOR = 1 << 6,
      MLN_CAMERA_OPTION_ROLL = 1 << 7,
      MLN_CAMERA_OPTION_FOV = 1 << 8;

  /** Field mask values for mln_animation_options. */
  /** enum mln_animation_option_field */
  public static final int MLN_ANIMATION_OPTION_DURATION = 1 << 0,
      MLN_ANIMATION_OPTION_VELOCITY = 1 << 1,
      MLN_ANIMATION_OPTION_MIN_ZOOM = 1 << 2,
      MLN_ANIMATION_OPTION_EASING = 1 << 3;

  /** Field mask values for mln_camera_fit_options. */
  /** enum mln_camera_fit_option_field */
  public static final int MLN_CAMERA_FIT_OPTION_PADDING = 1 << 0,
      MLN_CAMERA_FIT_OPTION_BEARING = 1 << 1,
      MLN_CAMERA_FIT_OPTION_PITCH = 1 << 2;

  /** Field mask values for mln_bound_options. */
  /** enum mln_bound_option_field */
  public static final int MLN_BOUND_OPTION_BOUNDS = 1 << 0,
      MLN_BOUND_OPTION_MIN_ZOOM = 1 << 1,
      MLN_BOUND_OPTION_MAX_ZOOM = 1 << 2,
      MLN_BOUND_OPTION_MIN_PITCH = 1 << 3,
      MLN_BOUND_OPTION_MAX_PITCH = 1 << 4;

  /** Field mask values for mln_free_camera_options. */
  /** enum mln_free_camera_option_field */
  public static final int MLN_FREE_CAMERA_OPTION_POSITION = 1 << 0,
      MLN_FREE_CAMERA_OPTION_ORIENTATION = 1 << 1;

  /** Field mask values for MapLibre axonometric rendering options. */
  /** enum mln_projection_mode_field */
  public static final int MLN_PROJECTION_MODE_AXONOMETRIC = 1 << 0,
      MLN_PROJECTION_MODE_X_SKEW = 1 << 1,
      MLN_PROJECTION_MODE_Y_SKEW = 1 << 2;

  /** Debug overlay mask values for mln_map_set_debug_options(). */
  /** enum mln_map_debug_option */
  public static final int MLN_MAP_DEBUG_TILE_BORDERS = 1 << 1,
      MLN_MAP_DEBUG_PARSE_STATUS = 1 << 2,
      MLN_MAP_DEBUG_TIMESTAMPS = 1 << 3,
      MLN_MAP_DEBUG_COLLISION = 1 << 4,
      MLN_MAP_DEBUG_OVERDRAW = 1 << 5,
      MLN_MAP_DEBUG_STENCIL_CLIP = 1 << 6,
      MLN_MAP_DEBUG_DEPTH_BUFFER = 1 << 7;

  /** Map north orientation values used by mln_map_viewport_options. */
  /** enum mln_north_orientation */
  public static final int MLN_NORTH_ORIENTATION_UP = 0,
      MLN_NORTH_ORIENTATION_RIGHT = 1,
      MLN_NORTH_ORIENTATION_DOWN = 2,
      MLN_NORTH_ORIENTATION_LEFT = 3;

  /** Map constraint modes used by mln_map_viewport_options. */
  /** enum mln_constrain_mode */
  public static final int MLN_CONSTRAIN_MODE_NONE = 0,
      MLN_CONSTRAIN_MODE_HEIGHT_ONLY = 1,
      MLN_CONSTRAIN_MODE_WIDTH_AND_HEIGHT = 2,
      MLN_CONSTRAIN_MODE_SCREEN = 3;

  /** Viewport orientation modes used by mln_map_viewport_options. */
  /** enum mln_viewport_mode */
  public static final int MLN_VIEWPORT_MODE_DEFAULT = 0, MLN_VIEWPORT_MODE_FLIPPED_Y = 1;

  /** Field mask values for mln_map_viewport_options. */
  /** enum mln_map_viewport_option_field */
  public static final int MLN_MAP_VIEWPORT_OPTION_NORTH_ORIENTATION = 1 << 0,
      MLN_MAP_VIEWPORT_OPTION_CONSTRAIN_MODE = 1 << 1,
      MLN_MAP_VIEWPORT_OPTION_VIEWPORT_MODE = 1 << 2,
      MLN_MAP_VIEWPORT_OPTION_FRUSTUM_OFFSET = 1 << 3;

  /** Tile LOD algorithms used by mln_map_tile_options. */
  /** enum mln_tile_lod_mode */
  public static final int MLN_TILE_LOD_MODE_DEFAULT = 0, MLN_TILE_LOD_MODE_DISTANCE = 1;

  /** Field mask values for mln_map_tile_options. */
  /** enum mln_map_tile_option_field */
  public static final int MLN_MAP_TILE_OPTION_PREFETCH_ZOOM_DELTA = 1 << 0,
      MLN_MAP_TILE_OPTION_LOD_MIN_RADIUS = 1 << 1,
      MLN_MAP_TILE_OPTION_LOD_SCALE = 1 << 2,
      MLN_MAP_TILE_OPTION_LOD_PITCH_THRESHOLD = 1 << 3,
      MLN_MAP_TILE_OPTION_LOD_ZOOM_SHIFT = 1 << 4,
      MLN_MAP_TILE_OPTION_LOD_MODE = 1 << 5;

  /** Map rendering modes used when creating a map. */
  /** enum mln_map_mode */
  public static final int
      /** Continuously updates as data arrives and map state changes. */
      MLN_MAP_MODE_CONTINUOUS = 0,
      /** Produces one-off still images of an arbitrary viewport. */
      MLN_MAP_MODE_STATIC = 1,
      /** Produces one-off still images for a single tile. */
      MLN_MAP_MODE_TILE = 2;

  /** Options used when creating a map. */
  public static class mln_map_options extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_map_options() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_map_options(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_map_options(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_map_options position(long position) {
      return (mln_map_options) super.position(position);
    }

    @Override
    public mln_map_options getPointer(long i) {
      return new mln_map_options((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_map_options size(int setter);

    public native @Cast("uint32_t") int width();

    public native mln_map_options width(int setter);

    public native @Cast("uint32_t") int height();

    public native mln_map_options height(int setter);

    public native double scale_factor();

    public native mln_map_options scale_factor(double setter);

    /** One of mln_map_mode. Defaults to MLN_MAP_MODE_CONTINUOUS. */
    public native @Cast("uint32_t") int map_mode();

    public native mln_map_options map_mode(int setter);
  }

  /** Screen-space point in logical map pixels. */
  public static class mln_screen_point extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_screen_point() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_screen_point(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_screen_point(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_screen_point position(long position) {
      return (mln_screen_point) super.position(position);
    }

    @Override
    public mln_screen_point getPointer(long i) {
      return new mln_screen_point((Pointer) this).offsetAddress(i);
    }

    public native double x();

    public native mln_screen_point x(double setter);

    public native double y();

    public native mln_screen_point y(double setter);
  }

  /** Screen-space inset in logical map pixels. */
  public static class mln_edge_insets extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_edge_insets() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_edge_insets(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_edge_insets(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_edge_insets position(long position) {
      return (mln_edge_insets) super.position(position);
    }

    @Override
    public mln_edge_insets getPointer(long i) {
      return new mln_edge_insets((Pointer) this).offsetAddress(i);
    }

    public native double top();

    public native mln_edge_insets top(double setter);

    public native double left();

    public native mln_edge_insets left(double setter);

    public native double bottom();

    public native mln_edge_insets bottom(double setter);

    public native double right();

    public native mln_edge_insets right(double setter);
  }

  /** Camera fields used for snapshots and camera commands. */
  public static class mln_camera_options extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_camera_options() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_camera_options(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_camera_options(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_camera_options position(long position) {
      return (mln_camera_options) super.position(position);
    }

    @Override
    public mln_camera_options getPointer(long i) {
      return new mln_camera_options((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_camera_options size(int setter);

    public native @Cast("uint32_t") int fields();

    public native mln_camera_options fields(int setter);

    public native double latitude();

    public native mln_camera_options latitude(double setter);

    public native double longitude();

    public native mln_camera_options longitude(double setter);

    public native double center_altitude();

    public native mln_camera_options center_altitude(double setter);

    public native @ByRef mln_edge_insets padding();

    public native mln_camera_options padding(mln_edge_insets setter);

    public native @ByRef mln_screen_point anchor();

    public native mln_camera_options anchor(mln_screen_point setter);

    public native double zoom();

    public native mln_camera_options zoom(double setter);

    public native double bearing();

    public native mln_camera_options bearing(double setter);

    public native double pitch();

    public native mln_camera_options pitch(double setter);

    public native double roll();

    public native mln_camera_options roll(double setter);

    public native double field_of_view();

    public native mln_camera_options field_of_view(double setter);
  }

  /** Cubic easing curve for animated camera transitions. */
  public static class mln_unit_bezier extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_unit_bezier() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_unit_bezier(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_unit_bezier(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_unit_bezier position(long position) {
      return (mln_unit_bezier) super.position(position);
    }

    @Override
    public mln_unit_bezier getPointer(long i) {
      return new mln_unit_bezier((Pointer) this).offsetAddress(i);
    }

    public native double x1();

    public native mln_unit_bezier x1(double setter);

    public native double y1();

    public native mln_unit_bezier y1(double setter);

    public native double x2();

    public native mln_unit_bezier x2(double setter);

    public native double y2();

    public native mln_unit_bezier y2(double setter);
  }

  /** Optional animation controls for camera transitions. */
  public static class mln_animation_options extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_animation_options() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_animation_options(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_animation_options(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_animation_options position(long position) {
      return (mln_animation_options) super.position(position);
    }

    @Override
    public mln_animation_options getPointer(long i) {
      return new mln_animation_options((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_animation_options size(int setter);

    public native @Cast("uint32_t") int fields();

    public native mln_animation_options fields(int setter);

    /**
     * Duration in milliseconds. Must be finite and non-negative. Values that would overflow
     * MapLibre Native's internal duration are invalid.
     */
    public native double duration_ms();

    public native mln_animation_options duration_ms(double setter);

    /** Average flyTo velocity in screenfuls per second. Must be positive. */
    public native double velocity();

    public native mln_animation_options velocity(double setter);

    /** Peak zoom for flyTo transitions. */
    public native double min_zoom();

    public native mln_animation_options min_zoom(double setter);

    public native @ByRef mln_unit_bezier easing();

    public native mln_animation_options easing(mln_unit_bezier setter);
  }

  /** Optional fitting controls for camera-for-viewport queries. */
  public static class mln_camera_fit_options extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_camera_fit_options() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_camera_fit_options(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_camera_fit_options(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_camera_fit_options position(long position) {
      return (mln_camera_fit_options) super.position(position);
    }

    @Override
    public mln_camera_fit_options getPointer(long i) {
      return new mln_camera_fit_options((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_camera_fit_options size(int setter);

    public native @Cast("uint32_t") int fields();

    public native mln_camera_fit_options fields(int setter);

    public native @ByRef mln_edge_insets padding();

    public native mln_camera_fit_options padding(mln_edge_insets setter);

    public native double bearing();

    public native mln_camera_fit_options bearing(double setter);

    public native double pitch();

    public native mln_camera_fit_options pitch(double setter);
  }

  /** Three-component vector used by free camera options. */
  public static class mln_vec3 extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_vec3() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_vec3(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_vec3(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_vec3 position(long position) {
      return (mln_vec3) super.position(position);
    }

    @Override
    public mln_vec3 getPointer(long i) {
      return new mln_vec3((Pointer) this).offsetAddress(i);
    }

    public native double x();

    public native mln_vec3 x(double setter);

    public native double y();

    public native mln_vec3 y(double setter);

    public native double z();

    public native mln_vec3 z(double setter);
  }

  /** Quaternion stored as x, y, z, w components. */
  public static class mln_quaternion extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_quaternion() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_quaternion(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_quaternion(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_quaternion position(long position) {
      return (mln_quaternion) super.position(position);
    }

    @Override
    public mln_quaternion getPointer(long i) {
      return new mln_quaternion((Pointer) this).offsetAddress(i);
    }

    public native double x();

    public native mln_quaternion x(double setter);

    public native double y();

    public native mln_quaternion y(double setter);

    public native double z();

    public native mln_quaternion z(double setter);

    public native double w();

    public native mln_quaternion w(double setter);
  }

  /** Free camera position and orientation in MapLibre Native camera space. */
  public static class mln_free_camera_options extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_free_camera_options() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_free_camera_options(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_free_camera_options(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_free_camera_options position(long position) {
      return (mln_free_camera_options) super.position(position);
    }

    @Override
    public mln_free_camera_options getPointer(long i) {
      return new mln_free_camera_options((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_free_camera_options size(int setter);

    public native @Cast("uint32_t") int fields();

    public native mln_free_camera_options fields(int setter);

    public native @ByRef @Name("position") mln_vec3 _position();

    public native mln_free_camera_options _position(mln_vec3 setter);

    public native @ByRef mln_quaternion orientation();

    public native mln_free_camera_options orientation(mln_quaternion setter);
  }

  /** Geographic coordinate in degrees used by map and projection APIs. */
  public static class mln_lat_lng extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_lat_lng() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_lat_lng(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_lat_lng(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_lat_lng position(long position) {
      return (mln_lat_lng) super.position(position);
    }

    @Override
    public mln_lat_lng getPointer(long i) {
      return new mln_lat_lng((Pointer) this).offsetAddress(i);
    }

    /** Latitude in degrees. Input latitude must be finite and within [-90, 90]. */
    public native double latitude();

    public native mln_lat_lng latitude(double setter);

    /** Longitude in degrees. Input longitude must be finite. */
    public native double longitude();

    public native mln_lat_lng longitude(double setter);
  }

  /** UTF-8 text view. The pointer may be null only when size is 0. */
  public static class mln_string_view extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_string_view() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_string_view(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_string_view(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_string_view position(long position) {
      return (mln_string_view) super.position(position);
    }

    @Override
    public mln_string_view getPointer(long i) {
      return new mln_string_view((Pointer) this).offsetAddress(i);
    }

    /** UTF-8 bytes. Null only when size is 0. */
    public native @Cast("const char*") BytePointer data();

    public native mln_string_view data(BytePointer setter);

    public native @Cast("size_t") long size();

    public native mln_string_view size(long setter);
  }

  /** Geometry variant tags used by mln_geometry. */
  /** enum mln_geometry_type */
  public static final int MLN_GEOMETRY_TYPE_EMPTY = 0,
      MLN_GEOMETRY_TYPE_POINT = 1,
      MLN_GEOMETRY_TYPE_LINE_STRING = 2,
      MLN_GEOMETRY_TYPE_POLYGON = 3,
      MLN_GEOMETRY_TYPE_MULTI_POINT = 4,
      MLN_GEOMETRY_TYPE_MULTI_LINE_STRING = 5,
      MLN_GEOMETRY_TYPE_MULTI_POLYGON = 6,
      MLN_GEOMETRY_TYPE_GEOMETRY_COLLECTION = 7;

  /** Coordinate array view. Coordinates are latitude/longitude pairs. */
  public static class mln_coordinate_span extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_coordinate_span() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_coordinate_span(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_coordinate_span(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_coordinate_span position(long position) {
      return (mln_coordinate_span) super.position(position);
    }

    @Override
    public mln_coordinate_span getPointer(long i) {
      return new mln_coordinate_span((Pointer) this).offsetAddress(i);
    }

    /** Coordinates. Null only when coordinate_count is 0. */
    public native @Const mln_lat_lng coordinates();

    public native mln_coordinate_span coordinates(mln_lat_lng setter);

    public native @Cast("size_t") long coordinate_count();

    public native mln_coordinate_span coordinate_count(long setter);
  }

  /** Polygon ring array view. Each ring is a coordinate span. */
  public static class mln_polygon_geometry extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_polygon_geometry() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_polygon_geometry(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_polygon_geometry(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_polygon_geometry position(long position) {
      return (mln_polygon_geometry) super.position(position);
    }

    @Override
    public mln_polygon_geometry getPointer(long i) {
      return new mln_polygon_geometry((Pointer) this).offsetAddress(i);
    }

    /** Rings. Null only when ring_count is 0. */
    public native @Const mln_coordinate_span rings();

    public native mln_polygon_geometry rings(mln_coordinate_span setter);

    public native @Cast("size_t") long ring_count();

    public native mln_polygon_geometry ring_count(long setter);
  }

  /** Multi-line geometry view. Each line is a coordinate span. */
  public static class mln_multi_line_geometry extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_multi_line_geometry() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_multi_line_geometry(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_multi_line_geometry(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_multi_line_geometry position(long position) {
      return (mln_multi_line_geometry) super.position(position);
    }

    @Override
    public mln_multi_line_geometry getPointer(long i) {
      return new mln_multi_line_geometry((Pointer) this).offsetAddress(i);
    }

    /** Lines. Null only when line_count is 0. */
    public native @Const mln_coordinate_span lines();

    public native mln_multi_line_geometry lines(mln_coordinate_span setter);

    public native @Cast("size_t") long line_count();

    public native mln_multi_line_geometry line_count(long setter);
  }

  /** Multi-polygon geometry view. Each polygon contains ring views. */
  public static class mln_multi_polygon_geometry extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_multi_polygon_geometry() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_multi_polygon_geometry(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_multi_polygon_geometry(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_multi_polygon_geometry position(long position) {
      return (mln_multi_polygon_geometry) super.position(position);
    }

    @Override
    public mln_multi_polygon_geometry getPointer(long i) {
      return new mln_multi_polygon_geometry((Pointer) this).offsetAddress(i);
    }

    /** Polygons. Null only when polygon_count is 0. */
    public native @Const mln_polygon_geometry polygons();

    public native mln_multi_polygon_geometry polygons(mln_polygon_geometry setter);

    public native @Cast("size_t") long polygon_count();

    public native mln_multi_polygon_geometry polygon_count(long setter);
  }

  /** Geometry collection view. */
  public static class mln_geometry_collection extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_geometry_collection() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_geometry_collection(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_geometry_collection(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_geometry_collection position(long position) {
      return (mln_geometry_collection) super.position(position);
    }

    @Override
    public mln_geometry_collection getPointer(long i) {
      return new mln_geometry_collection((Pointer) this).offsetAddress(i);
    }

    /** Child geometries. Null only when geometry_count is 0. */
    public native @Const mln_geometry geometries();

    public native mln_geometry_collection geometries(mln_geometry setter);

    public native @Cast("size_t") long geometry_count();

    public native mln_geometry_collection geometry_count(long setter);
  }

  /**
   * MapLibre geometry input descriptor graph.
   *
   * <p>Geometry coordinates use mln_lat_lng for consistency with the rest of the C API. They are
   * converted to native geometry points as longitude/latitude. A root geometry descriptor starts at
   * nesting depth 0. Status-returning functions reject geometry collection children past depth 64
   * with MLN_STATUS_INVALID_ARGUMENT.
   */
  public static class mln_geometry extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_geometry() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_geometry(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_geometry(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_geometry position(long position) {
      return (mln_geometry) super.position(position);
    }

    @Override
    public mln_geometry getPointer(long i) {
      return new mln_geometry((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_geometry size(int setter);

    /** One of mln_geometry_type. */
    public native @Cast("uint32_t") int type();

    public native mln_geometry type(int setter);

    @Name("data.point")
    public native @ByRef mln_lat_lng data_point();

    public native mln_geometry data_point(mln_lat_lng setter);

    @Name("data.line_string")
    public native @ByRef mln_coordinate_span data_line_string();

    public native mln_geometry data_line_string(mln_coordinate_span setter);

    @Name("data.polygon")
    public native @ByRef mln_polygon_geometry data_polygon();

    public native mln_geometry data_polygon(mln_polygon_geometry setter);

    @Name("data.multi_point")
    public native @ByRef mln_coordinate_span data_multi_point();

    public native mln_geometry data_multi_point(mln_coordinate_span setter);

    @Name("data.multi_line_string")
    public native @ByRef mln_multi_line_geometry data_multi_line_string();

    public native mln_geometry data_multi_line_string(mln_multi_line_geometry setter);

    @Name("data.multi_polygon")
    public native @ByRef mln_multi_polygon_geometry data_multi_polygon();

    public native mln_geometry data_multi_polygon(mln_multi_polygon_geometry setter);

    @Name("data.geometry_collection")
    public native @ByRef mln_geometry_collection data_geometry_collection();

    public native mln_geometry data_geometry_collection(mln_geometry_collection setter);
  }

  /** JSON-like value variant tags used by mln_json_value. */
  /** enum mln_json_value_type */
  public static final int MLN_JSON_VALUE_TYPE_NULL = 0,
      MLN_JSON_VALUE_TYPE_BOOL = 1,
      MLN_JSON_VALUE_TYPE_UINT = 2,
      MLN_JSON_VALUE_TYPE_INT = 3,
      MLN_JSON_VALUE_TYPE_DOUBLE = 4,
      MLN_JSON_VALUE_TYPE_STRING = 5,
      MLN_JSON_VALUE_TYPE_ARRAY = 6,
      MLN_JSON_VALUE_TYPE_OBJECT = 7;

  /** JSON value array view. */
  public static class mln_json_array extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_json_array() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_json_array(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_json_array(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_json_array position(long position) {
      return (mln_json_array) super.position(position);
    }

    @Override
    public mln_json_array getPointer(long i) {
      return new mln_json_array((Pointer) this).offsetAddress(i);
    }

    /** Values. Null only when value_count is 0. */
    public native @Const mln_json_value values();

    public native mln_json_array values(mln_json_value setter);

    public native @Cast("size_t") long value_count();

    public native mln_json_array value_count(long setter);
  }

  /** JSON object member view. */
  public static class mln_json_member extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_json_member() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_json_member(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_json_member(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_json_member position(long position) {
      return (mln_json_member) super.position(position);
    }

    @Override
    public mln_json_member getPointer(long i) {
      return new mln_json_member((Pointer) this).offsetAddress(i);
    }

    public native @ByRef mln_string_view key();

    public native mln_json_member key(mln_string_view setter);

    /** Value descriptor. Must not be null. */
    public native @Const mln_json_value value();

    public native mln_json_member value(mln_json_value setter);
  }

  /** JSON object member array view. */
  public static class mln_json_object extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_json_object() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_json_object(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_json_object(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_json_object position(long position) {
      return (mln_json_object) super.position(position);
    }

    @Override
    public mln_json_object getPointer(long i) {
      return new mln_json_object((Pointer) this).offsetAddress(i);
    }

    /** Members. Null only when member_count is 0. */
    public native @Const mln_json_member members();

    public native mln_json_object members(mln_json_member setter);

    public native @Cast("size_t") long member_count();

    public native mln_json_object member_count(long setter);
  }

  /**
   * JSON-like value input descriptor graph used by feature properties/states.
   *
   * <p>Input functions reject NaN and infinities for double values because JSON and GeoJSON numbers
   * are finite. A root JSON value descriptor starts at nesting depth 0. Status-returning functions
   * reject array/object children past depth 64 with MLN_STATUS_INVALID_ARGUMENT.
   */
  public static class mln_json_value extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_json_value() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_json_value(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_json_value(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_json_value position(long position) {
      return (mln_json_value) super.position(position);
    }

    @Override
    public mln_json_value getPointer(long i) {
      return new mln_json_value((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_json_value size(int setter);

    /** One of mln_json_value_type. */
    public native @Cast("uint32_t") int type();

    public native mln_json_value type(int setter);

    @Name("data.bool_value")
    public native @Cast("bool") boolean data_bool_value();

    public native mln_json_value data_bool_value(boolean setter);

    @Name("data.uint_value")
    public native @Cast("uint64_t") long data_uint_value();

    public native mln_json_value data_uint_value(long setter);

    @Name("data.int_value")
    public native @Cast("int64_t") long data_int_value();

    public native mln_json_value data_int_value(long setter);

    @Name("data.double_value")
    public native double data_double_value();

    public native mln_json_value data_double_value(double setter);

    @Name("data.string_value")
    public native @ByRef mln_string_view data_string_value();

    public native mln_json_value data_string_value(mln_string_view setter);

    @Name("data.array_value")
    public native @ByRef mln_json_array data_array_value();

    public native mln_json_value data_array_value(mln_json_array setter);

    @Name("data.object_value")
    public native @ByRef mln_json_object data_object_value();

    public native mln_json_value data_object_value(mln_json_object setter);
  }

  /** Optional fields for mln_feature_state_selector. */
  /** enum mln_feature_state_selector_field */
  public static final int MLN_FEATURE_STATE_SELECTOR_SOURCE_LAYER_ID = 1 << 0,
      MLN_FEATURE_STATE_SELECTOR_FEATURE_ID = 1 << 1,
      MLN_FEATURE_STATE_SELECTOR_STATE_KEY = 1 << 2;

  /** Feature-state source, feature, and key selector. */
  public static class mln_feature_state_selector extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_feature_state_selector() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_feature_state_selector(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_feature_state_selector(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_feature_state_selector position(long position) {
      return (mln_feature_state_selector) super.position(position);
    }

    @Override
    public mln_feature_state_selector getPointer(long i) {
      return new mln_feature_state_selector((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_feature_state_selector size(int setter);

    public native @Cast("uint32_t") int fields();

    public native mln_feature_state_selector fields(int setter);

    /** Source ID. Required and borrowed for the duration of the call. */
    public native @ByRef mln_string_view source_id();

    public native mln_feature_state_selector source_id(mln_string_view setter);

    /** Optional source layer ID. Required for vector-source disambiguation. */
    public native @ByRef mln_string_view source_layer_id();

    public native mln_feature_state_selector source_layer_id(mln_string_view setter);

    /** Optional feature ID string. Required by set/get and optional for remove. */
    public native @ByRef mln_string_view feature_id();

    public native mln_feature_state_selector feature_id(mln_string_view setter);

    /** Optional state key. Used only by remove and requires feature_id. */
    public native @ByRef mln_string_view state_key();

    public native mln_feature_state_selector state_key(mln_string_view setter);
  }

  /** Feature identifier variant tags used by mln_feature. */
  /** enum mln_feature_identifier_type */
  public static final int MLN_FEATURE_IDENTIFIER_TYPE_NULL = 0,
      MLN_FEATURE_IDENTIFIER_TYPE_UINT = 1,
      MLN_FEATURE_IDENTIFIER_TYPE_INT = 2,
      MLN_FEATURE_IDENTIFIER_TYPE_DOUBLE = 3,
      MLN_FEATURE_IDENTIFIER_TYPE_STRING = 4;

  /** GeoJSON feature input descriptor graph. */
  public static class mln_feature extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_feature() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_feature(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_feature(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_feature position(long position) {
      return (mln_feature) super.position(position);
    }

    @Override
    public mln_feature getPointer(long i) {
      return new mln_feature((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_feature size(int setter);

    /** Geometry descriptor. Must not be null. Use MLN_GEOMETRY_TYPE_EMPTY for an empty geometry. */
    public native @Const mln_geometry geometry();

    public native mln_feature geometry(mln_geometry setter);

    /** Property member views. May be null only when property_count is 0. */
    public native @Const mln_json_member properties();

    public native mln_feature properties(mln_json_member setter);

    public native @Cast("size_t") long property_count();

    public native mln_feature property_count(long setter);

    /** One of mln_feature_identifier_type. */
    public native @Cast("uint32_t") int identifier_type();

    public native mln_feature identifier_type(int setter);

    @Name("identifier.uint_value")
    public native @Cast("uint64_t") long identifier_uint_value();

    public native mln_feature identifier_uint_value(long setter);

    @Name("identifier.int_value")
    public native @Cast("int64_t") long identifier_int_value();

    public native mln_feature identifier_int_value(long setter);

    @Name("identifier.double_value")
    public native double identifier_double_value();

    public native mln_feature identifier_double_value(double setter);

    @Name("identifier.string_value")
    public native @ByRef mln_string_view identifier_string_value();

    public native mln_feature identifier_string_value(mln_string_view setter);
  }

  /** GeoJSON variant tags used by mln_geojson. */
  /** enum mln_geojson_type */
  public static final int MLN_GEOJSON_TYPE_GEOMETRY = 1,
      MLN_GEOJSON_TYPE_FEATURE = 2,
      MLN_GEOJSON_TYPE_FEATURE_COLLECTION = 3;

  /** Feature collection view. */
  public static class mln_feature_collection extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_feature_collection() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_feature_collection(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_feature_collection(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_feature_collection position(long position) {
      return (mln_feature_collection) super.position(position);
    }

    @Override
    public mln_feature_collection getPointer(long i) {
      return new mln_feature_collection((Pointer) this).offsetAddress(i);
    }

    /** Features. Null only when feature_count is 0. */
    public native @Const mln_feature features();

    public native mln_feature_collection features(mln_feature setter);

    public native @Cast("size_t") long feature_count();

    public native mln_feature_collection feature_count(long setter);
  }

  /**
   * GeoJSON geometry, feature, or feature collection input descriptor graph. Nested geometry and
   * property descriptors share the 64-depth descriptor limit documented on mln_geometry and
   * mln_json_value.
   */
  public static class mln_geojson extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_geojson() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_geojson(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_geojson(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_geojson position(long position) {
      return (mln_geojson) super.position(position);
    }

    @Override
    public mln_geojson getPointer(long i) {
      return new mln_geojson((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_geojson size(int setter);

    /** One of mln_geojson_type. */
    public native @Cast("uint32_t") int type();

    public native mln_geojson type(int setter);

    /** Geometry descriptor selected by MLN_GEOJSON_TYPE_GEOMETRY. Must not be null. */
    @Name("data.geometry")
    public native @Const mln_geometry data_geometry();

    public native mln_geojson data_geometry(mln_geometry setter);

    /** Feature descriptor selected by MLN_GEOJSON_TYPE_FEATURE. Must not be null. */
    @Name("data.feature")
    public native @Const mln_feature data_feature();

    public native mln_geojson data_feature(mln_feature setter);

    @Name("data.feature_collection")
    public native @ByRef mln_feature_collection data_feature_collection();

    public native mln_geojson data_feature_collection(mln_feature_collection setter);
  }

  /** Geographic bounds in degrees. */
  public static class mln_lat_lng_bounds extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_lat_lng_bounds() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_lat_lng_bounds(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_lat_lng_bounds(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_lat_lng_bounds position(long position) {
      return (mln_lat_lng_bounds) super.position(position);
    }

    @Override
    public mln_lat_lng_bounds getPointer(long i) {
      return new mln_lat_lng_bounds((Pointer) this).offsetAddress(i);
    }

    public native @ByRef mln_lat_lng southwest();

    public native mln_lat_lng_bounds southwest(mln_lat_lng setter);

    public native @ByRef mln_lat_lng northeast();

    public native mln_lat_lng_bounds northeast(mln_lat_lng setter);
  }

  /** Optional map camera constraint fields. */
  public static class mln_bound_options extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_bound_options() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_bound_options(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_bound_options(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_bound_options position(long position) {
      return (mln_bound_options) super.position(position);
    }

    @Override
    public mln_bound_options getPointer(long i) {
      return new mln_bound_options((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_bound_options size(int setter);

    public native @Cast("uint32_t") int fields();

    public native mln_bound_options fields(int setter);

    public native @ByRef mln_lat_lng_bounds bounds();

    public native mln_bound_options bounds(mln_lat_lng_bounds setter);

    public native double min_zoom();

    public native mln_bound_options min_zoom(double setter);

    public native double max_zoom();

    public native mln_bound_options max_zoom(double setter);

    public native double min_pitch();

    public native mln_bound_options min_pitch(double setter);

    public native double max_pitch();

    public native mln_bound_options max_pitch(double setter);
  }

  /** Tile-pyramid offline region definition. */
  public static class mln_offline_tile_pyramid_region_definition extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_offline_tile_pyramid_region_definition() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_offline_tile_pyramid_region_definition(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_offline_tile_pyramid_region_definition(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_offline_tile_pyramid_region_definition position(long position) {
      return (mln_offline_tile_pyramid_region_definition) super.position(position);
    }

    @Override
    public mln_offline_tile_pyramid_region_definition getPointer(long i) {
      return new mln_offline_tile_pyramid_region_definition((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_offline_tile_pyramid_region_definition size(int setter);

    /** Style URL. Copied during region creation. */
    public native @Cast("const char*") BytePointer style_url();

    public native mln_offline_tile_pyramid_region_definition style_url(BytePointer setter);

    public native @ByRef mln_lat_lng_bounds bounds();

    public native mln_offline_tile_pyramid_region_definition bounds(mln_lat_lng_bounds setter);

    public native double min_zoom();

    public native mln_offline_tile_pyramid_region_definition min_zoom(double setter);

    /**
     * Maximum zoom. Positive infinity follows MapLibre Native behavior and lets each tile source
     * use its own maximum zoom.
     */
    public native double max_zoom();

    public native mln_offline_tile_pyramid_region_definition max_zoom(double setter);

    public native float pixel_ratio();

    public native mln_offline_tile_pyramid_region_definition pixel_ratio(float setter);

    public native @Cast("bool") boolean include_ideographs();

    public native mln_offline_tile_pyramid_region_definition include_ideographs(boolean setter);
  }

  /** Geometry offline region definition. */
  public static class mln_offline_geometry_region_definition extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_offline_geometry_region_definition() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_offline_geometry_region_definition(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_offline_geometry_region_definition(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_offline_geometry_region_definition position(long position) {
      return (mln_offline_geometry_region_definition) super.position(position);
    }

    @Override
    public mln_offline_geometry_region_definition getPointer(long i) {
      return new mln_offline_geometry_region_definition((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_offline_geometry_region_definition size(int setter);

    /** Style URL. Copied during region creation. */
    public native @Cast("const char*") BytePointer style_url();

    public native mln_offline_geometry_region_definition style_url(BytePointer setter);

    /** Geometry descriptor. Borrowed for the duration of region creation. */
    public native @Const mln_geometry geometry();

    public native mln_offline_geometry_region_definition geometry(mln_geometry setter);

    public native double min_zoom();

    public native mln_offline_geometry_region_definition min_zoom(double setter);

    /**
     * Maximum zoom. Positive infinity follows MapLibre Native behavior and lets each tile source
     * use its own maximum zoom.
     */
    public native double max_zoom();

    public native mln_offline_geometry_region_definition max_zoom(double setter);

    public native float pixel_ratio();

    public native mln_offline_geometry_region_definition pixel_ratio(float setter);

    public native @Cast("bool") boolean include_ideographs();

    public native mln_offline_geometry_region_definition include_ideographs(boolean setter);
  }

  /** Tagged offline region definition. */
  public static class mln_offline_region_definition extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_offline_region_definition() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_offline_region_definition(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_offline_region_definition(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_offline_region_definition position(long position) {
      return (mln_offline_region_definition) super.position(position);
    }

    @Override
    public mln_offline_region_definition getPointer(long i) {
      return new mln_offline_region_definition((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_offline_region_definition size(int setter);

    /** One of mln_offline_region_definition_type. */
    public native @Cast("uint32_t") int type();

    public native mln_offline_region_definition type(int setter);

    @Name("data.tile_pyramid")
    public native @ByRef mln_offline_tile_pyramid_region_definition data_tile_pyramid();

    public native mln_offline_region_definition data_tile_pyramid(
        mln_offline_tile_pyramid_region_definition setter);

    @Name("data.geometry")
    public native @ByRef mln_offline_geometry_region_definition data_geometry();

    public native mln_offline_region_definition data_geometry(
        mln_offline_geometry_region_definition setter);
  }

  /** Region data view returned from a snapshot or list handle. */
  public static class mln_offline_region_info extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_offline_region_info() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_offline_region_info(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_offline_region_info(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_offline_region_info position(long position) {
      return (mln_offline_region_info) super.position(position);
    }

    @Override
    public mln_offline_region_info getPointer(long i) {
      return new mln_offline_region_info((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_offline_region_info size(int setter);

    public native @Cast("mln_offline_region_id") long id();

    public native mln_offline_region_info id(long setter);

    public native @ByRef mln_offline_region_definition definition();

    public native mln_offline_region_info definition(mln_offline_region_definition setter);

    /** Metadata bytes. Valid until the owner snapshot/list is destroyed. */
    public native @Cast("const uint8_t*") BytePointer metadata();

    public native mln_offline_region_info metadata(BytePointer setter);

    public native @Cast("size_t") long metadata_size();

    public native mln_offline_region_info metadata_size(long setter);
  }

  /**
   * Starts creating an offline region.
   *
   * <p>Input strings, geometry descriptors, and metadata are copied before this call returns.
   * Completion is reported through MLN_RUNTIME_EVENT_OFFLINE_OPERATION_COMPLETED. On successful
   * completion, call mln_runtime_offline_region_create_take_result() to take the snapshot.
   *
   * <p>Returns: - MLN_STATUS_OK when the operation was accepted and out_operation_id was set. -
   * MLN_STATUS_INVALID_ARGUMENT when runtime is null or not live, definition is null or invalid,
   * metadata is null with a non-zero size, or out_operation_id is null. - MLN_STATUS_WRONG_THREAD
   * when called from a thread other than the runtime owner thread. - MLN_STATUS_NATIVE_ERROR when
   * an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_create_start(
      mln_runtime runtime,
      @Const mln_offline_region_definition definition,
      @Cast("const uint8_t*") BytePointer metadata,
      @Cast("size_t") long metadata_size,
      @Cast("mln_offline_operation_id*") LongPointer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_create_start(
      mln_runtime runtime,
      @Const mln_offline_region_definition definition,
      @Cast("const uint8_t*") ByteBuffer metadata,
      @Cast("size_t") long metadata_size,
      @Cast("mln_offline_operation_id*") LongBuffer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_create_start(
      mln_runtime runtime,
      @Const mln_offline_region_definition definition,
      @Cast("const uint8_t*") byte[] metadata,
      @Cast("size_t") long metadata_size,
      @Cast("mln_offline_operation_id*") long[] out_operation_id);

  /**
   * Starts getting an offline region snapshot by ID.
   *
   * <p>Completion is reported through MLN_RUNTIME_EVENT_OFFLINE_OPERATION_COMPLETED. On successful
   * completion, call mln_runtime_offline_region_get_take_result().
   *
   * <p>Returns: - MLN_STATUS_OK when the operation was accepted and out_operation_id was set. -
   * MLN_STATUS_INVALID_ARGUMENT when runtime is null or not live, or out_operation_id is null. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the runtime owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_get_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("mln_offline_operation_id*") LongPointer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_get_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("mln_offline_operation_id*") LongBuffer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_get_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("mln_offline_operation_id*") long[] out_operation_id);

  /**
   * Starts listing offline region snapshots in the runtime database.
   *
   * <p>Returns: - MLN_STATUS_OK when the operation was accepted and out_operation_id was set. -
   * MLN_STATUS_INVALID_ARGUMENT when runtime is null or not live, or out_operation_id is null. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the runtime owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_regions_list_start(
      mln_runtime runtime, @Cast("mln_offline_operation_id*") LongPointer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_regions_list_start(
      mln_runtime runtime, @Cast("mln_offline_operation_id*") LongBuffer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_regions_list_start(
      mln_runtime runtime, @Cast("mln_offline_operation_id*") long[] out_operation_id);

  /**
   * Starts merging offline regions from another database path.
   *
   * <p>The side database may be upgraded in place by native code and must be writable when native
   * merge requires it.
   *
   * <p>Returns: - MLN_STATUS_OK when the operation was accepted and out_operation_id was set. -
   * MLN_STATUS_INVALID_ARGUMENT when runtime is null or not live, side_database_path is null, or
   * out_operation_id is null. - MLN_STATUS_WRONG_THREAD when called from a thread other than the
   * runtime owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to
   * status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_regions_merge_database_start(
      mln_runtime runtime,
      @Cast("const char*") BytePointer side_database_path,
      @Cast("mln_offline_operation_id*") LongPointer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_regions_merge_database_start(
      mln_runtime runtime,
      String side_database_path,
      @Cast("mln_offline_operation_id*") LongBuffer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_regions_merge_database_start(
      mln_runtime runtime,
      @Cast("const char*") BytePointer side_database_path,
      @Cast("mln_offline_operation_id*") long[] out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_regions_merge_database_start(
      mln_runtime runtime,
      String side_database_path,
      @Cast("mln_offline_operation_id*") LongPointer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_regions_merge_database_start(
      mln_runtime runtime,
      @Cast("const char*") BytePointer side_database_path,
      @Cast("mln_offline_operation_id*") LongBuffer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_regions_merge_database_start(
      mln_runtime runtime,
      String side_database_path,
      @Cast("mln_offline_operation_id*") long[] out_operation_id);

  /**
   * Starts updating opaque binary metadata for an offline region.
   *
   * <p>On successful completion, call mln_runtime_offline_region_update_metadata_take_result().
   *
   * <p>Returns: - MLN_STATUS_OK when the operation was accepted and out_operation_id was set. -
   * MLN_STATUS_INVALID_ARGUMENT when runtime is null or not live, metadata is null with a non-zero
   * size, or out_operation_id is null. - MLN_STATUS_WRONG_THREAD when called from a thread other
   * than the runtime owner thread. - MLN_STATUS_NATIVE_ERROR when an internal exception is
   * converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_update_metadata_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("const uint8_t*") BytePointer metadata,
      @Cast("size_t") long metadata_size,
      @Cast("mln_offline_operation_id*") LongPointer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_update_metadata_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("const uint8_t*") ByteBuffer metadata,
      @Cast("size_t") long metadata_size,
      @Cast("mln_offline_operation_id*") LongBuffer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_update_metadata_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("const uint8_t*") byte[] metadata,
      @Cast("size_t") long metadata_size,
      @Cast("mln_offline_operation_id*") long[] out_operation_id);

  /**
   * Starts getting the current completed/download status for an offline region.
   *
   * <p>Returns: - MLN_STATUS_OK when the operation was accepted and out_operation_id was set. -
   * MLN_STATUS_INVALID_ARGUMENT when runtime is null or not live, or out_operation_id is null. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the runtime owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_get_status_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("mln_offline_operation_id*") LongPointer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_get_status_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("mln_offline_operation_id*") LongBuffer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_get_status_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("mln_offline_operation_id*") long[] out_operation_id);

  /**
   * Enables or disables runtime events for an offline region.
   *
   * <p>Observer callbacks are copied into runtime events. Disabling observation also discards
   * queued events for this region.
   *
   * <p>Completion is reported through MLN_RUNTIME_EVENT_OFFLINE_OPERATION_COMPLETED.
   *
   * <p>Returns: - MLN_STATUS_OK when the operation was accepted and out_operation_id was set. -
   * MLN_STATUS_INVALID_ARGUMENT when runtime is null or not live, or out_operation_id is null. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the runtime owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_set_observed_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("bool") boolean observed,
      @Cast("mln_offline_operation_id*") LongPointer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_set_observed_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("bool") boolean observed,
      @Cast("mln_offline_operation_id*") LongBuffer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_set_observed_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("bool") boolean observed,
      @Cast("mln_offline_operation_id*") long[] out_operation_id);

  /**
   * Sets an offline region's native download state.
   *
   * <p>Register observation separately with mln_runtime_offline_region_set_observed_start() to
   * receive progress and error events.
   *
   * <p>Completion is reported through MLN_RUNTIME_EVENT_OFFLINE_OPERATION_COMPLETED.
   *
   * <p>Returns: - MLN_STATUS_OK when the operation was accepted and out_operation_id was set. -
   * MLN_STATUS_INVALID_ARGUMENT when runtime is null or not live, state is not a
   * mln_offline_region_download_state value, or out_operation_id is null. - MLN_STATUS_WRONG_THREAD
   * when called from a thread other than the runtime owner thread. - MLN_STATUS_NATIVE_ERROR when
   * an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_set_download_state_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("uint32_t") int state,
      @Cast("mln_offline_operation_id*") LongPointer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_set_download_state_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("uint32_t") int state,
      @Cast("mln_offline_operation_id*") LongBuffer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_set_download_state_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("uint32_t") int state,
      @Cast("mln_offline_operation_id*") long[] out_operation_id);

  /**
   * Invalidates cached resources for an offline region.
   *
   * <p>Completion is reported through MLN_RUNTIME_EVENT_OFFLINE_OPERATION_COMPLETED.
   *
   * <p>Returns: - MLN_STATUS_OK when the operation was accepted and out_operation_id was set. -
   * MLN_STATUS_INVALID_ARGUMENT when runtime is null or not live, or out_operation_id is null. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the runtime owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_invalidate_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("mln_offline_operation_id*") LongPointer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_invalidate_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("mln_offline_operation_id*") LongBuffer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_invalidate_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("mln_offline_operation_id*") long[] out_operation_id);

  /**
   * Deletes an offline region.
   *
   * <p>Completion is reported through MLN_RUNTIME_EVENT_OFFLINE_OPERATION_COMPLETED.
   *
   * <p>Returns: - MLN_STATUS_OK when the operation was accepted and out_operation_id was set. -
   * MLN_STATUS_INVALID_ARGUMENT when runtime is null or not live, or out_operation_id is null. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the runtime owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_delete_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("mln_offline_operation_id*") LongPointer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_delete_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("mln_offline_operation_id*") LongBuffer out_operation_id);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_delete_start(
      mln_runtime runtime,
      @Cast("mln_offline_region_id") long region_id,
      @Cast("mln_offline_operation_id*") long[] out_operation_id);

  /**
   * Takes the snapshot result from a completed offline region create operation.
   *
   * <p>Must only be called after the matching mln_runtime_offline_region_create_start() operation
   * has completed successfully (MLN_RUNTIME_EVENT_OFFLINE_OPERATION_COMPLETED with result status
   * MLN_STATUS_OK). The caller owns the returned snapshot handle and must destroy it with
   * mln_offline_region_snapshot_destroy().
   *
   * <p>On success, the operation entry is consumed. On failure, it remains live so the caller may
   * retry this call or discard the operation with mln_runtime_offline_operation_discard().
   *
   * <p>Returns: - MLN_STATUS_OK when the result was taken and out_region was set. -
   * MLN_STATUS_INVALID_STATE when the operation has not completed or its result kind does not match
   * a region snapshot. - MLN_STATUS_INVALID_ARGUMENT when runtime is null or not live, or
   * out_region is null.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_create_take_result(
      mln_runtime runtime,
      @Cast("mln_offline_operation_id") long operation_id,
      @Cast("mln_offline_region_snapshot**") PointerPointer out_region);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_create_take_result(
      mln_runtime runtime,
      @Cast("mln_offline_operation_id") long operation_id,
      @ByPtrPtr mln_offline_region_snapshot out_region);

  /**
   * Takes the snapshot result from a completed offline region get operation.
   *
   * <p>Must only be called after the matching mln_runtime_offline_region_get_start() operation has
   * completed successfully. The caller owns the returned snapshot handle and must destroy it with
   * mln_offline_region_snapshot_destroy().
   *
   * <p>On success, the operation entry is consumed. On failure, it remains live so the caller may
   * retry this call or discard the operation with mln_runtime_offline_operation_discard().
   *
   * <p>Returns: - MLN_STATUS_OK when the result was taken; out_found indicates whether a region
   * existed for the requested ID. - MLN_STATUS_INVALID_STATE when the operation has not completed
   * or its result kind does not match an optional region snapshot. - MLN_STATUS_INVALID_ARGUMENT
   * when runtime is null or not live, out_region is null, or out_found is null.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_get_take_result(
      mln_runtime runtime,
      @Cast("mln_offline_operation_id") long operation_id,
      @Cast("mln_offline_region_snapshot**") PointerPointer out_region,
      @Cast("bool*") BoolPointer out_found);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_get_take_result(
      mln_runtime runtime,
      @Cast("mln_offline_operation_id") long operation_id,
      @ByPtrPtr mln_offline_region_snapshot out_region,
      @Cast("bool*") BoolPointer out_found);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_get_take_result(
      mln_runtime runtime,
      @Cast("mln_offline_operation_id") long operation_id,
      @ByPtrPtr mln_offline_region_snapshot out_region,
      @Cast("bool*") boolean[] out_found);

  /**
   * Takes the region list from a completed offline regions list operation.
   *
   * <p>Must only be called after the matching mln_runtime_offline_regions_list_start() operation
   * has completed successfully. The caller owns the returned list handle and must destroy it with
   * mln_offline_region_list_destroy().
   *
   * <p>On success, the operation entry is consumed. On failure, it remains live so the caller may
   * retry this call or discard the operation with mln_runtime_offline_operation_discard().
   *
   * <p>Returns: - MLN_STATUS_OK when the result was taken and out_regions was set. -
   * MLN_STATUS_INVALID_STATE when the operation has not completed or its result kind does not match
   * a region list. - MLN_STATUS_INVALID_ARGUMENT when runtime is null or not live, or out_regions
   * is null.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_regions_list_take_result(
      mln_runtime runtime,
      @Cast("mln_offline_operation_id") long operation_id,
      @Cast("mln_offline_region_list**") PointerPointer out_regions);

  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_regions_list_take_result(
      mln_runtime runtime,
      @Cast("mln_offline_operation_id") long operation_id,
      @ByPtrPtr mln_offline_region_list out_regions);

  /**
   * Takes the region list from a completed offline database merge operation.
   *
   * <p>Must only be called after the matching mln_runtime_offline_regions_merge_database_start()
   * operation has completed successfully. The caller owns the returned list handle and must destroy
   * it with mln_offline_region_list_destroy().
   *
   * <p>On success, the operation entry is consumed. On failure, it remains live so the caller may
   * retry this call or discard the operation with mln_runtime_offline_operation_discard().
   *
   * <p>Returns: - MLN_STATUS_OK when the result was taken and out_regions was set. -
   * MLN_STATUS_INVALID_STATE when the operation has not completed or its result kind does not match
   * a region list. - MLN_STATUS_INVALID_ARGUMENT when runtime is null or not live, or out_regions
   * is null.
   */
  @NoException
  public static native @Cast("mln_status") int
      mln_runtime_offline_regions_merge_database_take_result(
          mln_runtime runtime,
          @Cast("mln_offline_operation_id") long operation_id,
          @Cast("mln_offline_region_list**") PointerPointer out_regions);

  @NoException
  public static native @Cast("mln_status") int
      mln_runtime_offline_regions_merge_database_take_result(
          mln_runtime runtime,
          @Cast("mln_offline_operation_id") long operation_id,
          @ByPtrPtr mln_offline_region_list out_regions);

  /**
   * Takes the snapshot result from a completed offline region update-metadata operation.
   *
   * <p>Must only be called after the matching mln_runtime_offline_region_update_metadata_start()
   * operation has completed successfully. The caller owns the returned snapshot handle and must
   * destroy it with mln_offline_region_snapshot_destroy().
   *
   * <p>On success, the operation entry is consumed. On failure, it remains live so the caller may
   * retry this call or discard the operation with mln_runtime_offline_operation_discard().
   *
   * <p>Returns: - MLN_STATUS_OK when the result was taken and out_region was set. -
   * MLN_STATUS_INVALID_STATE when the operation has not completed or its result kind does not match
   * a region snapshot. - MLN_STATUS_INVALID_ARGUMENT when runtime is null or not live, or
   * out_region is null.
   */
  @NoException
  public static native @Cast("mln_status") int
      mln_runtime_offline_region_update_metadata_take_result(
          mln_runtime runtime,
          @Cast("mln_offline_operation_id") long operation_id,
          @Cast("mln_offline_region_snapshot**") PointerPointer out_region);

  @NoException
  public static native @Cast("mln_status") int
      mln_runtime_offline_region_update_metadata_take_result(
          mln_runtime runtime,
          @Cast("mln_offline_operation_id") long operation_id,
          @ByPtrPtr mln_offline_region_snapshot out_region);

  /**
   * Takes the status struct from a completed offline region get-status operation.
   *
   * <p>Must only be called after the matching mln_runtime_offline_region_get_status_start()
   * operation has completed successfully. The caller provides a pre-allocated
   * mln_offline_region_status struct which is filled by this function.
   *
   * <p>On success, the operation entry is consumed. On failure, it remains live so the caller may
   * retry this call or discard the operation with mln_runtime_offline_operation_discard().
   *
   * <p>Returns: - MLN_STATUS_OK when the result was taken and out_status was filled. -
   * MLN_STATUS_INVALID_STATE when the operation has not completed or its result kind does not match
   * a region status. - MLN_STATUS_INVALID_ARGUMENT when runtime is null or not live, or out_status
   * is null.
   */
  @NoException
  public static native @Cast("mln_status") int mln_runtime_offline_region_get_status_take_result(
      mln_runtime runtime,
      @Cast("mln_offline_operation_id") long operation_id,
      mln_offline_region_status out_status);

  /**
   * Copies a region data view out of a snapshot handle.
   *
   * <p>On success, out_info receives pointers into snapshot-owned storage. Those pointers remain
   * valid until the snapshot is destroyed.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when snapshot is null or
   * not live, out_info is null, or out_info->size is too small. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_offline_region_snapshot_get(
      @Const mln_offline_region_snapshot snapshot, mln_offline_region_info out_info);

  /** Destroys an offline region snapshot handle. Null is accepted as a no-op. */
  @NoException
  public static native void mln_offline_region_snapshot_destroy(
      mln_offline_region_snapshot snapshot);

  /**
   * Gets the number of regions in a list handle.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when list is null or not
   * live, or out_count is null. - MLN_STATUS_NATIVE_ERROR when an internal exception is converted
   * to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_offline_region_list_count(
      @Const mln_offline_region_list list, @Cast("size_t*") SizeTPointer out_count);

  /**
   * Copies a region data view for one list entry.
   *
   * <p>On success, out_info receives pointers into list-owned storage. Those pointers remain valid
   * until the list is destroyed.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when list is null or not
   * live, index is out of range, out_info is null, or out_info->size is too small. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_offline_region_list_get(
      @Const mln_offline_region_list list,
      @Cast("size_t") long index,
      mln_offline_region_info out_info);

  /** Destroys an offline region list handle. Null is accepted as a no-op. */
  @NoException
  public static native void mln_offline_region_list_destroy(mln_offline_region_list list);

  /**
   * Lower-level Spherical Mercator projected-meter coordinate.
   *
   * <p>Map coordinate conversion APIs use mln_lat_lng. This type is only for Mercator helper
   * functions.
   */
  public static class mln_projected_meters extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_projected_meters() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_projected_meters(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_projected_meters(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_projected_meters position(long position) {
      return (mln_projected_meters) super.position(position);
    }

    @Override
    public mln_projected_meters getPointer(long i) {
      return new mln_projected_meters((Pointer) this).offsetAddress(i);
    }

    /** Distance measured northward from the equator, in meters. */
    public native double northing();

    public native mln_projected_meters northing(double setter);

    /** Distance measured eastward from the prime meridian, in meters. */
    public native double easting();

    public native mln_projected_meters easting(double setter);
  }

  /**
   * MapLibre axonometric rendering options used for snapshots and commands.
   *
   * <p>MapLibre Native names this native type ProjectionMode. It controls the live map render
   * transform, not the geographic coordinate model.
   */
  public static class mln_projection_mode extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_projection_mode() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_projection_mode(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_projection_mode(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_projection_mode position(long position) {
      return (mln_projection_mode) super.position(position);
    }

    @Override
    public mln_projection_mode getPointer(long i) {
      return new mln_projection_mode((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_projection_mode size(int setter);

    public native @Cast("uint32_t") int fields();

    public native mln_projection_mode fields(int setter);

    /** Enables a non-perspective axonometric render transform. */
    public native @Cast("bool") boolean axonometric();

    public native mln_projection_mode axonometric(boolean setter);

    /** Native x-skew factor used by the axonometric transform. */
    public native double x_skew();

    public native mln_projection_mode x_skew(double setter);

    /** Native y-skew factor used by the axonometric transform. */
    public native double y_skew();

    public native mln_projection_mode y_skew(double setter);
  }

  /** Live map viewport and render-transform controls. */
  public static class mln_map_viewport_options extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_map_viewport_options() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_map_viewport_options(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_map_viewport_options(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_map_viewport_options position(long position) {
      return (mln_map_viewport_options) super.position(position);
    }

    @Override
    public mln_map_viewport_options getPointer(long i) {
      return new mln_map_viewport_options((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_map_viewport_options size(int setter);

    public native @Cast("uint32_t") int fields();

    public native mln_map_viewport_options fields(int setter);

    /** One of mln_north_orientation. */
    public native @Cast("uint32_t") int north_orientation();

    public native mln_map_viewport_options north_orientation(int setter);

    /** One of mln_constrain_mode. */
    public native @Cast("uint32_t") int constrain_mode();

    public native mln_map_viewport_options constrain_mode(int setter);

    /** One of mln_viewport_mode. */
    public native @Cast("uint32_t") int viewport_mode();

    public native mln_map_viewport_options viewport_mode(int setter);

    public native @ByRef mln_edge_insets frustum_offset();

    public native mln_map_viewport_options frustum_offset(mln_edge_insets setter);
  }

  /** Tile prefetch and LOD tuning controls. */
  public static class mln_map_tile_options extends Pointer {
    static {
      Loader.load();
    }

    /** Default native constructor. */
    public mln_map_tile_options() {
      super((Pointer) null);
      allocate();
    }

    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public mln_map_tile_options(long size) {
      super((Pointer) null);
      allocateArray(size);
    }

    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public mln_map_tile_options(Pointer p) {
      super(p);
    }

    private native void allocate();

    private native void allocateArray(long size);

    @Override
    public mln_map_tile_options position(long position) {
      return (mln_map_tile_options) super.position(position);
    }

    @Override
    public mln_map_tile_options getPointer(long i) {
      return new mln_map_tile_options((Pointer) this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int size();

    public native mln_map_tile_options size(int setter);

    public native @Cast("uint32_t") int fields();

    public native mln_map_tile_options fields(int setter);

    /** Native uint8_t prefetch zoom delta. */
    public native @Cast("uint32_t") int prefetch_zoom_delta();

    public native mln_map_tile_options prefetch_zoom_delta(int setter);

    public native double lod_min_radius();

    public native mln_map_tile_options lod_min_radius(double setter);

    public native double lod_scale();

    public native mln_map_tile_options lod_scale(double setter);

    public native double lod_pitch_threshold();

    public native mln_map_tile_options lod_pitch_threshold(double setter);

    public native double lod_zoom_shift();

    public native mln_map_tile_options lod_zoom_shift(double setter);

    /** One of mln_tile_lod_mode. */
    public native @Cast("uint32_t") int lod_mode();

    public native mln_map_tile_options lod_mode(int setter);
  }

  /** Returns map options initialized for this C API version. */
  @NoException
  public static native @ByVal mln_map_options mln_map_options_default();

  /**
   * Creates a map handle on the runtime owner thread.
   *
   * <p>On success, the runtime owner thread becomes the map owner thread.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when runtime is null or
   * not live, out_map is null, *out_map is not null, or options are invalid. -
   * MLN_STATUS_WRONG_THREAD when called from a thread other than the runtime owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_create(
      mln_runtime runtime,
      @Const mln_map_options options,
      @Cast("mln_map**") PointerPointer out_map);

  @NoException
  public static native @Cast("mln_status") int mln_map_create(
      mln_runtime runtime, @Const mln_map_options options, @ByPtrPtr mln_map out_map);

  /**
   * Requests a repaint for a continuous map.
   *
   * <p>Continuous maps also invalidate automatically when style data, resources, camera, or
   * transitions change. Ask attached render targets to process the latest update when
   * MLN_RUNTIME_EVENT_MAP_RENDER_UPDATE_AVAILABLE is reported. Repaint requests do not produce
   * MLN_RUNTIME_EVENT_MAP_STILL_IMAGE_FINISHED or MLN_RUNTIME_EVENT_MAP_STILL_IMAGE_FAILED events.
   *
   * <p>Returns: - MLN_STATUS_OK when the request was accepted. - MLN_STATUS_INVALID_ARGUMENT when
   * map is null or not live. - MLN_STATUS_INVALID_STATE when map is not in MLN_MAP_MODE_CONTINUOUS.
   * - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_request_repaint(mln_map map);

  /**
   * Requests one still image for a static or tile map.
   *
   * <p>Pump the runtime and poll runtime events for this map until
   * MLN_RUNTIME_EVENT_MAP_STILL_IMAGE_FINISHED or MLN_RUNTIME_EVENT_MAP_STILL_IMAGE_FAILED is
   * reported. While the request is pending, process each
   * MLN_RUNTIME_EVENT_MAP_RENDER_UPDATE_AVAILABLE event from this map. Render targets use
   * mln_render_session_render_update(). Surface targets present directly. A render-update call can
   * return MLN_STATUS_INVALID_STATE before the next update is available; keep pumping and polling
   * in that case. After MLN_RUNTIME_EVENT_MAP_STILL_IMAGE_FINISHED, use the latest successful
   * texture update when the host needs image bytes or a backend texture.
   *
   * <p>Returns: - MLN_STATUS_OK when the request was accepted. - MLN_STATUS_INVALID_ARGUMENT when
   * map is null or not live. - MLN_STATUS_INVALID_STATE when map is not in MLN_MAP_MODE_STATIC or
   * MLN_MAP_MODE_TILE, or when a still-image request is already pending. - MLN_STATUS_WRONG_THREAD
   * when called from a thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when an
   * internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_request_still_image(mln_map map);

  /**
   * Destroys a map handle on its owner thread.
   *
   * <p>The map must not have an attached render session.
   *
   * <p>Returns: - MLN_STATUS_OK on success. - MLN_STATUS_INVALID_ARGUMENT when map is null or not a
   * live map handle. - MLN_STATUS_INVALID_STATE when map still has an attached render target
   * session. - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner thread. -
   * MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_destroy(mln_map map);

  /**
   * Loads a style URL through MapLibre Native style APIs.
   *
   * <p>This is a map command. The return status reports synchronous acceptance or failure. Later
   * native success and failure are reported through runtime events.
   *
   * <p>Returns: - MLN_STATUS_OK when the load request was accepted. - MLN_STATUS_INVALID_ARGUMENT
   * when map is null, not live, or url is null. - MLN_STATUS_WRONG_THREAD when called from a thread
   * other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when a synchronous native error is
   * reported or an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_style_url(
      mln_map map, @Cast("const char*") BytePointer url);

  @NoException
  public static native @Cast("mln_status") int mln_map_set_style_url(mln_map map, String url);

  /**
   * Loads inline style JSON through MapLibre Native style APIs.
   *
   * <p>This is a map command. The return status reports synchronous acceptance or failure. Later
   * native success and failure are reported through runtime events. Malformed JSON can fail
   * synchronously and still enqueue a loading-failed event.
   *
   * <p>Returns: - MLN_STATUS_OK when the load request was accepted. - MLN_STATUS_INVALID_ARGUMENT
   * when map is null, not live, or json is null. - MLN_STATUS_WRONG_THREAD when called from a
   * thread other than the map owner thread. - MLN_STATUS_NATIVE_ERROR when a synchronous native
   * error is reported or an internal exception is converted to status.
   */
  @NoException
  public static native @Cast("mln_status") int mln_map_set_style_json(
      mln_map map, @Cast("const char*") BytePointer json);

  @NoException
  public static native @Cast("mln_status") int mln_map_set_style_json(mln_map map, String json);

  // #ifdef __cplusplus
  // #endif

  // #endif  // MAPLIBRE_NATIVE_C_MAP_H

}
