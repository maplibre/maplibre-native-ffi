[tasks.build]
depends = ["//:ensure-native-library"]
dir = "{{env.MLN_FFI_REPO_ROOT}}"
run = '''
cargo build -p maplibre-native-sys -p maplibre-native-core -p maplibre-native
'''
run_windows = '''
export PKG_CONFIG_PATH="$(cygpath -u "$MLN_FFI_BUILD_DIR"):$(cygpath -u "$MLN_FFI_DEPENDENCY_LIBRARY_DIR")/pkgconfig"
cargo build -p maplibre-native-sys -p maplibre-native-core -p maplibre-native
'''

[tasks.test]
depends = ["//:ensure-native-library"]
dir = "{{env.MLN_FFI_REPO_ROOT}}"
run = '''
cargo test -p maplibre-native-sys -p maplibre-native-core -p maplibre-native -- --test-threads=1
'''
run_windows = '''
export PKG_CONFIG_PATH="$(cygpath -u "$MLN_FFI_BUILD_DIR"):$(cygpath -u "$MLN_FFI_DEPENDENCY_LIBRARY_DIR")/pkgconfig"
cargo test -p maplibre-native-sys -p maplibre-native-core -p maplibre-native -- --test-threads=1
'''

[tasks.ci]
depends = [":test"]
dir = "{{env.MLN_FFI_REPO_ROOT}}"
run = '''
cargo clippy -p maplibre-native-sys -p maplibre-native-core -p maplibre-native --all-targets -- -D warnings
'''
run_windows = '''
export PKG_CONFIG_PATH="$(cygpath -u "$MLN_FFI_BUILD_DIR"):$(cygpath -u "$MLN_FFI_DEPENDENCY_LIBRARY_DIR")/pkgconfig"
cargo clippy -p maplibre-native-sys -p maplibre-native-core -p maplibre-native --all-targets -- -D warnings
'''
