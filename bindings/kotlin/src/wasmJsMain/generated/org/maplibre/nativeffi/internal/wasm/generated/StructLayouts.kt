// Generated by scripts/generate-wasm-struct-layouts.py from the browser ABI
// manifest. Edit neither; change the generator or the manifest it reads.
//
// Offsets, sizes, and field accessors are measured for wasm32-unknown-emscripten.
// A host that loads a browser module built from different headers is rejected
// before any of these are used; see BrowserModule.
//
// The accessors exist so that no hand-written code names a *field* offset. An
// offset alone says where four bytes are, not whether they hold an integer, an
// enum, or a pointer, and reading a descriptor at the wrong width is the failure
// this generated layer exists to prevent. Hand-written code still positions its
// own scratch -- an out-parameter placed after a descriptor, say -- and those
// offsets belong to the caller rather than to any C struct.

package org.maplibre.nativeffi.internal.wasm.generated

import org.maplibre.nativeffi.internal.wasm.Heap
import org.maplibre.nativeffi.internal.wasm.HeapPointer

/** Byte offsets and sizes of the C API's descriptor structs. */
internal object StructLayouts {
  /**
   * Digest of the headers these offsets were measured from.
   *
   * The loader refuses a browser module whose manifest reports a different one. Nothing else can
   * catch that mismatch: the documented C ABI version stays zero for the whole prerelease, so a
   * module built from other headers reports the same version and the first descriptor written at
   * these offsets corrupts its heap.
   */
  const val HEADERS_DIGEST: String =
    "72DDB338BE17F42A9CCE15BFA7D84FB8556C1A96A57ABF24644A600A43BAD26C"
}

/** Values of `enum mln_adapter_http_header_route_flags`. */
internal object MlnAdapterHttpHeaderRouteFlags {
  const val MLN_ADAPTER_HTTP_HEADER_ROUTE_FLAGS_NONE: Int = 0
  const val MLN_ADAPTER_HTTP_HEADER_ROUTE_MATCH_PREFIX: Int = 1
}

/** Values of `enum mln_adapter_resource_route_flags`. */
internal object MlnAdapterResourceRouteFlags {
  const val MLN_ADAPTER_RESOURCE_ROUTE_FLAGS_NONE: Int = 0
  const val MLN_ADAPTER_RESOURCE_ROUTE_MATCH_PREFIX: Int = 1
  const val MLN_ADAPTER_RESOURCE_ROUTE_USE_REQUESTED_URL: Int = 2
}

/** Values of `enum mln_ambient_cache_operation`. */
internal object MlnAmbientCacheOperation {
  const val MLN_AMBIENT_CACHE_OPERATION_RESET_DATABASE: Int = 1
  const val MLN_AMBIENT_CACHE_OPERATION_PACK_DATABASE: Int = 2
  const val MLN_AMBIENT_CACHE_OPERATION_INVALIDATE: Int = 3
  const val MLN_AMBIENT_CACHE_OPERATION_CLEAR: Int = 4
}

/** Values of `enum mln_animation_option_field`. */
internal object MlnAnimationOptionField {
  const val MLN_ANIMATION_OPTION_DURATION: Int = 1
  const val MLN_ANIMATION_OPTION_VELOCITY: Int = 2
  const val MLN_ANIMATION_OPTION_MIN_ZOOM: Int = 4
  const val MLN_ANIMATION_OPTION_EASING: Int = 8
  const val MLN_ANIMATION_OPTION_TRANSITION_ID: Int = 16
}

/** Values of `enum mln_bound_option_field`. */
internal object MlnBoundOptionField {
  const val MLN_BOUND_OPTION_BOUNDS: Int = 1
  const val MLN_BOUND_OPTION_MIN_ZOOM: Int = 2
  const val MLN_BOUND_OPTION_MAX_ZOOM: Int = 4
  const val MLN_BOUND_OPTION_MIN_PITCH: Int = 8
  const val MLN_BOUND_OPTION_MAX_PITCH: Int = 16
  const val MLN_BOUND_OPTION_UNBOUNDED: Int = 32
}

/** Values of `enum mln_camera_change_mode`. */
internal object MlnCameraChangeMode {
  const val MLN_CAMERA_CHANGE_MODE_IMMEDIATE: Int = 0
  const val MLN_CAMERA_CHANGE_MODE_ANIMATED: Int = 1
}

/** Values of `enum mln_camera_fit_option_field`. */
internal object MlnCameraFitOptionField {
  const val MLN_CAMERA_FIT_OPTION_PADDING: Int = 1
  const val MLN_CAMERA_FIT_OPTION_BEARING: Int = 2
  const val MLN_CAMERA_FIT_OPTION_PITCH: Int = 4
}

/** Values of `enum mln_camera_option_field`. */
internal object MlnCameraOptionField {
  const val MLN_CAMERA_OPTION_CENTER: Int = 1
  const val MLN_CAMERA_OPTION_ZOOM: Int = 2
  const val MLN_CAMERA_OPTION_BEARING: Int = 4
  const val MLN_CAMERA_OPTION_PITCH: Int = 8
  const val MLN_CAMERA_OPTION_CENTER_ALTITUDE: Int = 16
  const val MLN_CAMERA_OPTION_PADDING: Int = 32
  const val MLN_CAMERA_OPTION_ANCHOR: Int = 64
  const val MLN_CAMERA_OPTION_ROLL: Int = 128
  const val MLN_CAMERA_OPTION_FOV: Int = 256
}

/** Values of `enum mln_constrain_mode`. */
internal object MlnConstrainMode {
  const val MLN_CONSTRAIN_MODE_NONE: Int = 0
  const val MLN_CONSTRAIN_MODE_HEIGHT_ONLY: Int = 1
  const val MLN_CONSTRAIN_MODE_WIDTH_AND_HEIGHT: Int = 2
  const val MLN_CONSTRAIN_MODE_SCREEN: Int = 3
}

/** Values of `enum mln_custom_geometry_source_option_field`. */
internal object MlnCustomGeometrySourceOptionField {
  const val MLN_CUSTOM_GEOMETRY_SOURCE_OPTION_MIN_ZOOM: Int = 1
  const val MLN_CUSTOM_GEOMETRY_SOURCE_OPTION_MAX_ZOOM: Int = 2
  const val MLN_CUSTOM_GEOMETRY_SOURCE_OPTION_TOLERANCE: Int = 4
  const val MLN_CUSTOM_GEOMETRY_SOURCE_OPTION_TILE_SIZE: Int = 8
  const val MLN_CUSTOM_GEOMETRY_SOURCE_OPTION_BUFFER: Int = 16
  const val MLN_CUSTOM_GEOMETRY_SOURCE_OPTION_CLIP: Int = 32
  const val MLN_CUSTOM_GEOMETRY_SOURCE_OPTION_WRAP: Int = 64
}

/** Values of `enum mln_feature_extension_result_type`. */
internal object MlnFeatureExtensionResultType {
  const val MLN_FEATURE_EXTENSION_RESULT_TYPE_VALUE: Int = 1
  const val MLN_FEATURE_EXTENSION_RESULT_TYPE_FEATURE_COLLECTION: Int = 2
}

/** Values of `enum mln_feature_identifier_type`. */
internal object MlnFeatureIdentifierType {
  const val MLN_FEATURE_IDENTIFIER_TYPE_NULL: Int = 0
  const val MLN_FEATURE_IDENTIFIER_TYPE_UINT: Int = 1
  const val MLN_FEATURE_IDENTIFIER_TYPE_INT: Int = 2
  const val MLN_FEATURE_IDENTIFIER_TYPE_DOUBLE: Int = 3
  const val MLN_FEATURE_IDENTIFIER_TYPE_STRING: Int = 4
}

/** Values of `enum mln_feature_state_selector_field`. */
internal object MlnFeatureStateSelectorField {
  const val MLN_FEATURE_STATE_SELECTOR_SOURCE_LAYER_ID: Int = 1
  const val MLN_FEATURE_STATE_SELECTOR_FEATURE_ID: Int = 2
  const val MLN_FEATURE_STATE_SELECTOR_STATE_KEY: Int = 4
}

/** Values of `enum mln_free_camera_option_field`. */
internal object MlnFreeCameraOptionField {
  const val MLN_FREE_CAMERA_OPTION_POSITION: Int = 1
  const val MLN_FREE_CAMERA_OPTION_ORIENTATION: Int = 2
}

/** Values of `enum mln_geojson_source_option_field`. */
internal object MlnGeojsonSourceOptionField {
  const val MLN_GEOJSON_SOURCE_OPTION_MIN_ZOOM: Int = 1
  const val MLN_GEOJSON_SOURCE_OPTION_MAX_ZOOM: Int = 2
  const val MLN_GEOJSON_SOURCE_OPTION_TOLERANCE: Int = 4
  const val MLN_GEOJSON_SOURCE_OPTION_CLUSTER_MAX_ZOOM: Int = 8
  const val MLN_GEOJSON_SOURCE_OPTION_CLUSTER_PROPERTIES: Int = 16
  const val MLN_GEOJSON_SOURCE_OPTION_TILE_SIZE: Int = 32
  const val MLN_GEOJSON_SOURCE_OPTION_BUFFER: Int = 64
  const val MLN_GEOJSON_SOURCE_OPTION_CLUSTER_RADIUS: Int = 128
  const val MLN_GEOJSON_SOURCE_OPTION_CLUSTER_MIN_POINTS: Int = 256
  const val MLN_GEOJSON_SOURCE_OPTION_LINE_METRICS: Int = 512
  const val MLN_GEOJSON_SOURCE_OPTION_CLUSTER: Int = 1024
  const val MLN_GEOJSON_SOURCE_OPTION_SYNCHRONOUS_UPDATE: Int = 2048
}

/** Values of `enum mln_geojson_type`. */
internal object MlnGeojsonType {
  const val MLN_GEOJSON_TYPE_GEOMETRY: Int = 1
  const val MLN_GEOJSON_TYPE_FEATURE: Int = 2
  const val MLN_GEOJSON_TYPE_FEATURE_COLLECTION: Int = 3
}

/** Values of `enum mln_geometry_type`. */
internal object MlnGeometryType {
  const val MLN_GEOMETRY_TYPE_EMPTY: Int = 0
  const val MLN_GEOMETRY_TYPE_POINT: Int = 1
  const val MLN_GEOMETRY_TYPE_LINE_STRING: Int = 2
  const val MLN_GEOMETRY_TYPE_POLYGON: Int = 3
  const val MLN_GEOMETRY_TYPE_MULTI_POINT: Int = 4
  const val MLN_GEOMETRY_TYPE_MULTI_LINE_STRING: Int = 5
  const val MLN_GEOMETRY_TYPE_MULTI_POLYGON: Int = 6
  const val MLN_GEOMETRY_TYPE_GEOMETRY_COLLECTION: Int = 7
}

/** Values of `enum mln_json_value_type`. */
internal object MlnJsonValueType {
  const val MLN_JSON_VALUE_TYPE_NULL: Int = 0
  const val MLN_JSON_VALUE_TYPE_BOOL: Int = 1
  const val MLN_JSON_VALUE_TYPE_UINT: Int = 2
  const val MLN_JSON_VALUE_TYPE_INT: Int = 3
  const val MLN_JSON_VALUE_TYPE_DOUBLE: Int = 4
  const val MLN_JSON_VALUE_TYPE_STRING: Int = 5
  const val MLN_JSON_VALUE_TYPE_ARRAY: Int = 6
  const val MLN_JSON_VALUE_TYPE_OBJECT: Int = 7
}

/** Values of `enum mln_location_indicator_image_kind`. */
internal object MlnLocationIndicatorImageKind {
  const val MLN_LOCATION_INDICATOR_IMAGE_KIND_TOP: Int = 0
  const val MLN_LOCATION_INDICATOR_IMAGE_KIND_BEARING: Int = 1
  const val MLN_LOCATION_INDICATOR_IMAGE_KIND_SHADOW: Int = 2
}

/** Values of `enum mln_log_event`. */
internal object MlnLogEvent {
  const val MLN_LOG_EVENT_GENERAL: Int = 0
  const val MLN_LOG_EVENT_SETUP: Int = 1
  const val MLN_LOG_EVENT_SHADER: Int = 2
  const val MLN_LOG_EVENT_PARSE_STYLE: Int = 3
  const val MLN_LOG_EVENT_PARSE_TILE: Int = 4
  const val MLN_LOG_EVENT_RENDER: Int = 5
  const val MLN_LOG_EVENT_STYLE: Int = 6
  const val MLN_LOG_EVENT_DATABASE: Int = 7
  const val MLN_LOG_EVENT_HTTP_REQUEST: Int = 8
  const val MLN_LOG_EVENT_SPRITE: Int = 9
  const val MLN_LOG_EVENT_IMAGE: Int = 10
  const val MLN_LOG_EVENT_OPENGL: Int = 11
  const val MLN_LOG_EVENT_JNI: Int = 12
  const val MLN_LOG_EVENT_ANDROID: Int = 13
  const val MLN_LOG_EVENT_CRASH: Int = 14
  const val MLN_LOG_EVENT_GLYPH: Int = 15
  const val MLN_LOG_EVENT_TIMING: Int = 16
}

/** Values of `enum mln_log_severity`. */
internal object MlnLogSeverity {
  const val MLN_LOG_SEVERITY_INFO: Int = 1
  const val MLN_LOG_SEVERITY_WARNING: Int = 2
  const val MLN_LOG_SEVERITY_ERROR: Int = 3
}

/** Values of `enum mln_log_severity_mask`. */
internal object MlnLogSeverityMask {
  const val MLN_LOG_SEVERITY_MASK_INFO: Int = 2
  const val MLN_LOG_SEVERITY_MASK_WARNING: Int = 4
  const val MLN_LOG_SEVERITY_MASK_DEFAULT: Int = 6
  const val MLN_LOG_SEVERITY_MASK_ERROR: Int = 8
  const val MLN_LOG_SEVERITY_MASK_ALL: Int = 14
}

/** Values of `enum mln_map_debug_option`. */
internal object MlnMapDebugOption {
  const val MLN_MAP_DEBUG_TILE_BORDERS: Int = 2
  const val MLN_MAP_DEBUG_PARSE_STATUS: Int = 4
  const val MLN_MAP_DEBUG_TIMESTAMPS: Int = 8
  const val MLN_MAP_DEBUG_COLLISION: Int = 16
  const val MLN_MAP_DEBUG_OVERDRAW: Int = 32
  const val MLN_MAP_DEBUG_STENCIL_CLIP: Int = 64
  const val MLN_MAP_DEBUG_DEPTH_BUFFER: Int = 128
}

/** Values of `enum mln_map_mode`. */
internal object MlnMapMode {
  const val MLN_MAP_MODE_CONTINUOUS: Int = 0
  const val MLN_MAP_MODE_STATIC: Int = 1
  const val MLN_MAP_MODE_TILE: Int = 2
}

/** Values of `enum mln_map_tile_option_field`. */
internal object MlnMapTileOptionField {
  const val MLN_MAP_TILE_OPTION_PREFETCH_ZOOM_DELTA: Int = 1
  const val MLN_MAP_TILE_OPTION_LOD_MIN_RADIUS: Int = 2
  const val MLN_MAP_TILE_OPTION_LOD_SCALE: Int = 4
  const val MLN_MAP_TILE_OPTION_LOD_PITCH_THRESHOLD: Int = 8
  const val MLN_MAP_TILE_OPTION_LOD_ZOOM_SHIFT: Int = 16
  const val MLN_MAP_TILE_OPTION_LOD_MODE: Int = 32
}

/** Values of `enum mln_map_viewport_option_field`. */
internal object MlnMapViewportOptionField {
  const val MLN_MAP_VIEWPORT_OPTION_NORTH_ORIENTATION: Int = 1
  const val MLN_MAP_VIEWPORT_OPTION_CONSTRAIN_MODE: Int = 2
  const val MLN_MAP_VIEWPORT_OPTION_VIEWPORT_MODE: Int = 4
  const val MLN_MAP_VIEWPORT_OPTION_FRUSTUM_OFFSET: Int = 8
}

/** Values of `enum mln_network_status`. */
internal object MlnNetworkStatus {
  const val MLN_NETWORK_STATUS_ONLINE: Int = 1
  const val MLN_NETWORK_STATUS_OFFLINE: Int = 2
}

/** Values of `enum mln_north_orientation`. */
internal object MlnNorthOrientation {
  const val MLN_NORTH_ORIENTATION_UP: Int = 0
  const val MLN_NORTH_ORIENTATION_RIGHT: Int = 1
  const val MLN_NORTH_ORIENTATION_DOWN: Int = 2
  const val MLN_NORTH_ORIENTATION_LEFT: Int = 3
}

/** Values of `enum mln_offline_operation_kind`. */
internal object MlnOfflineOperationKind {
  const val MLN_OFFLINE_OPERATION_AMBIENT_CACHE: Int = 1
  const val MLN_OFFLINE_OPERATION_REGION_CREATE: Int = 2
  const val MLN_OFFLINE_OPERATION_REGION_GET: Int = 3
  const val MLN_OFFLINE_OPERATION_REGIONS_LIST: Int = 4
  const val MLN_OFFLINE_OPERATION_REGIONS_MERGE_DATABASE: Int = 5
  const val MLN_OFFLINE_OPERATION_REGION_UPDATE_METADATA: Int = 6
  const val MLN_OFFLINE_OPERATION_REGION_GET_STATUS: Int = 7
  const val MLN_OFFLINE_OPERATION_REGION_SET_OBSERVED: Int = 8
  const val MLN_OFFLINE_OPERATION_REGION_SET_DOWNLOAD_STATE: Int = 9
  const val MLN_OFFLINE_OPERATION_REGION_INVALIDATE: Int = 10
  const val MLN_OFFLINE_OPERATION_REGION_DELETE: Int = 11
  const val MLN_OFFLINE_OPERATION_SET_MAXIMUM_AMBIENT_CACHE_SIZE: Int = 12
}

/** Values of `enum mln_offline_operation_result_kind`. */
internal object MlnOfflineOperationResultKind {
  const val MLN_OFFLINE_OPERATION_RESULT_NONE: Int = 0
  const val MLN_OFFLINE_OPERATION_RESULT_REGION: Int = 1
  const val MLN_OFFLINE_OPERATION_RESULT_OPTIONAL_REGION: Int = 2
  const val MLN_OFFLINE_OPERATION_RESULT_REGION_LIST: Int = 3
  const val MLN_OFFLINE_OPERATION_RESULT_REGION_STATUS: Int = 4
}

/** Values of `enum mln_offline_region_definition_type`. */
internal object MlnOfflineRegionDefinitionType {
  const val MLN_OFFLINE_REGION_DEFINITION_TILE_PYRAMID: Int = 1
  const val MLN_OFFLINE_REGION_DEFINITION_GEOMETRY: Int = 2
}

/** Values of `enum mln_offline_region_download_state`. */
internal object MlnOfflineRegionDownloadState {
  const val MLN_OFFLINE_REGION_DOWNLOAD_INACTIVE: Int = 0
  const val MLN_OFFLINE_REGION_DOWNLOAD_ACTIVE: Int = 1
}

/** Values of `enum mln_opengl_context_platform`. */
internal object MlnOpenglContextPlatform {
  const val MLN_OPENGL_CONTEXT_PLATFORM_UNSPECIFIED: Int = 0
  const val MLN_OPENGL_CONTEXT_PLATFORM_WGL: Int = 1
  const val MLN_OPENGL_CONTEXT_PLATFORM_EGL: Int = 2
  const val MLN_OPENGL_CONTEXT_PLATFORM_WEBGL: Int = 3
}

/** Values of `enum mln_opengl_context_provider_flag`. */
internal object MlnOpenglContextProviderFlag {
  const val MLN_OPENGL_CONTEXT_PROVIDER_FLAG_WGL: Int = 1
  const val MLN_OPENGL_CONTEXT_PROVIDER_FLAG_EGL: Int = 2
  const val MLN_OPENGL_CONTEXT_PROVIDER_FLAG_WEBGL: Int = 4
}

/** Values of `enum mln_projection_mode_field`. */
internal object MlnProjectionModeField {
  const val MLN_PROJECTION_MODE_AXONOMETRIC: Int = 1
  const val MLN_PROJECTION_MODE_X_SKEW: Int = 2
  const val MLN_PROJECTION_MODE_Y_SKEW: Int = 4
}

/** Values of `enum mln_queried_feature_field`. */
internal object MlnQueriedFeatureField {
  const val MLN_QUERIED_FEATURE_SOURCE_ID: Int = 1
  const val MLN_QUERIED_FEATURE_SOURCE_LAYER_ID: Int = 2
  const val MLN_QUERIED_FEATURE_STATE: Int = 4
}

/** Values of `enum mln_render_backend_flag`. */
internal object MlnRenderBackendFlag {
  const val MLN_RENDER_BACKEND_FLAG_METAL: Int = 1
  const val MLN_RENDER_BACKEND_FLAG_VULKAN: Int = 2
  const val MLN_RENDER_BACKEND_FLAG_OPENGL: Int = 4
  const val MLN_RENDER_BACKEND_FLAG_WEBGPU: Int = 8
}

/** Values of `enum mln_render_mode`. */
internal object MlnRenderMode {
  const val MLN_RENDER_MODE_PARTIAL: Int = 0
  const val MLN_RENDER_MODE_FULL: Int = 1
}

/** Values of `enum mln_rendered_feature_query_option_field`. */
internal object MlnRenderedFeatureQueryOptionField {
  const val MLN_RENDERED_FEATURE_QUERY_OPTION_LAYER_IDS: Int = 1
}

/** Values of `enum mln_rendered_query_geometry_type`. */
internal object MlnRenderedQueryGeometryType {
  const val MLN_RENDERED_QUERY_GEOMETRY_TYPE_POINT: Int = 1
  const val MLN_RENDERED_QUERY_GEOMETRY_TYPE_BOX: Int = 2
  const val MLN_RENDERED_QUERY_GEOMETRY_TYPE_LINE_STRING: Int = 3
}

/** Values of `enum mln_resource_error_reason`. */
internal object MlnResourceErrorReason {
  const val MLN_RESOURCE_ERROR_REASON_NONE: Int = 0
  const val MLN_RESOURCE_ERROR_REASON_NOT_FOUND: Int = 1
  const val MLN_RESOURCE_ERROR_REASON_SERVER: Int = 2
  const val MLN_RESOURCE_ERROR_REASON_CONNECTION: Int = 3
  const val MLN_RESOURCE_ERROR_REASON_RATE_LIMIT: Int = 4
  const val MLN_RESOURCE_ERROR_REASON_OTHER: Int = 5
}

/** Values of `enum mln_resource_kind`. */
internal object MlnResourceKind {
  const val MLN_RESOURCE_KIND_UNKNOWN: Int = 0
  const val MLN_RESOURCE_KIND_STYLE: Int = 1
  const val MLN_RESOURCE_KIND_SOURCE: Int = 2
  const val MLN_RESOURCE_KIND_TILE: Int = 3
  const val MLN_RESOURCE_KIND_GLYPHS: Int = 4
  const val MLN_RESOURCE_KIND_SPRITE_IMAGE: Int = 5
  const val MLN_RESOURCE_KIND_SPRITE_JSON: Int = 6
  const val MLN_RESOURCE_KIND_IMAGE: Int = 7
}

/** Values of `enum mln_resource_loading_method`. */
internal object MlnResourceLoadingMethod {
  const val MLN_RESOURCE_LOADING_METHOD_ALL: Int = 0
  const val MLN_RESOURCE_LOADING_METHOD_CACHE_ONLY: Int = 1
  const val MLN_RESOURCE_LOADING_METHOD_NETWORK_ONLY: Int = 2
}

/** Values of `enum mln_resource_priority`. */
internal object MlnResourcePriority {
  const val MLN_RESOURCE_PRIORITY_REGULAR: Int = 0
  const val MLN_RESOURCE_PRIORITY_LOW: Int = 1
}

/** Values of `enum mln_resource_provider_decision`. */
internal object MlnResourceProviderDecision {
  const val MLN_RESOURCE_PROVIDER_DECISION_PASS_THROUGH: Int = 0
  const val MLN_RESOURCE_PROVIDER_DECISION_HANDLE: Int = 1
}

/** Values of `enum mln_resource_response_status`. */
internal object MlnResourceResponseStatus {
  const val MLN_RESOURCE_RESPONSE_STATUS_OK: Int = 0
  const val MLN_RESOURCE_RESPONSE_STATUS_ERROR: Int = 1
  const val MLN_RESOURCE_RESPONSE_STATUS_NO_CONTENT: Int = 2
  const val MLN_RESOURCE_RESPONSE_STATUS_NOT_MODIFIED: Int = 3
}

/** Values of `enum mln_resource_storage_policy`. */
internal object MlnResourceStoragePolicy {
  const val MLN_RESOURCE_STORAGE_POLICY_PERMANENT: Int = 0
  const val MLN_RESOURCE_STORAGE_POLICY_VOLATILE: Int = 1
}

/** Values of `enum mln_resource_usage`. */
internal object MlnResourceUsage {
  const val MLN_RESOURCE_USAGE_ONLINE: Int = 0
  const val MLN_RESOURCE_USAGE_OFFLINE: Int = 1
}

/** Values of `enum mln_runtime_event_payload_type`. */
internal object MlnRuntimeEventPayloadType {
  const val MLN_RUNTIME_EVENT_PAYLOAD_NONE: Int = 0
  const val MLN_RUNTIME_EVENT_PAYLOAD_RENDER_FRAME: Int = 1
  const val MLN_RUNTIME_EVENT_PAYLOAD_RENDER_MAP: Int = 2
  const val MLN_RUNTIME_EVENT_PAYLOAD_STYLE_IMAGE_MISSING: Int = 3
  const val MLN_RUNTIME_EVENT_PAYLOAD_TILE_ACTION: Int = 4
  const val MLN_RUNTIME_EVENT_PAYLOAD_OFFLINE_REGION_STATUS: Int = 5
  const val MLN_RUNTIME_EVENT_PAYLOAD_OFFLINE_REGION_RESPONSE_ERROR: Int = 6
  const val MLN_RUNTIME_EVENT_PAYLOAD_OFFLINE_REGION_TILE_COUNT_LIMIT: Int = 7
  const val MLN_RUNTIME_EVENT_PAYLOAD_OFFLINE_OPERATION_COMPLETED: Int = 8
  const val MLN_RUNTIME_EVENT_PAYLOAD_CAMERA_TRANSITION_FINISHED: Int = 9
}

/** Values of `enum mln_runtime_event_source_type`. */
internal object MlnRuntimeEventSourceType {
  const val MLN_RUNTIME_EVENT_SOURCE_RUNTIME: Int = 0
  const val MLN_RUNTIME_EVENT_SOURCE_MAP: Int = 1
}

/** Values of `enum mln_runtime_event_type`. */
internal object MlnRuntimeEventType {
  const val MLN_RUNTIME_EVENT_MAP_CAMERA_WILL_CHANGE: Int = 1
  const val MLN_RUNTIME_EVENT_MAP_CAMERA_IS_CHANGING: Int = 2
  const val MLN_RUNTIME_EVENT_MAP_CAMERA_DID_CHANGE: Int = 3
  const val MLN_RUNTIME_EVENT_MAP_STYLE_LOADED: Int = 4
  const val MLN_RUNTIME_EVENT_MAP_LOADING_STARTED: Int = 5
  const val MLN_RUNTIME_EVENT_MAP_LOADING_FINISHED: Int = 6
  const val MLN_RUNTIME_EVENT_MAP_LOADING_FAILED: Int = 7
  const val MLN_RUNTIME_EVENT_MAP_IDLE: Int = 8
  const val MLN_RUNTIME_EVENT_MAP_RENDER_UPDATE_AVAILABLE: Int = 9
  const val MLN_RUNTIME_EVENT_MAP_RENDER_ERROR: Int = 10
  const val MLN_RUNTIME_EVENT_MAP_STILL_IMAGE_FINISHED: Int = 11
  const val MLN_RUNTIME_EVENT_MAP_STILL_IMAGE_FAILED: Int = 12
  const val MLN_RUNTIME_EVENT_MAP_RENDER_FRAME_STARTED: Int = 13
  const val MLN_RUNTIME_EVENT_MAP_RENDER_FRAME_FINISHED: Int = 14
  const val MLN_RUNTIME_EVENT_MAP_RENDER_MAP_STARTED: Int = 15
  const val MLN_RUNTIME_EVENT_MAP_RENDER_MAP_FINISHED: Int = 16
  const val MLN_RUNTIME_EVENT_MAP_STYLE_IMAGE_MISSING: Int = 17
  const val MLN_RUNTIME_EVENT_MAP_TILE_ACTION: Int = 18
  const val MLN_RUNTIME_EVENT_OFFLINE_REGION_STATUS_CHANGED: Int = 19
  const val MLN_RUNTIME_EVENT_OFFLINE_REGION_RESPONSE_ERROR: Int = 20
  const val MLN_RUNTIME_EVENT_OFFLINE_REGION_TILE_COUNT_LIMIT_EXCEEDED: Int = 21
  const val MLN_RUNTIME_EVENT_OFFLINE_OPERATION_COMPLETED: Int = 22
  const val MLN_RUNTIME_EVENT_MAP_CAMERA_TRANSITION_FINISHED: Int = 23
}

/** Values of `enum mln_source_feature_query_option_field`. */
internal object MlnSourceFeatureQueryOptionField {
  const val MLN_SOURCE_FEATURE_QUERY_OPTION_SOURCE_LAYER_IDS: Int = 1
}

/** Values of `enum mln_status`. */
internal object MlnStatus {
  const val MLN_STATUS_NATIVE_ERROR: Int = -5
  const val MLN_STATUS_UNSUPPORTED: Int = -4
  const val MLN_STATUS_WRONG_THREAD: Int = -3
  const val MLN_STATUS_INVALID_STATE: Int = -2
  const val MLN_STATUS_INVALID_ARGUMENT: Int = -1
  const val MLN_STATUS_OK: Int = 0
}

/** Values of `enum mln_style_image_option_field`. */
internal object MlnStyleImageOptionField {
  const val MLN_STYLE_IMAGE_OPTION_PIXEL_RATIO: Int = 1
  const val MLN_STYLE_IMAGE_OPTION_SDF: Int = 2
  const val MLN_STYLE_IMAGE_OPTION_STRETCH_X: Int = 4
  const val MLN_STYLE_IMAGE_OPTION_STRETCH_Y: Int = 8
  const val MLN_STYLE_IMAGE_OPTION_CONTENT: Int = 16
  const val MLN_STYLE_IMAGE_OPTION_TEXT_FIT_WIDTH: Int = 32
  const val MLN_STYLE_IMAGE_OPTION_TEXT_FIT_HEIGHT: Int = 64
}

/** Values of `enum mln_style_image_text_fit`. */
internal object MlnStyleImageTextFit {
  const val MLN_STYLE_IMAGE_TEXT_FIT_STRETCH_OR_SHRINK: Int = 0
  const val MLN_STYLE_IMAGE_TEXT_FIT_STRETCH_ONLY: Int = 1
  const val MLN_STYLE_IMAGE_TEXT_FIT_PROPORTIONAL: Int = 2
}

/** Values of `enum mln_style_layer_visibility`. */
internal object MlnStyleLayerVisibility {
  const val MLN_STYLE_LAYER_VISIBILITY_VISIBLE: Int = 0
  const val MLN_STYLE_LAYER_VISIBILITY_NONE: Int = 1
}

/** Values of `enum mln_style_raster_dem_encoding`. */
internal object MlnStyleRasterDemEncoding {
  const val MLN_STYLE_RASTER_DEM_ENCODING_MAPBOX: Int = 0
  const val MLN_STYLE_RASTER_DEM_ENCODING_TERRARIUM: Int = 1
}

/** Values of `enum mln_style_source_type`. */
internal object MlnStyleSourceType {
  const val MLN_STYLE_SOURCE_TYPE_UNKNOWN: Int = 0
  const val MLN_STYLE_SOURCE_TYPE_VECTOR: Int = 1
  const val MLN_STYLE_SOURCE_TYPE_RASTER: Int = 2
  const val MLN_STYLE_SOURCE_TYPE_RASTER_DEM: Int = 3
  const val MLN_STYLE_SOURCE_TYPE_GEOJSON: Int = 4
  const val MLN_STYLE_SOURCE_TYPE_IMAGE: Int = 5
  const val MLN_STYLE_SOURCE_TYPE_VIDEO: Int = 6
  const val MLN_STYLE_SOURCE_TYPE_ANNOTATIONS: Int = 7
  const val MLN_STYLE_SOURCE_TYPE_CUSTOM_VECTOR: Int = 8
}

/** Values of `enum mln_style_tile_scheme`. */
internal object MlnStyleTileScheme {
  const val MLN_STYLE_TILE_SCHEME_XYZ: Int = 0
  const val MLN_STYLE_TILE_SCHEME_TMS: Int = 1
}

/** Values of `enum mln_style_tile_source_option_field`. */
internal object MlnStyleTileSourceOptionField {
  const val MLN_STYLE_TILE_SOURCE_OPTION_MIN_ZOOM: Int = 1
  const val MLN_STYLE_TILE_SOURCE_OPTION_MAX_ZOOM: Int = 2
  const val MLN_STYLE_TILE_SOURCE_OPTION_ATTRIBUTION: Int = 4
  const val MLN_STYLE_TILE_SOURCE_OPTION_SCHEME: Int = 8
  const val MLN_STYLE_TILE_SOURCE_OPTION_BOUNDS: Int = 16
  const val MLN_STYLE_TILE_SOURCE_OPTION_TILE_SIZE: Int = 32
  const val MLN_STYLE_TILE_SOURCE_OPTION_VECTOR_ENCODING: Int = 64
  const val MLN_STYLE_TILE_SOURCE_OPTION_RASTER_ENCODING: Int = 128
}

/** Values of `enum mln_style_transition_option_field`. */
internal object MlnStyleTransitionOptionField {
  const val MLN_STYLE_TRANSITION_OPTION_DURATION: Int = 1
  const val MLN_STYLE_TRANSITION_OPTION_DELAY: Int = 2
  const val MLN_STYLE_TRANSITION_OPTION_ENABLE_PLACEMENT_TRANSITIONS: Int = 4
}

/** Values of `enum mln_style_vector_tile_encoding`. */
internal object MlnStyleVectorTileEncoding {
  const val MLN_STYLE_VECTOR_TILE_ENCODING_MVT: Int = 0
  const val MLN_STYLE_VECTOR_TILE_ENCODING_MLT: Int = 1
}

/** Values of `enum mln_tile_lod_mode`. */
internal object MlnTileLodMode {
  const val MLN_TILE_LOD_MODE_DEFAULT: Int = 0
  const val MLN_TILE_LOD_MODE_DISTANCE: Int = 1
}

/** Values of `enum mln_tile_operation`. */
internal object MlnTileOperation {
  const val MLN_TILE_OPERATION_REQUESTED_FROM_CACHE: Int = 0
  const val MLN_TILE_OPERATION_REQUESTED_FROM_NETWORK: Int = 1
  const val MLN_TILE_OPERATION_LOAD_FROM_NETWORK: Int = 2
  const val MLN_TILE_OPERATION_LOAD_FROM_CACHE: Int = 3
  const val MLN_TILE_OPERATION_START_PARSE: Int = 4
  const val MLN_TILE_OPERATION_END_PARSE: Int = 5
  const val MLN_TILE_OPERATION_ERROR: Int = 6
  const val MLN_TILE_OPERATION_CANCELLED: Int = 7
  const val MLN_TILE_OPERATION_NULL: Int = 8
}

/** Values of `enum mln_viewport_mode`. */
internal object MlnViewportMode {
  const val MLN_VIEWPORT_MODE_DEFAULT: Int = 0
  const val MLN_VIEWPORT_MODE_FLIPPED_Y: Int = 1
}

/** Fields of `struct mln_adapter_http_header`. */
internal object MlnAdapterHttpHeader {
  const val SIZEOF: Int = 8

  fun name(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 0))

  fun setName(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 0, value.address)
  }

  fun value(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 4))

  fun setValue(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 4, value.address)
  }
}

/** Fields of `struct mln_adapter_http_header_transform_rule`. */
internal object MlnAdapterHttpHeaderTransformRule {
  const val SIZEOF: Int = 20

  fun kind(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setKind(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun flags(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFlags(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun url(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setUrl(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }

  fun headers(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 12))

  fun setHeaders(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 12, value.address)
  }

  fun headerCount(base: HeapPointer): Int = Heap.loadInt(base + 16)

  fun setHeaderCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 16, value)
  }
}

/** Fields of `struct mln_adapter_http_header_transform_rules`. */
internal object MlnAdapterHttpHeaderTransformRules {
  const val SIZEOF: Int = 8

  fun rules(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 0))

  fun setRules(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 0, value.address)
  }

  fun count(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }
}

/** Fields of `struct mln_adapter_log_callback_state`. */
internal object MlnAdapterLogCallbackState {
  const val SIZEOF: Int = 8
  const val OFFSET_LISTENER: Int = 0

  fun consume(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setConsume(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }
}

/** Fields of `struct mln_adapter_log_record`. */
internal object MlnAdapterLogRecord {
  const val SIZEOF: Int = 32

  fun owner(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 0))

  fun setOwner(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 0, value.address)
  }

  fun retireCallback(base: HeapPointer): Boolean = Heap.loadByte(base + 4) != 0.toByte()

  fun setRetireCallback(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 4, if (value) 1 else 0)
  }

  fun severity(base: HeapPointer): Int = Heap.loadInt(base + 8)

  fun setSeverity(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 8, value)
  }

  fun event(base: HeapPointer): Int = Heap.loadInt(base + 12)

  fun setEvent(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 12, value)
  }

  fun code(base: HeapPointer): Long = Heap.loadLong(base + 16)

  fun setCode(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 16, value)
  }

  fun message(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 24))

  fun setMessage(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 24, value.address)
  }
}

/** Fields of `struct mln_adapter_queued_resource_provider`. */
internal object MlnAdapterQueuedResourceProvider {
  const val SIZEOF: Int = 12

  fun routes(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 0))

  fun setRoutes(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 0, value.address)
  }

  fun routeCount(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setRouteCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  const val OFFSET_LISTENER: Int = 8
}

/** Fields of `struct mln_adapter_queued_resource_provider_route`. */
internal object MlnAdapterQueuedResourceProviderRoute {
  const val SIZEOF: Int = 12

  fun kind(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setKind(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun flags(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFlags(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun url(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setUrl(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }
}

/** Fields of `struct mln_adapter_queued_resource_request`. */
internal object MlnAdapterQueuedResourceRequest {
  const val SIZEOF: Int = 112

  fun owner(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 0))

  fun setOwner(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 0, value.address)
  }

  fun handle(base: HeapPointer): Long = Heap.loadLong(base + 8)

  fun setHandle(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 8, value)
  }

  fun requestedUrl(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 16))

  fun setRequestedUrl(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 16, value.address)
  }

  fun resolvedUrl(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 20))

  fun setResolvedUrl(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 20, value.address)
  }

  fun kind(base: HeapPointer): Int = Heap.loadInt(base + 24)

  fun setKind(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 24, value)
  }

  fun loadingMethod(base: HeapPointer): Int = Heap.loadInt(base + 28)

  fun setLoadingMethod(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 28, value)
  }

  fun priority(base: HeapPointer): Int = Heap.loadInt(base + 32)

  fun setPriority(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 32, value)
  }

  fun usage(base: HeapPointer): Int = Heap.loadInt(base + 36)

  fun setUsage(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 36, value)
  }

  fun storagePolicy(base: HeapPointer): Int = Heap.loadInt(base + 40)

  fun setStoragePolicy(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 40, value)
  }

  fun hasRange(base: HeapPointer): Boolean = Heap.loadByte(base + 44) != 0.toByte()

  fun setHasRange(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 44, if (value) 1 else 0)
  }

  fun rangeStart(base: HeapPointer): Long = Heap.loadLong(base + 48)

  fun setRangeStart(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 48, value)
  }

  fun rangeEnd(base: HeapPointer): Long = Heap.loadLong(base + 56)

  fun setRangeEnd(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 56, value)
  }

  fun hasPriorModified(base: HeapPointer): Boolean = Heap.loadByte(base + 64) != 0.toByte()

  fun setHasPriorModified(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 64, if (value) 1 else 0)
  }

  fun priorModifiedUnixMs(base: HeapPointer): Long = Heap.loadLong(base + 72)

  fun setPriorModifiedUnixMs(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 72, value)
  }

  fun hasPriorExpires(base: HeapPointer): Boolean = Heap.loadByte(base + 80) != 0.toByte()

  fun setHasPriorExpires(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 80, if (value) 1 else 0)
  }

  fun priorExpiresUnixMs(base: HeapPointer): Long = Heap.loadLong(base + 88)

  fun setPriorExpiresUnixMs(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 88, value)
  }

  fun priorEtag(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 96))

  fun setPriorEtag(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 96, value.address)
  }

  fun priorData(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 100))

  fun setPriorData(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 100, value.address)
  }

  fun priorDataSize(base: HeapPointer): Int = Heap.loadInt(base + 104)

  fun setPriorDataSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 104, value)
  }
}

/** Fields of `struct mln_adapter_resource_provider_rule`. */
internal object MlnAdapterResourceProviderRule {
  const val SIZEOF: Int = 80

  fun kind(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setKind(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun requestedUrl(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 4))

  fun setRequestedUrl(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 4, value.address)
  }

  const val OFFSET_RESPONSE: Int = 8
}

/** Fields of `struct mln_adapter_resource_provider_rules`. */
internal object MlnAdapterResourceProviderRules {
  const val SIZEOF: Int = 8

  fun rules(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 0))

  fun setRules(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 0, value.address)
  }

  fun count(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }
}

/** Fields of `struct mln_adapter_resource_rewrite_rule`. */
internal object MlnAdapterResourceRewriteRule {
  const val SIZEOF: Int = 12

  fun kind(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setKind(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun url(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 4))

  fun setUrl(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 4, value.address)
  }

  fun replacementUrl(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setReplacementUrl(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }
}

/** Fields of `struct mln_adapter_resource_rewrite_rules`. */
internal object MlnAdapterResourceRewriteRules {
  const val SIZEOF: Int = 8

  fun rules(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 0))

  fun setRules(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 0, value.address)
  }

  fun count(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }
}

/** Fields of `struct mln_animation_options`. */
internal object MlnAnimationOptions {
  const val SIZEOF: Int = 72

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun fields(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFields(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun durationMs(base: HeapPointer): Double = Heap.loadDouble(base + 8)

  fun setDurationMs(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 8, value)
  }

  fun velocity(base: HeapPointer): Double = Heap.loadDouble(base + 16)

  fun setVelocity(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 16, value)
  }

  fun minZoom(base: HeapPointer): Double = Heap.loadDouble(base + 24)

  fun setMinZoom(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 24, value)
  }

  const val OFFSET_EASING: Int = 32

  fun transitionId(base: HeapPointer): Long = Heap.loadLong(base + 64)

  fun setTransitionId(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 64, value)
  }
}

/** Fields of `struct mln_bound_options`. */
internal object MlnBoundOptions {
  const val SIZEOF: Int = 72

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun fields(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFields(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  const val OFFSET_BOUNDS: Int = 8

  fun minZoom(base: HeapPointer): Double = Heap.loadDouble(base + 40)

  fun setMinZoom(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 40, value)
  }

  fun maxZoom(base: HeapPointer): Double = Heap.loadDouble(base + 48)

  fun setMaxZoom(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 48, value)
  }

  fun minPitch(base: HeapPointer): Double = Heap.loadDouble(base + 56)

  fun setMinPitch(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 56, value)
  }

  fun maxPitch(base: HeapPointer): Double = Heap.loadDouble(base + 64)

  fun setMaxPitch(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 64, value)
  }
}

/** Fields of `struct mln_camera_fit_options`. */
internal object MlnCameraFitOptions {
  const val SIZEOF: Int = 56

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun fields(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFields(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  const val OFFSET_PADDING: Int = 8

  fun bearing(base: HeapPointer): Double = Heap.loadDouble(base + 40)

  fun setBearing(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 40, value)
  }

  fun pitch(base: HeapPointer): Double = Heap.loadDouble(base + 48)

  fun setPitch(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 48, value)
  }
}

/** Fields of `struct mln_camera_options`. */
internal object MlnCameraOptions {
  const val SIZEOF: Int = 120

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun fields(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFields(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun latitude(base: HeapPointer): Double = Heap.loadDouble(base + 8)

  fun setLatitude(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 8, value)
  }

  fun longitude(base: HeapPointer): Double = Heap.loadDouble(base + 16)

  fun setLongitude(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 16, value)
  }

  fun centerAltitude(base: HeapPointer): Double = Heap.loadDouble(base + 24)

  fun setCenterAltitude(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 24, value)
  }

  const val OFFSET_PADDING: Int = 32
  const val OFFSET_ANCHOR: Int = 64

  fun zoom(base: HeapPointer): Double = Heap.loadDouble(base + 80)

  fun setZoom(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 80, value)
  }

  fun bearing(base: HeapPointer): Double = Heap.loadDouble(base + 88)

  fun setBearing(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 88, value)
  }

  fun pitch(base: HeapPointer): Double = Heap.loadDouble(base + 96)

  fun setPitch(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 96, value)
  }

  fun roll(base: HeapPointer): Double = Heap.loadDouble(base + 104)

  fun setRoll(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 104, value)
  }

  fun fieldOfView(base: HeapPointer): Double = Heap.loadDouble(base + 112)

  fun setFieldOfView(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 112, value)
  }
}

/** Fields of `struct mln_canonical_tile_id`. */
internal object MlnCanonicalTileId {
  const val SIZEOF: Int = 12

  fun z(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setZ(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun x(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setX(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun y(base: HeapPointer): Int = Heap.loadInt(base + 8)

  fun setY(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 8, value)
  }
}

/** Fields of `struct mln_coordinate_span`. */
internal object MlnCoordinateSpan {
  const val SIZEOF: Int = 8

  fun coordinates(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 0))

  fun setCoordinates(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 0, value.address)
  }

  fun coordinateCount(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setCoordinateCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }
}

/** Fields of `struct mln_custom_geometry_source_options`. */
internal object MlnCustomGeometrySourceOptions {
  const val SIZEOF: Int = 64

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun fields(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFields(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  const val OFFSET_FETCH_TILE: Int = 8
  const val OFFSET_CANCEL_TILE: Int = 12

  fun userData(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 16))

  fun setUserData(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 16, value.address)
  }

  fun minZoom(base: HeapPointer): Double = Heap.loadDouble(base + 24)

  fun setMinZoom(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 24, value)
  }

  fun maxZoom(base: HeapPointer): Double = Heap.loadDouble(base + 32)

  fun setMaxZoom(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 32, value)
  }

  fun tolerance(base: HeapPointer): Double = Heap.loadDouble(base + 40)

  fun setTolerance(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 40, value)
  }

  fun tileSize(base: HeapPointer): Int = Heap.loadInt(base + 48)

  fun setTileSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 48, value)
  }

  fun buffer(base: HeapPointer): Int = Heap.loadInt(base + 52)

  fun setBuffer(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 52, value)
  }

  fun clip(base: HeapPointer): Boolean = Heap.loadByte(base + 56) != 0.toByte()

  fun setClip(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 56, if (value) 1 else 0)
  }

  fun wrap(base: HeapPointer): Boolean = Heap.loadByte(base + 57) != 0.toByte()

  fun setWrap(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 57, if (value) 1 else 0)
  }
}

/** Fields of `struct mln_edge_insets`. */
internal object MlnEdgeInsets {
  const val SIZEOF: Int = 32

  fun top(base: HeapPointer): Double = Heap.loadDouble(base + 0)

  fun setTop(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 0, value)
  }

  fun left(base: HeapPointer): Double = Heap.loadDouble(base + 8)

  fun setLeft(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 8, value)
  }

  fun bottom(base: HeapPointer): Double = Heap.loadDouble(base + 16)

  fun setBottom(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 16, value)
  }

  fun right(base: HeapPointer): Double = Heap.loadDouble(base + 24)

  fun setRight(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 24, value)
  }
}

/** Fields of `struct mln_egl_context_descriptor`. */
internal object MlnEglContextDescriptor {
  const val SIZEOF: Int = 20

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun display(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 4))

  fun setDisplay(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 4, value.address)
  }

  fun config(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setConfig(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }

  fun shareContext(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 12))

  fun setShareContext(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 12, value.address)
  }

  fun getProcAddress(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 16))

  fun setGetProcAddress(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 16, value.address)
  }
}

/** Fields of `struct mln_feature`. */
internal object MlnFeature {
  const val SIZEOF: Int = 32

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun geometry(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 4))

  fun setGeometry(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 4, value.address)
  }

  fun properties(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setProperties(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }

  fun propertyCount(base: HeapPointer): Int = Heap.loadInt(base + 12)

  fun setPropertyCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 12, value)
  }

  fun identifierType(base: HeapPointer): Int = Heap.loadInt(base + 16)

  fun setIdentifierType(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 16, value)
  }

  const val OFFSET_IDENTIFIER: Int = 24
}

/** Fields of `struct mln_feature_collection`. */
internal object MlnFeatureCollection {
  const val SIZEOF: Int = 8

  fun features(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 0))

  fun setFeatures(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 0, value.address)
  }

  fun featureCount(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFeatureCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }
}

/** Fields of `struct mln_feature_extension_result_info`. */
internal object MlnFeatureExtensionResultInfo {
  const val SIZEOF: Int = 16

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun type(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setType(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  const val OFFSET_DATA: Int = 8
}

/** Fields of `struct mln_feature_state_selector`. */
internal object MlnFeatureStateSelector {
  const val SIZEOF: Int = 40

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun fields(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFields(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  const val OFFSET_SOURCE_ID: Int = 8
  const val OFFSET_SOURCE_LAYER_ID: Int = 16
  const val OFFSET_FEATURE_ID: Int = 24
  const val OFFSET_STATE_KEY: Int = 32
}

/** Fields of `struct mln_free_camera_options`. */
internal object MlnFreeCameraOptions {
  const val SIZEOF: Int = 64

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun fields(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFields(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  const val OFFSET_POSITION: Int = 8
  const val OFFSET_ORIENTATION: Int = 32
}

/** Fields of `struct mln_geojson`. */
internal object MlnGeojson {
  const val SIZEOF: Int = 16

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun type(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setType(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  const val OFFSET_DATA: Int = 8
}

/** Fields of `struct mln_geojson_source_options`. */
internal object MlnGeojsonSourceOptions {
  const val SIZEOF: Int = 64

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun fields(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFields(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun minZoom(base: HeapPointer): Double = Heap.loadDouble(base + 8)

  fun setMinZoom(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 8, value)
  }

  fun maxZoom(base: HeapPointer): Double = Heap.loadDouble(base + 16)

  fun setMaxZoom(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 16, value)
  }

  fun tolerance(base: HeapPointer): Double = Heap.loadDouble(base + 24)

  fun setTolerance(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 24, value)
  }

  fun clusterMaxZoom(base: HeapPointer): Double = Heap.loadDouble(base + 32)

  fun setClusterMaxZoom(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 32, value)
  }

  fun clusterProperties(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 40))

  fun setClusterProperties(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 40, value.address)
  }

  fun tileSize(base: HeapPointer): Int = Heap.loadInt(base + 44)

  fun setTileSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 44, value)
  }

  fun buffer(base: HeapPointer): Int = Heap.loadInt(base + 48)

  fun setBuffer(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 48, value)
  }

  fun clusterRadius(base: HeapPointer): Int = Heap.loadInt(base + 52)

  fun setClusterRadius(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 52, value)
  }

  fun clusterMinPoints(base: HeapPointer): Int = Heap.loadInt(base + 56)

  fun setClusterMinPoints(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 56, value)
  }

  fun lineMetrics(base: HeapPointer): Boolean = Heap.loadByte(base + 60) != 0.toByte()

  fun setLineMetrics(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 60, if (value) 1 else 0)
  }

  fun cluster(base: HeapPointer): Boolean = Heap.loadByte(base + 61) != 0.toByte()

  fun setCluster(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 61, if (value) 1 else 0)
  }

  fun synchronousUpdate(base: HeapPointer): Boolean = Heap.loadByte(base + 62) != 0.toByte()

  fun setSynchronousUpdate(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 62, if (value) 1 else 0)
  }
}

/** Fields of `struct mln_geometry`. */
internal object MlnGeometry {
  const val SIZEOF: Int = 24

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun type(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setType(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  const val OFFSET_DATA: Int = 8
}

/** Fields of `struct mln_geometry_collection`. */
internal object MlnGeometryCollection {
  const val SIZEOF: Int = 8

  fun geometries(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 0))

  fun setGeometries(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 0, value.address)
  }

  fun geometryCount(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setGeometryCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }
}

/** Fields of `struct mln_http_header_transform`. */
internal object MlnHttpHeaderTransform {
  const val SIZEOF: Int = 12

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  const val OFFSET_CALLBACK: Int = 4

  fun userData(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setUserData(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }
}

/** Fields of `struct mln_http_header_transform_response`. */
internal object MlnHttpHeaderTransformResponse {
  const val SIZEOF: Int = 8

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun context(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 4))

  fun setContext(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 4, value.address)
  }
}

/** Fields of `struct mln_image_content`. */
internal object MlnImageContent {
  const val SIZEOF: Int = 16

  fun left(base: HeapPointer): Float = Heap.loadFloat(base + 0)

  fun setLeft(base: HeapPointer, value: Float) {
    Heap.storeFloat(base + 0, value)
  }

  fun top(base: HeapPointer): Float = Heap.loadFloat(base + 4)

  fun setTop(base: HeapPointer, value: Float) {
    Heap.storeFloat(base + 4, value)
  }

  fun right(base: HeapPointer): Float = Heap.loadFloat(base + 8)

  fun setRight(base: HeapPointer, value: Float) {
    Heap.storeFloat(base + 8, value)
  }

  fun bottom(base: HeapPointer): Float = Heap.loadFloat(base + 12)

  fun setBottom(base: HeapPointer, value: Float) {
    Heap.storeFloat(base + 12, value)
  }
}

/** Fields of `struct mln_image_stretch`. */
internal object MlnImageStretch {
  const val SIZEOF: Int = 8

  fun from(base: HeapPointer): Float = Heap.loadFloat(base + 0)

  fun setFrom(base: HeapPointer, value: Float) {
    Heap.storeFloat(base + 0, value)
  }

  fun to(base: HeapPointer): Float = Heap.loadFloat(base + 4)

  fun setTo(base: HeapPointer, value: Float) {
    Heap.storeFloat(base + 4, value)
  }
}

/** Fields of `struct mln_json_array`. */
internal object MlnJsonArray {
  const val SIZEOF: Int = 8

  fun values(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 0))

  fun setValues(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 0, value.address)
  }

  fun valueCount(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setValueCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }
}

/** Fields of `struct mln_json_member`. */
internal object MlnJsonMember {
  const val SIZEOF: Int = 12
  const val OFFSET_KEY: Int = 0

  fun value(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setValue(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }
}

/** Fields of `struct mln_json_object`. */
internal object MlnJsonObject {
  const val SIZEOF: Int = 8

  fun members(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 0))

  fun setMembers(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 0, value.address)
  }

  fun memberCount(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setMemberCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }
}

/** Fields of `struct mln_json_value`. */
internal object MlnJsonValue {
  const val SIZEOF: Int = 16

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun type(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setType(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  const val OFFSET_DATA: Int = 8
}

/** Fields of `struct mln_lat_lng`. */
internal object MlnLatLng {
  const val SIZEOF: Int = 16

  fun latitude(base: HeapPointer): Double = Heap.loadDouble(base + 0)

  fun setLatitude(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 0, value)
  }

  fun longitude(base: HeapPointer): Double = Heap.loadDouble(base + 8)

  fun setLongitude(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 8, value)
  }
}

/** Fields of `struct mln_lat_lng_bounds`. */
internal object MlnLatLngBounds {
  const val SIZEOF: Int = 32
  const val OFFSET_SOUTHWEST: Int = 0
  const val OFFSET_NORTHEAST: Int = 16
}

/** Fields of `struct mln_map_options`. */
internal object MlnMapOptions {
  const val SIZEOF: Int = 32

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun width(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setWidth(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun height(base: HeapPointer): Int = Heap.loadInt(base + 8)

  fun setHeight(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 8, value)
  }

  fun scaleFactor(base: HeapPointer): Double = Heap.loadDouble(base + 16)

  fun setScaleFactor(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 16, value)
  }

  fun mapMode(base: HeapPointer): Int = Heap.loadInt(base + 24)

  fun setMapMode(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 24, value)
  }

  fun fastPforEnabled(base: HeapPointer): Boolean = Heap.loadByte(base + 28) != 0.toByte()

  fun setFastPforEnabled(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 28, if (value) 1 else 0)
  }
}

/** Fields of `struct mln_map_tile_options`. */
internal object MlnMapTileOptions {
  const val SIZEOF: Int = 56

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun fields(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFields(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun prefetchZoomDelta(base: HeapPointer): Int = Heap.loadInt(base + 8)

  fun setPrefetchZoomDelta(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 8, value)
  }

  fun lodMinRadius(base: HeapPointer): Double = Heap.loadDouble(base + 16)

  fun setLodMinRadius(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 16, value)
  }

  fun lodScale(base: HeapPointer): Double = Heap.loadDouble(base + 24)

  fun setLodScale(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 24, value)
  }

  fun lodPitchThreshold(base: HeapPointer): Double = Heap.loadDouble(base + 32)

  fun setLodPitchThreshold(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 32, value)
  }

  fun lodZoomShift(base: HeapPointer): Double = Heap.loadDouble(base + 40)

  fun setLodZoomShift(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 40, value)
  }

  fun lodMode(base: HeapPointer): Int = Heap.loadInt(base + 48)

  fun setLodMode(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 48, value)
  }
}

/** Fields of `struct mln_map_viewport_options`. */
internal object MlnMapViewportOptions {
  const val SIZEOF: Int = 56

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun fields(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFields(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun northOrientation(base: HeapPointer): Int = Heap.loadInt(base + 8)

  fun setNorthOrientation(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 8, value)
  }

  fun constrainMode(base: HeapPointer): Int = Heap.loadInt(base + 12)

  fun setConstrainMode(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 12, value)
  }

  fun viewportMode(base: HeapPointer): Int = Heap.loadInt(base + 16)

  fun setViewportMode(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 16, value)
  }

  const val OFFSET_FRUSTUM_OFFSET: Int = 24
}

/** Fields of `struct mln_metal_borrowed_texture_descriptor`. */
internal object MlnMetalBorrowedTextureDescriptor {
  const val SIZEOF: Int = 48

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  const val OFFSET_EXTENT: Int = 8

  fun physicalWidth(base: HeapPointer): Int = Heap.loadInt(base + 32)

  fun setPhysicalWidth(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 32, value)
  }

  fun physicalHeight(base: HeapPointer): Int = Heap.loadInt(base + 36)

  fun setPhysicalHeight(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 36, value)
  }

  fun texture(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 40))

  fun setTexture(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 40, value.address)
  }
}

/** Fields of `struct mln_metal_context_descriptor`. */
internal object MlnMetalContextDescriptor {
  const val SIZEOF: Int = 8

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun device(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 4))

  fun setDevice(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 4, value.address)
  }
}

/** Fields of `struct mln_metal_owned_texture_descriptor`. */
internal object MlnMetalOwnedTextureDescriptor {
  const val SIZEOF: Int = 40

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  const val OFFSET_EXTENT: Int = 8
  const val OFFSET_CONTEXT: Int = 32
}

/** Fields of `struct mln_metal_owned_texture_frame`. */
internal object MlnMetalOwnedTextureFrame {
  const val SIZEOF: Int = 56

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun generation(base: HeapPointer): Long = Heap.loadLong(base + 8)

  fun setGeneration(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 8, value)
  }

  fun width(base: HeapPointer): Int = Heap.loadInt(base + 16)

  fun setWidth(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 16, value)
  }

  fun height(base: HeapPointer): Int = Heap.loadInt(base + 20)

  fun setHeight(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 20, value)
  }

  fun scaleFactor(base: HeapPointer): Double = Heap.loadDouble(base + 24)

  fun setScaleFactor(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 24, value)
  }

  fun frameId(base: HeapPointer): Long = Heap.loadLong(base + 32)

  fun setFrameId(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 32, value)
  }

  fun texture(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 40))

  fun setTexture(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 40, value.address)
  }

  fun device(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 44))

  fun setDevice(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 44, value.address)
  }

  fun pixelFormat(base: HeapPointer): Long = Heap.loadLong(base + 48)

  fun setPixelFormat(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 48, value)
  }
}

/** Fields of `struct mln_metal_surface_descriptor`. */
internal object MlnMetalSurfaceDescriptor {
  const val SIZEOF: Int = 48

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  const val OFFSET_EXTENT: Int = 8
  const val OFFSET_CONTEXT: Int = 32

  fun layer(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 40))

  fun setLayer(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 40, value.address)
  }
}

/** Fields of `struct mln_multi_line_geometry`. */
internal object MlnMultiLineGeometry {
  const val SIZEOF: Int = 8

  fun lines(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 0))

  fun setLines(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 0, value.address)
  }

  fun lineCount(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setLineCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }
}

/** Fields of `struct mln_multi_polygon_geometry`. */
internal object MlnMultiPolygonGeometry {
  const val SIZEOF: Int = 8

  fun polygons(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 0))

  fun setPolygons(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 0, value.address)
  }

  fun polygonCount(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setPolygonCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }
}

/** Fields of `struct mln_offline_geometry_region_definition`. */
internal object MlnOfflineGeometryRegionDefinition {
  const val SIZEOF: Int = 40

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun styleUrl(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 4))

  fun setStyleUrl(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 4, value.address)
  }

  fun geometry(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setGeometry(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }

  fun minZoom(base: HeapPointer): Double = Heap.loadDouble(base + 16)

  fun setMinZoom(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 16, value)
  }

  fun maxZoom(base: HeapPointer): Double = Heap.loadDouble(base + 24)

  fun setMaxZoom(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 24, value)
  }

  fun pixelRatio(base: HeapPointer): Float = Heap.loadFloat(base + 32)

  fun setPixelRatio(base: HeapPointer, value: Float) {
    Heap.storeFloat(base + 32, value)
  }

  fun includeIdeographs(base: HeapPointer): Boolean = Heap.loadByte(base + 36) != 0.toByte()

  fun setIncludeIdeographs(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 36, if (value) 1 else 0)
  }
}

/** Fields of `struct mln_offline_region_definition`. */
internal object MlnOfflineRegionDefinition {
  const val SIZEOF: Int = 72

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun type(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setType(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  const val OFFSET_DATA: Int = 8
}

/** Fields of `struct mln_offline_region_info`. */
internal object MlnOfflineRegionInfo {
  const val SIZEOF: Int = 96

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun id(base: HeapPointer): Long = Heap.loadLong(base + 8)

  fun setId(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 8, value)
  }

  const val OFFSET_DEFINITION: Int = 16

  fun metadata(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 88))

  fun setMetadata(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 88, value.address)
  }

  fun metadataSize(base: HeapPointer): Int = Heap.loadInt(base + 92)

  fun setMetadataSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 92, value)
  }
}

/** Fields of `struct mln_offline_region_status`. */
internal object MlnOfflineRegionStatus {
  const val SIZEOF: Int = 64

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun downloadState(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setDownloadState(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun completedResourceCount(base: HeapPointer): Long = Heap.loadLong(base + 8)

  fun setCompletedResourceCount(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 8, value)
  }

  fun completedResourceSize(base: HeapPointer): Long = Heap.loadLong(base + 16)

  fun setCompletedResourceSize(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 16, value)
  }

  fun completedTileCount(base: HeapPointer): Long = Heap.loadLong(base + 24)

  fun setCompletedTileCount(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 24, value)
  }

  fun requiredTileCount(base: HeapPointer): Long = Heap.loadLong(base + 32)

  fun setRequiredTileCount(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 32, value)
  }

  fun completedTileSize(base: HeapPointer): Long = Heap.loadLong(base + 40)

  fun setCompletedTileSize(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 40, value)
  }

  fun requiredResourceCount(base: HeapPointer): Long = Heap.loadLong(base + 48)

  fun setRequiredResourceCount(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 48, value)
  }

  fun requiredResourceCountIsPrecise(base: HeapPointer): Boolean =
    Heap.loadByte(base + 56) != 0.toByte()

  fun setRequiredResourceCountIsPrecise(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 56, if (value) 1 else 0)
  }

  fun complete(base: HeapPointer): Boolean = Heap.loadByte(base + 57) != 0.toByte()

  fun setComplete(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 57, if (value) 1 else 0)
  }
}

/** Fields of `struct mln_offline_tile_pyramid_region_definition`. */
internal object MlnOfflineTilePyramidRegionDefinition {
  const val SIZEOF: Int = 64

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun styleUrl(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 4))

  fun setStyleUrl(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 4, value.address)
  }

  const val OFFSET_BOUNDS: Int = 8

  fun minZoom(base: HeapPointer): Double = Heap.loadDouble(base + 40)

  fun setMinZoom(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 40, value)
  }

  fun maxZoom(base: HeapPointer): Double = Heap.loadDouble(base + 48)

  fun setMaxZoom(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 48, value)
  }

  fun pixelRatio(base: HeapPointer): Float = Heap.loadFloat(base + 56)

  fun setPixelRatio(base: HeapPointer, value: Float) {
    Heap.storeFloat(base + 56, value)
  }

  fun includeIdeographs(base: HeapPointer): Boolean = Heap.loadByte(base + 60) != 0.toByte()

  fun setIncludeIdeographs(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 60, if (value) 1 else 0)
  }
}

/** Fields of `struct mln_opengl_borrowed_texture_descriptor`. */
internal object MlnOpenglBorrowedTextureDescriptor {
  const val SIZEOF: Int = 80

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  const val OFFSET_EXTENT: Int = 8

  fun physicalWidth(base: HeapPointer): Int = Heap.loadInt(base + 32)

  fun setPhysicalWidth(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 32, value)
  }

  fun physicalHeight(base: HeapPointer): Int = Heap.loadInt(base + 36)

  fun setPhysicalHeight(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 36, value)
  }

  const val OFFSET_CONTEXT: Int = 40

  fun texture(base: HeapPointer): Int = Heap.loadInt(base + 68)

  fun setTexture(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 68, value)
  }

  fun target(base: HeapPointer): Int = Heap.loadInt(base + 72)

  fun setTarget(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 72, value)
  }
}

/** Fields of `struct mln_opengl_context_descriptor`. */
internal object MlnOpenglContextDescriptor {
  const val SIZEOF: Int = 28

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun platform(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setPlatform(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  const val OFFSET_DATA: Int = 8
}

/** Fields of `struct mln_opengl_owned_texture_descriptor`. */
internal object MlnOpenglOwnedTextureDescriptor {
  const val SIZEOF: Int = 64

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  const val OFFSET_EXTENT: Int = 8
  const val OFFSET_CONTEXT: Int = 32
}

/** Fields of `struct mln_opengl_owned_texture_frame`. */
internal object MlnOpenglOwnedTextureFrame {
  const val SIZEOF: Int = 64

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun generation(base: HeapPointer): Long = Heap.loadLong(base + 8)

  fun setGeneration(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 8, value)
  }

  fun width(base: HeapPointer): Int = Heap.loadInt(base + 16)

  fun setWidth(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 16, value)
  }

  fun height(base: HeapPointer): Int = Heap.loadInt(base + 20)

  fun setHeight(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 20, value)
  }

  fun scaleFactor(base: HeapPointer): Double = Heap.loadDouble(base + 24)

  fun setScaleFactor(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 24, value)
  }

  fun frameId(base: HeapPointer): Long = Heap.loadLong(base + 32)

  fun setFrameId(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 32, value)
  }

  fun texture(base: HeapPointer): Int = Heap.loadInt(base + 40)

  fun setTexture(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 40, value)
  }

  fun target(base: HeapPointer): Int = Heap.loadInt(base + 44)

  fun setTarget(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 44, value)
  }

  fun internalFormat(base: HeapPointer): Int = Heap.loadInt(base + 48)

  fun setInternalFormat(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 48, value)
  }

  fun format(base: HeapPointer): Int = Heap.loadInt(base + 52)

  fun setFormat(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 52, value)
  }

  fun type(base: HeapPointer): Int = Heap.loadInt(base + 56)

  fun setType(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 56, value)
  }
}

/** Fields of `struct mln_opengl_surface_descriptor`. */
internal object MlnOpenglSurfaceDescriptor {
  const val SIZEOF: Int = 64

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  const val OFFSET_EXTENT: Int = 8
  const val OFFSET_CONTEXT: Int = 32

  fun surface(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 60))

  fun setSurface(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 60, value.address)
  }
}

/** Fields of `struct mln_polygon_geometry`. */
internal object MlnPolygonGeometry {
  const val SIZEOF: Int = 8

  fun rings(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 0))

  fun setRings(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 0, value.address)
  }

  fun ringCount(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setRingCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }
}

/** Fields of `struct mln_premultiplied_rgba8_image`. */
internal object MlnPremultipliedRgba8Image {
  const val SIZEOF: Int = 24

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun width(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setWidth(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun height(base: HeapPointer): Int = Heap.loadInt(base + 8)

  fun setHeight(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 8, value)
  }

  fun stride(base: HeapPointer): Int = Heap.loadInt(base + 12)

  fun setStride(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 12, value)
  }

  fun pixels(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 16))

  fun setPixels(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 16, value.address)
  }

  fun byteLength(base: HeapPointer): Int = Heap.loadInt(base + 20)

  fun setByteLength(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 20, value)
  }
}

/** Fields of `struct mln_projected_meters`. */
internal object MlnProjectedMeters {
  const val SIZEOF: Int = 16

  fun northing(base: HeapPointer): Double = Heap.loadDouble(base + 0)

  fun setNorthing(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 0, value)
  }

  fun easting(base: HeapPointer): Double = Heap.loadDouble(base + 8)

  fun setEasting(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 8, value)
  }
}

/** Fields of `struct mln_projection_mode`. */
internal object MlnProjectionMode {
  const val SIZEOF: Int = 32

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun fields(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFields(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun axonometric(base: HeapPointer): Boolean = Heap.loadByte(base + 8) != 0.toByte()

  fun setAxonometric(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 8, if (value) 1 else 0)
  }

  fun xSkew(base: HeapPointer): Double = Heap.loadDouble(base + 16)

  fun setXSkew(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 16, value)
  }

  fun ySkew(base: HeapPointer): Double = Heap.loadDouble(base + 24)

  fun setYSkew(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 24, value)
  }
}

/** Fields of `struct mln_quaternion`. */
internal object MlnQuaternion {
  const val SIZEOF: Int = 32

  fun x(base: HeapPointer): Double = Heap.loadDouble(base + 0)

  fun setX(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 0, value)
  }

  fun y(base: HeapPointer): Double = Heap.loadDouble(base + 8)

  fun setY(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 8, value)
  }

  fun z(base: HeapPointer): Double = Heap.loadDouble(base + 16)

  fun setZ(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 16, value)
  }

  fun w(base: HeapPointer): Double = Heap.loadDouble(base + 24)

  fun setW(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 24, value)
  }
}

/** Fields of `struct mln_queried_feature`. */
internal object MlnQueriedFeature {
  const val SIZEOF: Int = 64

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun fields(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFields(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  const val OFFSET_FEATURE: Int = 8
  const val OFFSET_SOURCE_ID: Int = 40
  const val OFFSET_SOURCE_LAYER_ID: Int = 48

  fun state(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 56))

  fun setState(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 56, value.address)
  }
}

/** Fields of `struct mln_render_target_extent`. */
internal object MlnRenderTargetExtent {
  const val SIZEOF: Int = 24

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun width(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setWidth(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun height(base: HeapPointer): Int = Heap.loadInt(base + 8)

  fun setHeight(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 8, value)
  }

  fun scaleFactor(base: HeapPointer): Double = Heap.loadDouble(base + 16)

  fun setScaleFactor(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 16, value)
  }
}

/** Fields of `struct mln_rendered_feature_query_options`. */
internal object MlnRenderedFeatureQueryOptions {
  const val SIZEOF: Int = 20

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun fields(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFields(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun layerIds(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setLayerIds(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }

  fun layerIdCount(base: HeapPointer): Int = Heap.loadInt(base + 12)

  fun setLayerIdCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 12, value)
  }

  fun filter(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 16))

  fun setFilter(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 16, value.address)
  }
}

/** Fields of `struct mln_rendered_query_geometry`. */
internal object MlnRenderedQueryGeometry {
  const val SIZEOF: Int = 40

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun type(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setType(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  const val OFFSET_DATA: Int = 8
}

/** Fields of `struct mln_rendering_stats`. */
internal object MlnRenderingStats {
  const val SIZEOF: Int = 48

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun encodingTime(base: HeapPointer): Double = Heap.loadDouble(base + 8)

  fun setEncodingTime(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 8, value)
  }

  fun renderingTime(base: HeapPointer): Double = Heap.loadDouble(base + 16)

  fun setRenderingTime(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 16, value)
  }

  fun frameCount(base: HeapPointer): Long = Heap.loadLong(base + 24)

  fun setFrameCount(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 24, value)
  }

  fun drawCallCount(base: HeapPointer): Long = Heap.loadLong(base + 32)

  fun setDrawCallCount(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 32, value)
  }

  fun totalDrawCallCount(base: HeapPointer): Long = Heap.loadLong(base + 40)

  fun setTotalDrawCallCount(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 40, value)
  }
}

/** Fields of `struct mln_resource_provider`. */
internal object MlnResourceProvider {
  const val SIZEOF: Int = 12

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  const val OFFSET_CALLBACK: Int = 4

  fun userData(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setUserData(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }
}

/** Fields of `struct mln_resource_request`. */
internal object MlnResourceRequest {
  const val SIZEOF: Int = 104

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun requestedUrl(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 4))

  fun setRequestedUrl(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 4, value.address)
  }

  fun resolvedUrl(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setResolvedUrl(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }

  fun kind(base: HeapPointer): Int = Heap.loadInt(base + 12)

  fun setKind(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 12, value)
  }

  fun loadingMethod(base: HeapPointer): Int = Heap.loadInt(base + 16)

  fun setLoadingMethod(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 16, value)
  }

  fun priority(base: HeapPointer): Int = Heap.loadInt(base + 20)

  fun setPriority(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 20, value)
  }

  fun usage(base: HeapPointer): Int = Heap.loadInt(base + 24)

  fun setUsage(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 24, value)
  }

  fun storagePolicy(base: HeapPointer): Int = Heap.loadInt(base + 28)

  fun setStoragePolicy(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 28, value)
  }

  fun hasRange(base: HeapPointer): Boolean = Heap.loadByte(base + 32) != 0.toByte()

  fun setHasRange(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 32, if (value) 1 else 0)
  }

  fun rangeStart(base: HeapPointer): Long = Heap.loadLong(base + 40)

  fun setRangeStart(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 40, value)
  }

  fun rangeEnd(base: HeapPointer): Long = Heap.loadLong(base + 48)

  fun setRangeEnd(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 48, value)
  }

  fun hasPriorModified(base: HeapPointer): Boolean = Heap.loadByte(base + 56) != 0.toByte()

  fun setHasPriorModified(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 56, if (value) 1 else 0)
  }

  fun priorModifiedUnixMs(base: HeapPointer): Long = Heap.loadLong(base + 64)

  fun setPriorModifiedUnixMs(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 64, value)
  }

  fun hasPriorExpires(base: HeapPointer): Boolean = Heap.loadByte(base + 72) != 0.toByte()

  fun setHasPriorExpires(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 72, if (value) 1 else 0)
  }

  fun priorExpiresUnixMs(base: HeapPointer): Long = Heap.loadLong(base + 80)

  fun setPriorExpiresUnixMs(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 80, value)
  }

  fun priorEtag(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 88))

  fun setPriorEtag(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 88, value.address)
  }

  fun priorData(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 92))

  fun setPriorData(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 92, value.address)
  }

  fun priorDataSize(base: HeapPointer): Int = Heap.loadInt(base + 96)

  fun setPriorDataSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 96, value)
  }
}

/** Fields of `struct mln_resource_response`. */
internal object MlnResourceResponse {
  const val SIZEOF: Int = 72

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun status(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setStatus(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun errorReason(base: HeapPointer): Int = Heap.loadInt(base + 8)

  fun setErrorReason(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 8, value)
  }

  fun bytes(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 12))

  fun setBytes(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 12, value.address)
  }

  fun byteCount(base: HeapPointer): Int = Heap.loadInt(base + 16)

  fun setByteCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 16, value)
  }

  fun errorMessage(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 20))

  fun setErrorMessage(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 20, value.address)
  }

  fun mustRevalidate(base: HeapPointer): Boolean = Heap.loadByte(base + 24) != 0.toByte()

  fun setMustRevalidate(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 24, if (value) 1 else 0)
  }

  fun hasModified(base: HeapPointer): Boolean = Heap.loadByte(base + 25) != 0.toByte()

  fun setHasModified(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 25, if (value) 1 else 0)
  }

  fun modifiedUnixMs(base: HeapPointer): Long = Heap.loadLong(base + 32)

  fun setModifiedUnixMs(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 32, value)
  }

  fun hasExpires(base: HeapPointer): Boolean = Heap.loadByte(base + 40) != 0.toByte()

  fun setHasExpires(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 40, if (value) 1 else 0)
  }

  fun expiresUnixMs(base: HeapPointer): Long = Heap.loadLong(base + 48)

  fun setExpiresUnixMs(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 48, value)
  }

  fun etag(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 56))

  fun setEtag(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 56, value.address)
  }

  fun hasRetryAfter(base: HeapPointer): Boolean = Heap.loadByte(base + 60) != 0.toByte()

  fun setHasRetryAfter(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 60, if (value) 1 else 0)
  }

  fun retryAfterUnixMs(base: HeapPointer): Long = Heap.loadLong(base + 64)

  fun setRetryAfterUnixMs(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 64, value)
  }
}

/** Fields of `struct mln_resource_transform`. */
internal object MlnResourceTransform {
  const val SIZEOF: Int = 12

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  const val OFFSET_CALLBACK: Int = 4

  fun userData(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setUserData(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }
}

/** Fields of `struct mln_resource_transform_response`. */
internal object MlnResourceTransformResponse {
  const val SIZEOF: Int = 12

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun url(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 4))

  fun setUrl(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 4, value.address)
  }

  fun context(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setContext(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }
}

/** Fields of `struct mln_runtime_event`. */
internal object MlnRuntimeEvent {
  const val SIZEOF: Int = 48

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun type(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setType(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun sourceType(base: HeapPointer): Int = Heap.loadInt(base + 8)

  fun setSourceType(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 8, value)
  }

  fun source(base: HeapPointer): Long = Heap.loadLong(base + 16)

  fun setSource(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 16, value)
  }

  fun code(base: HeapPointer): Int = Heap.loadInt(base + 24)

  fun setCode(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 24, value)
  }

  fun payloadType(base: HeapPointer): Int = Heap.loadInt(base + 28)

  fun setPayloadType(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 28, value)
  }

  fun payload(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 32))

  fun setPayload(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 32, value.address)
  }

  fun payloadSize(base: HeapPointer): Int = Heap.loadInt(base + 36)

  fun setPayloadSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 36, value)
  }

  fun message(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 40))

  fun setMessage(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 40, value.address)
  }

  fun messageSize(base: HeapPointer): Int = Heap.loadInt(base + 44)

  fun setMessageSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 44, value)
  }
}

/** Fields of `struct mln_runtime_event_camera_transition_finished`. */
internal object MlnRuntimeEventCameraTransitionFinished {
  const val SIZEOF: Int = 16

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun transitionId(base: HeapPointer): Long = Heap.loadLong(base + 8)

  fun setTransitionId(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 8, value)
  }
}

/** Fields of `struct mln_runtime_event_offline_operation_completed`. */
internal object MlnRuntimeEventOfflineOperationCompleted {
  const val SIZEOF: Int = 32

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun operationId(base: HeapPointer): Long = Heap.loadLong(base + 8)

  fun setOperationId(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 8, value)
  }

  fun operationKind(base: HeapPointer): Int = Heap.loadInt(base + 16)

  fun setOperationKind(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 16, value)
  }

  fun resultKind(base: HeapPointer): Int = Heap.loadInt(base + 20)

  fun setResultKind(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 20, value)
  }

  fun resultStatus(base: HeapPointer): Int = Heap.loadInt(base + 24)

  fun setResultStatus(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 24, value)
  }

  fun found(base: HeapPointer): Boolean = Heap.loadByte(base + 28) != 0.toByte()

  fun setFound(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 28, if (value) 1 else 0)
  }
}

/** Fields of `struct mln_runtime_event_offline_region_response_error`. */
internal object MlnRuntimeEventOfflineRegionResponseError {
  const val SIZEOF: Int = 24

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun regionId(base: HeapPointer): Long = Heap.loadLong(base + 8)

  fun setRegionId(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 8, value)
  }

  fun reason(base: HeapPointer): Int = Heap.loadInt(base + 16)

  fun setReason(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 16, value)
  }
}

/** Fields of `struct mln_runtime_event_offline_region_status`. */
internal object MlnRuntimeEventOfflineRegionStatus {
  const val SIZEOF: Int = 80

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun regionId(base: HeapPointer): Long = Heap.loadLong(base + 8)

  fun setRegionId(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 8, value)
  }

  const val OFFSET_STATUS: Int = 16
}

/** Fields of `struct mln_runtime_event_offline_region_tile_count_limit`. */
internal object MlnRuntimeEventOfflineRegionTileCountLimit {
  const val SIZEOF: Int = 24

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun regionId(base: HeapPointer): Long = Heap.loadLong(base + 8)

  fun setRegionId(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 8, value)
  }

  fun limit(base: HeapPointer): Long = Heap.loadLong(base + 16)

  fun setLimit(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 16, value)
  }
}

/** Fields of `struct mln_runtime_event_render_frame`. */
internal object MlnRuntimeEventRenderFrame {
  const val SIZEOF: Int = 64

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun mode(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setMode(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun needsRepaint(base: HeapPointer): Boolean = Heap.loadByte(base + 8) != 0.toByte()

  fun setNeedsRepaint(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 8, if (value) 1 else 0)
  }

  fun placementChanged(base: HeapPointer): Boolean = Heap.loadByte(base + 9) != 0.toByte()

  fun setPlacementChanged(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 9, if (value) 1 else 0)
  }

  const val OFFSET_STATS: Int = 16
}

/** Fields of `struct mln_runtime_event_render_map`. */
internal object MlnRuntimeEventRenderMap {
  const val SIZEOF: Int = 8

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun mode(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setMode(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }
}

/** Fields of `struct mln_runtime_event_style_image_missing`. */
internal object MlnRuntimeEventStyleImageMissing {
  const val SIZEOF: Int = 12

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun imageId(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 4))

  fun setImageId(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 4, value.address)
  }

  fun imageIdSize(base: HeapPointer): Int = Heap.loadInt(base + 8)

  fun setImageIdSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 8, value)
  }
}

/** Fields of `struct mln_runtime_event_tile_action`. */
internal object MlnRuntimeEventTileAction {
  const val SIZEOF: Int = 36

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun operation(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setOperation(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  const val OFFSET_TILE_ID: Int = 8

  fun sourceId(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 28))

  fun setSourceId(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 28, value.address)
  }

  fun sourceIdSize(base: HeapPointer): Int = Heap.loadInt(base + 32)

  fun setSourceIdSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 32, value)
  }
}

/** Fields of `struct mln_runtime_options`. */
internal object MlnRuntimeOptions {
  const val SIZEOF: Int = 16

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun flags(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFlags(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun assetPath(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setAssetPath(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }

  fun cachePath(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 12))

  fun setCachePath(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 12, value.address)
  }
}

/** Fields of `struct mln_screen_box`. */
internal object MlnScreenBox {
  const val SIZEOF: Int = 32
  const val OFFSET_MIN: Int = 0
  const val OFFSET_MAX: Int = 16
}

/** Fields of `struct mln_screen_line_string`. */
internal object MlnScreenLineString {
  const val SIZEOF: Int = 8

  fun points(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 0))

  fun setPoints(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 0, value.address)
  }

  fun pointCount(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setPointCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }
}

/** Fields of `struct mln_screen_point`. */
internal object MlnScreenPoint {
  const val SIZEOF: Int = 16

  fun x(base: HeapPointer): Double = Heap.loadDouble(base + 0)

  fun setX(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 0, value)
  }

  fun y(base: HeapPointer): Double = Heap.loadDouble(base + 8)

  fun setY(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 8, value)
  }
}

/** Fields of `struct mln_source_feature_query_options`. */
internal object MlnSourceFeatureQueryOptions {
  const val SIZEOF: Int = 20

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun fields(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFields(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun sourceLayerIds(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setSourceLayerIds(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }

  fun sourceLayerIdCount(base: HeapPointer): Int = Heap.loadInt(base + 12)

  fun setSourceLayerIdCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 12, value)
  }

  fun filter(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 16))

  fun setFilter(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 16, value.address)
  }
}

/** Fields of `struct mln_string_view`. */
internal object MlnStringView {
  const val SIZEOF: Int = 8

  fun data(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 0))

  fun setData(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 0, value.address)
  }

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }
}

/** Fields of `struct mln_style_image_info`. */
internal object MlnStyleImageInfo {
  const val SIZEOF: Int = 60

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun width(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setWidth(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun height(base: HeapPointer): Int = Heap.loadInt(base + 8)

  fun setHeight(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 8, value)
  }

  fun stride(base: HeapPointer): Int = Heap.loadInt(base + 12)

  fun setStride(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 12, value)
  }

  fun byteLength(base: HeapPointer): Int = Heap.loadInt(base + 16)

  fun setByteLength(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 16, value)
  }

  fun stretchXCount(base: HeapPointer): Int = Heap.loadInt(base + 20)

  fun setStretchXCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 20, value)
  }

  fun stretchYCount(base: HeapPointer): Int = Heap.loadInt(base + 24)

  fun setStretchYCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 24, value)
  }

  const val OFFSET_CONTENT: Int = 28

  fun textFitWidth(base: HeapPointer): Int = Heap.loadInt(base + 44)

  fun setTextFitWidth(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 44, value)
  }

  fun textFitHeight(base: HeapPointer): Int = Heap.loadInt(base + 48)

  fun setTextFitHeight(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 48, value)
  }

  fun pixelRatio(base: HeapPointer): Float = Heap.loadFloat(base + 52)

  fun setPixelRatio(base: HeapPointer, value: Float) {
    Heap.storeFloat(base + 52, value)
  }

  fun sdf(base: HeapPointer): Boolean = Heap.loadByte(base + 56) != 0.toByte()

  fun setSdf(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 56, if (value) 1 else 0)
  }

  fun hasContent(base: HeapPointer): Boolean = Heap.loadByte(base + 57) != 0.toByte()

  fun setHasContent(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 57, if (value) 1 else 0)
  }

  fun hasTextFitWidth(base: HeapPointer): Boolean = Heap.loadByte(base + 58) != 0.toByte()

  fun setHasTextFitWidth(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 58, if (value) 1 else 0)
  }

  fun hasTextFitHeight(base: HeapPointer): Boolean = Heap.loadByte(base + 59) != 0.toByte()

  fun setHasTextFitHeight(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 59, if (value) 1 else 0)
  }
}

/** Fields of `struct mln_style_image_options`. */
internal object MlnStyleImageOptions {
  const val SIZEOF: Int = 56

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun fields(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFields(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun stretchX(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setStretchX(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }

  fun stretchXCount(base: HeapPointer): Int = Heap.loadInt(base + 12)

  fun setStretchXCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 12, value)
  }

  fun stretchY(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 16))

  fun setStretchY(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 16, value.address)
  }

  fun stretchYCount(base: HeapPointer): Int = Heap.loadInt(base + 20)

  fun setStretchYCount(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 20, value)
  }

  const val OFFSET_CONTENT: Int = 24

  fun textFitWidth(base: HeapPointer): Int = Heap.loadInt(base + 40)

  fun setTextFitWidth(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 40, value)
  }

  fun textFitHeight(base: HeapPointer): Int = Heap.loadInt(base + 44)

  fun setTextFitHeight(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 44, value)
  }

  fun pixelRatio(base: HeapPointer): Float = Heap.loadFloat(base + 48)

  fun setPixelRatio(base: HeapPointer, value: Float) {
    Heap.storeFloat(base + 48, value)
  }

  fun sdf(base: HeapPointer): Boolean = Heap.loadByte(base + 52) != 0.toByte()

  fun setSdf(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 52, if (value) 1 else 0)
  }
}

/** Fields of `struct mln_style_source_info`. */
internal object MlnStyleSourceInfo {
  const val SIZEOF: Int = 20

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun type(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setType(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun idSize(base: HeapPointer): Int = Heap.loadInt(base + 8)

  fun setIdSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 8, value)
  }

  fun isVolatile(base: HeapPointer): Boolean = Heap.loadByte(base + 12) != 0.toByte()

  fun setIsVolatile(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 12, if (value) 1 else 0)
  }

  fun hasAttribution(base: HeapPointer): Boolean = Heap.loadByte(base + 13) != 0.toByte()

  fun setHasAttribution(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 13, if (value) 1 else 0)
  }

  fun attributionSize(base: HeapPointer): Int = Heap.loadInt(base + 16)

  fun setAttributionSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 16, value)
  }
}

/** Fields of `struct mln_style_tile_source_options`. */
internal object MlnStyleTileSourceOptions {
  const val SIZEOF: Int = 88

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun fields(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFields(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun minZoom(base: HeapPointer): Double = Heap.loadDouble(base + 8)

  fun setMinZoom(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 8, value)
  }

  fun maxZoom(base: HeapPointer): Double = Heap.loadDouble(base + 16)

  fun setMaxZoom(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 16, value)
  }

  const val OFFSET_ATTRIBUTION: Int = 24

  fun scheme(base: HeapPointer): Int = Heap.loadInt(base + 32)

  fun setScheme(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 32, value)
  }

  const val OFFSET_BOUNDS: Int = 40

  fun tileSize(base: HeapPointer): Int = Heap.loadInt(base + 72)

  fun setTileSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 72, value)
  }

  fun vectorEncoding(base: HeapPointer): Int = Heap.loadInt(base + 76)

  fun setVectorEncoding(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 76, value)
  }

  fun rasterEncoding(base: HeapPointer): Int = Heap.loadInt(base + 80)

  fun setRasterEncoding(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 80, value)
  }
}

/** Fields of `struct mln_style_transition_options`. */
internal object MlnStyleTransitionOptions {
  const val SIZEOF: Int = 32

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun fields(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setFields(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun durationMs(base: HeapPointer): Double = Heap.loadDouble(base + 8)

  fun setDurationMs(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 8, value)
  }

  fun delayMs(base: HeapPointer): Double = Heap.loadDouble(base + 16)

  fun setDelayMs(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 16, value)
  }

  fun enablePlacementTransitions(base: HeapPointer): Boolean =
    Heap.loadByte(base + 24) != 0.toByte()

  fun setEnablePlacementTransitions(base: HeapPointer, value: Boolean) {
    Heap.storeByte(base + 24, if (value) 1 else 0)
  }
}

/** Fields of `struct mln_texture_image_info`. */
internal object MlnTextureImageInfo {
  const val SIZEOF: Int = 20

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun width(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setWidth(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun height(base: HeapPointer): Int = Heap.loadInt(base + 8)

  fun setHeight(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 8, value)
  }

  fun stride(base: HeapPointer): Int = Heap.loadInt(base + 12)

  fun setStride(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 12, value)
  }

  fun byteLength(base: HeapPointer): Int = Heap.loadInt(base + 16)

  fun setByteLength(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 16, value)
  }
}

/** Fields of `struct mln_tile_id`. */
internal object MlnTileId {
  const val SIZEOF: Int = 20

  fun overscaledZ(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setOverscaledZ(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun wrap(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setWrap(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }

  fun canonicalZ(base: HeapPointer): Int = Heap.loadInt(base + 8)

  fun setCanonicalZ(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 8, value)
  }

  fun canonicalX(base: HeapPointer): Int = Heap.loadInt(base + 12)

  fun setCanonicalX(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 12, value)
  }

  fun canonicalY(base: HeapPointer): Int = Heap.loadInt(base + 16)

  fun setCanonicalY(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 16, value)
  }
}

/** Fields of `struct mln_unit_bezier`. */
internal object MlnUnitBezier {
  const val SIZEOF: Int = 32

  fun x1(base: HeapPointer): Double = Heap.loadDouble(base + 0)

  fun setX1(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 0, value)
  }

  fun y1(base: HeapPointer): Double = Heap.loadDouble(base + 8)

  fun setY1(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 8, value)
  }

  fun x2(base: HeapPointer): Double = Heap.loadDouble(base + 16)

  fun setX2(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 16, value)
  }

  fun y2(base: HeapPointer): Double = Heap.loadDouble(base + 24)

  fun setY2(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 24, value)
  }
}

/** Fields of `struct mln_vec3`. */
internal object MlnVec3 {
  const val SIZEOF: Int = 24

  fun x(base: HeapPointer): Double = Heap.loadDouble(base + 0)

  fun setX(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 0, value)
  }

  fun y(base: HeapPointer): Double = Heap.loadDouble(base + 8)

  fun setY(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 8, value)
  }

  fun z(base: HeapPointer): Double = Heap.loadDouble(base + 16)

  fun setZ(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 16, value)
  }
}

/** Fields of `struct mln_vulkan_borrowed_texture_descriptor`. */
internal object MlnVulkanBorrowedTextureDescriptor {
  const val SIZEOF: Int = 96

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  const val OFFSET_EXTENT: Int = 8

  fun physicalWidth(base: HeapPointer): Int = Heap.loadInt(base + 32)

  fun setPhysicalWidth(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 32, value)
  }

  fun physicalHeight(base: HeapPointer): Int = Heap.loadInt(base + 36)

  fun setPhysicalHeight(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 36, value)
  }

  const val OFFSET_CONTEXT: Int = 40

  fun image(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 72))

  fun setImage(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 72, value.address)
  }

  fun imageView(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 76))

  fun setImageView(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 76, value.address)
  }

  fun format(base: HeapPointer): Int = Heap.loadInt(base + 80)

  fun setFormat(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 80, value)
  }

  fun initialLayout(base: HeapPointer): Int = Heap.loadInt(base + 84)

  fun setInitialLayout(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 84, value)
  }

  fun finalLayout(base: HeapPointer): Int = Heap.loadInt(base + 88)

  fun setFinalLayout(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 88, value)
  }
}

/** Fields of `struct mln_vulkan_context_descriptor`. */
internal object MlnVulkanContextDescriptor {
  const val SIZEOF: Int = 32

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun instance(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 4))

  fun setInstance(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 4, value.address)
  }

  fun physicalDevice(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setPhysicalDevice(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }

  fun device(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 12))

  fun setDevice(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 12, value.address)
  }

  fun graphicsQueue(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 16))

  fun setGraphicsQueue(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 16, value.address)
  }

  fun graphicsQueueFamilyIndex(base: HeapPointer): Int = Heap.loadInt(base + 20)

  fun setGraphicsQueueFamilyIndex(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 20, value)
  }

  fun getInstanceProcAddr(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 24))

  fun setGetInstanceProcAddr(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 24, value.address)
  }

  fun getDeviceProcAddr(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 28))

  fun setGetDeviceProcAddr(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 28, value.address)
  }
}

/** Fields of `struct mln_vulkan_owned_texture_descriptor`. */
internal object MlnVulkanOwnedTextureDescriptor {
  const val SIZEOF: Int = 64

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  const val OFFSET_EXTENT: Int = 8
  const val OFFSET_CONTEXT: Int = 32
}

/** Fields of `struct mln_vulkan_owned_texture_frame`. */
internal object MlnVulkanOwnedTextureFrame {
  const val SIZEOF: Int = 64

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun generation(base: HeapPointer): Long = Heap.loadLong(base + 8)

  fun setGeneration(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 8, value)
  }

  fun width(base: HeapPointer): Int = Heap.loadInt(base + 16)

  fun setWidth(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 16, value)
  }

  fun height(base: HeapPointer): Int = Heap.loadInt(base + 20)

  fun setHeight(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 20, value)
  }

  fun scaleFactor(base: HeapPointer): Double = Heap.loadDouble(base + 24)

  fun setScaleFactor(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 24, value)
  }

  fun frameId(base: HeapPointer): Long = Heap.loadLong(base + 32)

  fun setFrameId(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 32, value)
  }

  fun image(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 40))

  fun setImage(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 40, value.address)
  }

  fun imageView(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 44))

  fun setImageView(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 44, value.address)
  }

  fun device(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 48))

  fun setDevice(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 48, value.address)
  }

  fun format(base: HeapPointer): Int = Heap.loadInt(base + 52)

  fun setFormat(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 52, value)
  }

  fun layout(base: HeapPointer): Int = Heap.loadInt(base + 56)

  fun setLayout(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 56, value)
  }
}

/** Fields of `struct mln_vulkan_surface_descriptor`. */
internal object MlnVulkanSurfaceDescriptor {
  const val SIZEOF: Int = 72

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  const val OFFSET_EXTENT: Int = 8
  const val OFFSET_CONTEXT: Int = 32

  fun surface(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 64))

  fun setSurface(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 64, value.address)
  }
}

/** Fields of `struct mln_webgl_context_descriptor`. */
internal object MlnWebglContextDescriptor {
  const val SIZEOF: Int = 8

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun context(base: HeapPointer): Int = Heap.loadInt(base + 4)

  fun setContext(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 4, value)
  }
}

/** Fields of `struct mln_webgpu_borrowed_texture_descriptor`. */
internal object MlnWebgpuBorrowedTextureDescriptor {
  const val SIZEOF: Int = 72

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  const val OFFSET_EXTENT: Int = 8

  fun physicalWidth(base: HeapPointer): Int = Heap.loadInt(base + 32)

  fun setPhysicalWidth(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 32, value)
  }

  fun physicalHeight(base: HeapPointer): Int = Heap.loadInt(base + 36)

  fun setPhysicalHeight(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 36, value)
  }

  const val OFFSET_CONTEXT: Int = 40

  fun texture(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 56))

  fun setTexture(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 56, value.address)
  }

  fun textureView(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 60))

  fun setTextureView(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 60, value.address)
  }

  fun format(base: HeapPointer): Int = Heap.loadInt(base + 64)

  fun setFormat(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 64, value)
  }
}

/** Fields of `struct mln_webgpu_context_descriptor`. */
internal object MlnWebgpuContextDescriptor {
  const val SIZEOF: Int = 16

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun instance(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 4))

  fun setInstance(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 4, value.address)
  }

  fun device(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setDevice(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }

  fun queue(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 12))

  fun setQueue(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 12, value.address)
  }
}

/** Fields of `struct mln_webgpu_owned_texture_descriptor`. */
internal object MlnWebgpuOwnedTextureDescriptor {
  const val SIZEOF: Int = 48

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  const val OFFSET_EXTENT: Int = 8
  const val OFFSET_CONTEXT: Int = 32
}

/** Fields of `struct mln_webgpu_owned_texture_frame`. */
internal object MlnWebgpuOwnedTextureFrame {
  const val SIZEOF: Int = 56

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun generation(base: HeapPointer): Long = Heap.loadLong(base + 8)

  fun setGeneration(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 8, value)
  }

  fun width(base: HeapPointer): Int = Heap.loadInt(base + 16)

  fun setWidth(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 16, value)
  }

  fun height(base: HeapPointer): Int = Heap.loadInt(base + 20)

  fun setHeight(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 20, value)
  }

  fun scaleFactor(base: HeapPointer): Double = Heap.loadDouble(base + 24)

  fun setScaleFactor(base: HeapPointer, value: Double) {
    Heap.storeDouble(base + 24, value)
  }

  fun frameId(base: HeapPointer): Long = Heap.loadLong(base + 32)

  fun setFrameId(base: HeapPointer, value: Long) {
    Heap.storeLong(base + 32, value)
  }

  fun texture(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 40))

  fun setTexture(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 40, value.address)
  }

  fun textureView(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 44))

  fun setTextureView(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 44, value.address)
  }

  fun device(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 48))

  fun setDevice(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 48, value.address)
  }

  fun format(base: HeapPointer): Int = Heap.loadInt(base + 52)

  fun setFormat(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 52, value)
  }
}

/** Fields of `struct mln_wgl_context_descriptor`. */
internal object MlnWglContextDescriptor {
  const val SIZEOF: Int = 16

  fun size(base: HeapPointer): Int = Heap.loadInt(base + 0)

  fun setSize(base: HeapPointer, value: Int) {
    Heap.storeInt(base + 0, value)
  }

  fun deviceContext(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 4))

  fun setDeviceContext(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 4, value.address)
  }

  fun shareContext(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 8))

  fun setShareContext(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 8, value.address)
  }

  fun getProcAddress(base: HeapPointer): HeapPointer = HeapPointer(Heap.loadInt(base + 12))

  fun setGetProcAddress(base: HeapPointer, value: HeapPointer) {
    Heap.storeInt(base + 12, value.address)
  }
}
