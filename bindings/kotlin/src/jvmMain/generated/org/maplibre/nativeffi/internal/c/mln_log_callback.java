// Generated by jextract

package org.maplibre.nativeffi.internal.c;

import java.lang.invoke.*;
import java.lang.foreign.*;
import java.nio.ByteOrder;
import java.util.*;
import java.util.function.*;
import java.util.stream.*;

import static java.lang.foreign.ValueLayout.*;
import static java.lang.foreign.MemoryLayout.PathElement.*;

/**
 * {@snippet lang=c :
 * typedef uint32_t (*mln_log_callback)(void *, uint32_t, uint32_t, int64_t, const char *)
 * }
 */
public final class mln_log_callback {

    private mln_log_callback() {
        // Should not be called directly
    }

    /**
     * The function pointer signature, expressed as a functional interface
     */
    public interface Function {
        int apply(MemorySegment user_data, int severity, int event, long code, MemorySegment message);
    }

    private static final FunctionDescriptor $DESC = FunctionDescriptor.of(
        MapLibreNativeC.C_INT,
        MapLibreNativeC.C_POINTER,
        MapLibreNativeC.C_INT,
        MapLibreNativeC.C_INT,
        MapLibreNativeC.C_LONG,
        MapLibreNativeC.C_POINTER
    );

    /**
     * The descriptor of this function pointer
     */
    public static FunctionDescriptor descriptor() {
        return $DESC;
    }

    private static final MethodHandle UP$MH = MapLibreNativeC.upcallHandle(mln_log_callback.Function.class, "apply", $DESC);

    /**
     * Allocates a new upcall stub, whose implementation is defined by {@code fi}.
     * The lifetime of the returned segment is managed by {@code arena}
     */
    public static MemorySegment allocate(mln_log_callback.Function fi, Arena arena) {
        return Linker.nativeLinker().upcallStub(UP$MH.bindTo(fi), $DESC, arena);
    }

    private static final MethodHandle DOWN$MH = Linker.nativeLinker().downcallHandle($DESC);

    /**
     * Invoke the upcall stub {@code funcPtr}, with given parameters
     */
    public static int invoke(MemorySegment funcPtr, MemorySegment user_data, int severity, int event, long code, MemorySegment message) {
        try {
            return (int) DOWN$MH.invokeExact(funcPtr, user_data, severity, event, code, message);
        } catch (Error | RuntimeException ex) {
            throw ex;
        } catch (Throwable ex$) {
            throw new AssertionError("should not reach here", ex$);
        }
    }
}
