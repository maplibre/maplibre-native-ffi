/** The ability the test runner starts. It exists so hypium has one to attach to. */
import UIAbility from '@ohos.app.ability.UIAbility';

export default class TestAbility extends UIAbility {
  onCreate() {}
  onDestroy() {}
}
