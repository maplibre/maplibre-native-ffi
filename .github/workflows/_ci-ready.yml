# Generated by `mise run ci:generate-workflow`. Do not edit directly.
name: CI ready coverage
on:
  workflow_call:
    inputs:
      scope:
        type: string
        required: true
      jobs:
        type: string
        required: true
permissions:
  contents: read
  actions: read
jobs:
  target-emscripten-wasm32-webgl:
    name: target / emscripten-wasm32-webgl
    runs-on: ubuntu-latest
    timeout-minutes: 120
    environment: ${{ github.event_name == 'push' && 'sccache' || '' }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-ci-deps
        id: setup
        with:
          sccache-access-key-id: ${{ secrets.R2_SCCACHE_ACCESS_KEY_ID }}
          sccache-secret-access-key: ${{ secrets.R2_SCCACHE_SECRET_ACCESS_KEY }}
          target: emscripten-wasm32-webgl
          gradle: false
          save-toolchains: true
      - run: mise run test emscripten-wasm32-webgl
      - name: Archive native artifact
        run: mise run archive-native emscripten-wasm32-webgl
      - name: Install packaged native artifact
        run: mise run install-native-package emscripten-wasm32-webgl build/packages/native/dist/maplibre-native-c-emscripten-wasm32-webgl.tar.gz
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/rust:test emscripten-wasm32-webgl
      - name: Upload native artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: native-package-emscripten-wasm32-webgl-${{ github.sha }}
          path: build/packages/native/dist/maplibre-native-c-emscripten-wasm32-webgl.tar.gz
          if-no-files-found: error
          retention-days: ${{ github.event_name == 'pull_request' && 1 || 30 }}
    if: contains(fromJSON(inputs.jobs), 'target-emscripten-wasm32-webgl')
  target-emscripten-wasm32-webgpu:
    name: target / emscripten-wasm32-webgpu
    runs-on: ubuntu-latest
    timeout-minutes: 120
    environment: ${{ github.event_name == 'push' && 'sccache' || '' }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-ci-deps
        id: setup
        with:
          sccache-access-key-id: ${{ secrets.R2_SCCACHE_ACCESS_KEY_ID }}
          sccache-secret-access-key: ${{ secrets.R2_SCCACHE_SECRET_ACCESS_KEY }}
          target: emscripten-wasm32-webgpu
          gradle: false
      - run: mise run test emscripten-wasm32-webgpu
      - name: Archive native artifact
        run: mise run archive-native emscripten-wasm32-webgpu
      - name: Install packaged native artifact
        run: mise run install-native-package emscripten-wasm32-webgpu build/packages/native/dist/maplibre-native-c-emscripten-wasm32-webgpu.tar.gz
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/rust:test emscripten-wasm32-webgpu
      - name: Upload native artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: native-package-emscripten-wasm32-webgpu-${{ github.sha }}
          path: build/packages/native/dist/maplibre-native-c-emscripten-wasm32-webgpu.tar.gz
          if-no-files-found: error
          retention-days: ${{ github.event_name == 'pull_request' && 1 || 30 }}
    if: contains(fromJSON(inputs.jobs), 'target-emscripten-wasm32-webgpu')
  target-macos-arm64-metal:
    name: target / macos-arm64-metal
    runs-on: macos-26
    timeout-minutes: 120
    environment: ${{ github.event_name == 'push' && 'sccache' || '' }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-ci-deps
        id: setup
        with:
          sccache-access-key-id: ${{ secrets.R2_SCCACHE_ACCESS_KEY_ID }}
          sccache-secret-access-key: ${{ secrets.R2_SCCACHE_SECRET_ACCESS_KEY }}
          target: macos-arm64-metal
          zig: true
          save-toolchains: true
      - run: mise run test macos-arm64-metal
      - name: Archive native artifact
        run: mise run archive-native macos-arm64-metal
      - name: Install packaged native artifact
        run: mise run install-native-package macos-arm64-metal build/packages/native/dist/maplibre-native-c-macos-arm64-metal.tar.gz
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/rust:test macos-arm64-metal
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/rust-map:check macos-arm64-metal
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/zig:test macos-arm64-metal
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/zig-map:build macos-arm64-metal
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/zig-readback:run macos-arm64-metal
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/kotlin:test macos-arm64-metal
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/compose-map:build macos-arm64-metal
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/lwjgl-map:build macos-arm64-metal
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/swift:test macos-arm64-metal
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/swift-map:build macos-arm64-metal
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/dotnet:test macos-arm64-metal
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/go:test macos-arm64-metal
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/python:test macos-arm64-metal
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/dart:test macos-arm64-metal
      - name: Save Zig packages
        if: steps.setup.outputs.zig-cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cache/zig/p
            ~/AppData/Local/zig/p
          key: ${{ steps.setup.outputs.zig-cache-key }}
      - name: Upload native artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: native-package-macos-arm64-metal-${{ github.sha }}
          path: build/packages/native/dist/maplibre-native-c-macos-arm64-metal.tar.gz
          if-no-files-found: error
          retention-days: ${{ github.event_name == 'pull_request' && 1 || 30 }}
    if: contains(fromJSON(inputs.jobs), 'target-macos-arm64-metal')
  target-android-x64-egl:
    name: target / android-x64-egl
    runs-on: ubuntu-latest
    timeout-minutes: 120
    environment: ${{ github.event_name == 'push' && 'sccache' || '' }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-ci-deps
        id: setup
        with:
          sccache-access-key-id: ${{ secrets.R2_SCCACHE_ACCESS_KEY_ID }}
          sccache-secret-access-key: ${{ secrets.R2_SCCACHE_SECRET_ACCESS_KEY }}
          target: android-x64-egl
      - run: mise run test android-x64-egl
      - name: Archive native artifact
        run: mise run archive-native android-x64-egl
      - name: Install packaged native artifact
        run: mise run install-native-package android-x64-egl build/packages/native/dist/maplibre-native-c-android-x64-egl.tar.gz
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/kotlin:build android-x64-egl
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/kotlin:test android-x64-egl
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/go:test android-x64-egl
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/rust:test android-x64-egl
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/zig:test android-x64-egl
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/python:test android-x64-egl
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/android-map:build opengl x86_64 --prebuilt
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/dart:build:mobile android-x64-egl
      - name: Upload native artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: native-package-android-x64-egl-${{ github.sha }}
          path: build/packages/native/dist/maplibre-native-c-android-x64-egl.tar.gz
          if-no-files-found: error
          retention-days: ${{ github.event_name == 'pull_request' && 1 || 30 }}
    if: contains(fromJSON(inputs.jobs), 'target-android-x64-egl')
  target-android-x64-vulkan:
    name: target / android-x64-vulkan
    runs-on: ubuntu-latest
    timeout-minutes: 120
    environment: ${{ github.event_name == 'push' && 'sccache' || '' }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-ci-deps
        id: setup
        with:
          sccache-access-key-id: ${{ secrets.R2_SCCACHE_ACCESS_KEY_ID }}
          sccache-secret-access-key: ${{ secrets.R2_SCCACHE_SECRET_ACCESS_KEY }}
          target: android-x64-vulkan
      - run: mise run test android-x64-vulkan
      - name: Archive native artifact
        run: mise run archive-native android-x64-vulkan
      - name: Install packaged native artifact
        run: mise run install-native-package android-x64-vulkan build/packages/native/dist/maplibre-native-c-android-x64-vulkan.tar.gz
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/kotlin:build android-x64-vulkan
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/kotlin:test android-x64-vulkan
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/go:test android-x64-vulkan
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/rust:test android-x64-vulkan
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/zig:test android-x64-vulkan
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/python:test android-x64-vulkan
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/android-map:build vulkan x86_64 --prebuilt
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/dart:build:mobile android-x64-vulkan
      - name: Upload native artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: native-package-android-x64-vulkan-${{ github.sha }}
          path: build/packages/native/dist/maplibre-native-c-android-x64-vulkan.tar.gz
          if-no-files-found: error
          retention-days: ${{ github.event_name == 'pull_request' && 1 || 30 }}
    if: contains(fromJSON(inputs.jobs), 'target-android-x64-vulkan')
  target-windows-x64-wgl:
    name: target / windows-x64-wgl
    runs-on: windows-2022
    timeout-minutes: 120
    environment: ${{ github.event_name == 'push' && 'sccache' || '' }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-ci-deps
        id: setup
        with:
          sccache-access-key-id: ${{ secrets.R2_SCCACHE_ACCESS_KEY_ID }}
          sccache-secret-access-key: ${{ secrets.R2_SCCACHE_SECRET_ACCESS_KEY }}
          target: windows-x64-wgl
          zig: true
          save-toolchains: true
      - run: mise run test windows-x64-wgl
      - name: Archive native artifact
        run: mise run archive-native windows-x64-wgl
      - name: Install packaged native artifact
        run: mise run install-native-package windows-x64-wgl build/packages/native/dist/maplibre-native-c-windows-x64-wgl.tar.gz
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/rust:test windows-x64-wgl
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/rust-map:check windows-x64-wgl
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/zig:test windows-x64-wgl
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/zig-map:build windows-x64-wgl
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/zig-readback:run windows-x64-wgl
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/kotlin:test windows-x64-wgl
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/compose-map:build windows-x64-wgl
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/lwjgl-map:build windows-x64-wgl
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/dotnet:test windows-x64-wgl
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/python:test windows-x64-wgl
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/dart:test windows-x64-wgl
      - name: Save Zig packages
        if: steps.setup.outputs.zig-cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cache/zig/p
            ~/AppData/Local/zig/p
          key: ${{ steps.setup.outputs.zig-cache-key }}
      - name: Upload native artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: native-package-windows-x64-wgl-${{ github.sha }}
          path: build/packages/native/dist/maplibre-native-c-windows-x64-wgl.tar.gz
          if-no-files-found: error
          retention-days: ${{ github.event_name == 'pull_request' && 1 || 30 }}
    if: contains(fromJSON(inputs.jobs), 'target-windows-x64-wgl')
  target-windows-x64-vulkan:
    name: target / windows-x64-vulkan
    runs-on: windows-2022
    timeout-minutes: 120
    environment: ${{ github.event_name == 'push' && 'sccache' || '' }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-ci-deps
        id: setup
        with:
          sccache-access-key-id: ${{ secrets.R2_SCCACHE_ACCESS_KEY_ID }}
          sccache-secret-access-key: ${{ secrets.R2_SCCACHE_SECRET_ACCESS_KEY }}
          target: windows-x64-vulkan
          zig: true
      - run: mise run test windows-x64-vulkan
      - name: Archive native artifact
        run: mise run archive-native windows-x64-vulkan
      - name: Install packaged native artifact
        run: mise run install-native-package windows-x64-vulkan build/packages/native/dist/maplibre-native-c-windows-x64-vulkan.tar.gz
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/rust:test windows-x64-vulkan
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/rust-map:check windows-x64-vulkan
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/zig:test windows-x64-vulkan
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/zig-map:build windows-x64-vulkan
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/zig-readback:run windows-x64-vulkan
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/kotlin:test windows-x64-vulkan
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/compose-map:build windows-x64-vulkan
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //examples/lwjgl-map:build windows-x64-vulkan
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/dotnet:test windows-x64-vulkan
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/python:test windows-x64-vulkan
      - env:
          MISE_TASK_SKIP: //:build
        run: mise run //bindings/dart:test windows-x64-vulkan
      - name: Save Zig packages
        if: steps.setup.outputs.zig-cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cache/zig/p
            ~/AppData/Local/zig/p
          key: ${{ steps.setup.outputs.zig-cache-key }}
      - name: Upload native artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: native-package-windows-x64-vulkan-${{ github.sha }}
          path: build/packages/native/dist/maplibre-native-c-windows-x64-vulkan.tar.gz
          if-no-files-found: error
          retention-days: ${{ github.event_name == 'pull_request' && 1 || 30 }}
    if: contains(fromJSON(inputs.jobs), 'target-windows-x64-vulkan')
  verified:
    name: verified (${{ github.sha }} / ${{ inputs.scope }})
    runs-on: ubuntu-slim
    timeout-minutes: 5
    if: always()
    needs:
      - target-emscripten-wasm32-webgl
      - target-emscripten-wasm32-webgpu
      - target-macos-arm64-metal
      - target-android-x64-egl
      - target-android-x64-vulkan
      - target-windows-x64-wgl
      - target-windows-x64-vulkan
    steps:
      - name: Require every coverage job
        env:
          EXPECTED: '["target-android-x64-egl", "target-android-x64-vulkan", "target-emscripten-wasm32-webgl", "target-emscripten-wasm32-webgpu", "target-macos-arm64-metal", "target-windows-x64-vulkan", "target-windows-x64-wgl"]'
          RESULTS: ${{ toJSON(needs) }}
          SELECTED: ${{ inputs.jobs }}
        run: |
          jq -e -n --argjson expected "$EXPECTED" --argjson results "$RESULTS" --argjson selected "$SELECTED"   '($expected == ($results | keys)) and ($selected | length > 0)
             and (($selected - $expected) | length == 0)
             and all($expected[]; . as $job | $results[$job].result ==
               (if $selected | index($job) then "success" else "skipped" end))'
