name: Setup CI deps
description: Install sccache and mise toolchain

runs:
  using: composite
  steps:
    - name: Enable Git long paths
      if: ${{ runner.os == 'Windows' }}
      shell: pwsh
      run: git config --global core.longpaths true

    - name: Cache MapLibre Native source
      uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
      with:
        path: |
          third_party/maplibre-native
          .git/modules/third_party/maplibre-native
        key: mln-src-v2-${{ hashFiles('.gitmodules', 'cmake/mln_version.cmake') }}

    - name: Install sccache
      uses: mozilla-actions/sccache-action@9e7fa8a12102821edf02ca5dbea1acd0f89a2696 # v0.0.10

    - name: Wire sccache into CMake
      shell: bash
      run: |
        {
          echo "SCCACHE_GHA_ENABLED=true"
          echo "CMAKE_C_COMPILER_LAUNCHER=sccache"
          echo "CMAKE_CXX_COMPILER_LAUNCHER=sccache"
        } >> "$GITHUB_ENV"

    - name: Use software OpenGL on Linux
      if: ${{ runner.os == 'Linux' }}
      shell: bash
      run: |
        {
          echo "EGL_PLATFORM=surfaceless"
          echo "LIBGL_ALWAYS_SOFTWARE=true"
        } >> "$GITHUB_ENV"

    - name: Use software OpenGL on Windows
      if: ${{ runner.os == 'Windows' }}
      shell: pwsh
      run: '"GALLIUM_DRIVER=llvmpipe" >> $env:GITHUB_ENV'

    - name: Cache Cargo packages
      uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
      with:
        path: |
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
          ~/.cargo/git/db/
        key: ${{ runner.os }}-${{ runner.arch }}-cargo-registry-v1-${{ hashFiles('**/Cargo.lock') }}

    - name: Cache Gradle
      uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-${{ runner.arch }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-${{ runner.arch }}-gradle-

    - name: Trim Windows PATH before Pixi activation
      if: ${{ runner.os == 'Windows' }}
      shell: pwsh
      run: |
        # Pixi's Windows activation uses cmd.exe and emits a single PATH update
        # line; the default GitHub Actions Path can make that line exceed cmd's
        # limit. See https://github.com/prefix-dev/pixi/issues/1108.
        $paths = @(
          $(if ($env:SCCACHE_PATH) { Split-Path -Parent $env:SCCACHE_PATH }),
          "C:\Program Files\Git\bin",
          "C:\Program Files\Git\usr\bin",
          "$env:SystemRoot\System32",
          "$env:SystemRoot",
          "$env:SystemRoot\System32\Wbem",
          "$env:SystemRoot\System32\WindowsPowerShell\v1.0",
          "C:\Program Files\PowerShell\7",
          "C:\Program Files\Git\cmd"
        ) | Where-Object { $_ -and (Test-Path $_) }

        "Path=$($paths -join ';')" >> $env:GITHUB_ENV

    - name: Install mise toolchain
      uses: jdx/mise-action@1648a7812b9aeae629881980618f079932869151 # v4.0.1
      with:
        cache: true
        cache_key_prefix: mise-v2
        install: true

    - name: Register Windows CI software graphics drivers
      if: ${{ runner.os == 'Windows' }}
      shell: pwsh
      run: |
        $pixiLibrary = Join-Path $env:GITHUB_WORKSPACE ".pixi\envs\default\Library"

        # Register Pixi's Mesa WGL driver so system opengl32.dll can load llvmpipe.
        $mesaWglDriver = Join-Path $pixiLibrary "bin\libgallium_wgl.dll"
        if (-not (Test-Path $mesaWglDriver)) {
          throw "Missing Mesa WGL driver: $mesaWglDriver"
        }

        $mesaWglSystemDriver = Join-Path $env:WINDIR "System32\mesadrv.dll"
        Copy-Item $mesaWglDriver $mesaWglSystemDriver -Force

        $openglDriverKey = "HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\OpenGLDrivers\MSOGL"
        New-Item -Path $openglDriverKey -Force | Out-Null
        New-ItemProperty -Path $openglDriverKey -Name DLL -PropertyType String -Value "mesadrv.dll" -Force | Out-Null
        New-ItemProperty -Path $openglDriverKey -Name DriverVersion -PropertyType DWord -Value 1 -Force | Out-Null
        New-ItemProperty -Path $openglDriverKey -Name Flags -PropertyType DWord -Value 1 -Force | Out-Null
        New-ItemProperty -Path $openglDriverKey -Name Version -PropertyType DWord -Value 2 -Force | Out-Null

        # Register Pixi's lavapipe ICD so the Vulkan loader finds software Vulkan.
        $driverManifest = Join-Path $pixiLibrary "share\vulkan\icd.d\lvp_icd.x86_64.json"
        if (-not (Test-Path $driverManifest)) {
          throw "Missing lavapipe Vulkan ICD manifest: $driverManifest"
        }

        $driverKey = "HKLM:\SOFTWARE\Khronos\Vulkan\Drivers"
        New-Item -Path $driverKey -Force | Out-Null
        New-ItemProperty -Path $driverKey -Name $driverManifest -PropertyType DWord -Value 0 -Force | Out-Null

    - name: Diagnose EGL setup
      if: ${{ runner.os == 'Linux' }}
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install --no-install-recommends --yes libegl-mesa0 libgl1-mesa-dri mesa-utils
        mise exec -- pixi run --locked bash -euo pipefail -c '
          env | sort | grep -E "^(EGL|LIBGL|MESA|LD_LIBRARY_PATH|__EGL|CONDA|PIXI)" || true
          command -v eglinfo
          EGL_LOG_LEVEL=debug LIBGL_DEBUG=verbose LD_DEBUG=libs eglinfo || true
        '

    - name: Diagnose Vulkan setup
      if: ${{ contains(env.MISE_ENV, 'vulkan') }}
      shell: bash
      run: mise run ci:vulkan-info

    # The mise action may restore Rust metadata without all rustup-managed
    # component files. Repair the toolchain only when rustup cannot add the
    # configured components or the component binaries still cannot run.
    - name: Ensure Rust components
      shell: bash
      run: |
        ensure_rust_components() {
          mise exec -- rustup component add rustfmt clippy &&
            mise exec -- cargo fmt --version &&
            mise exec -- cargo clippy --version
        }

        if ! ensure_rust_components; then
          mise install --force rust
          ensure_rust_components
        fi
