[project]
name = "maplibre-native-ffi-workspace"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["bindings/python"]

[dependency-groups]
dev = [
  "ruff==0.16.1",
  "ty==0.0.65",
]
