#!/usr/bin/env python3
# [MISE] description="Verify CI consumer tasks have mise project ownership."
# [MISE] shell="python"

import pathlib
import sys

ROOT = pathlib.Path(__file__).resolve().parents[3]
sys.path.insert(0, str(ROOT))

from ci.affected import check_graph

check_graph(ROOT)
