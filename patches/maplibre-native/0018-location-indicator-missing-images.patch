diff --git a/src/mln/renderer/layers/render_location_indicator_layer.cpp b/src/mln/renderer/layers/render_location_indicator_layer.cpp
index fccbcb7bf8151b791bbc95ec1b5209bdcdff48e6..56d6407c336a98e71b8bcb85bd58a27e36bf0615 100644
--- a/src/mln/renderer/layers/render_location_indicator_layer.cpp
+++ b/src/mln/renderer/layers/render_location_indicator_layer.cpp
@@ -1303,2 +1303,3 @@ void RenderLocationIndicatorLayer::update(gfx::ShaderRegistry& shaders,
         }
+        drawable.setEnabled(static_cast<bool>(info.textureInfo.texture));
     };
diff --git a/test/map/map.test.cpp b/test/map/map.test.cpp
--- a/test/map/map.test.cpp
+++ b/test/map/map.test.cpp
@@ -102,2 +102,19 @@
 };
+
+TEST(Map, LocationIndicatorWithoutImages) {
+    MapTest<> test;
+    test.map.getStyle().loadJSON(R"STYLE({
+      "version": 8,
+      "sources": {},
+      "layers": [{
+        "id": "location",
+        "type": "location-indicator",
+        "paint": {"location": [0, 0, 0], "accuracy-radius": 0}
+      }]
+    })STYLE");
+    test.map.jumpTo(CameraOptions().withCenter(LatLng{0, 0}).withZoom(16));
+
+    // Neither the accuracy circle nor any image should produce a draw call.
+    EXPECT_EQ(test.frontend.render(test.map).stats.numDrawCalls, 0);
+}
 
