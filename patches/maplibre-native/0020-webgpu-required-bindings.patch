diff --git a/src/mln/webgpu/drawable.cpp b/src/mln/webgpu/drawable.cpp
index e562ccedbe078fe257b710cb640fa9fbba385570..dcaa50ff9a6deb48dddc87758e7e9d83a94040d3 100644
--- a/src/mln/webgpu/drawable.cpp
+++ b/src/mln/webgpu/drawable.cpp
@@ -538,5 +538,5 @@ void Drawable::draw(PaintParameters& parameters) const {
                                      "WebGPU: missing bind group layout for group " + std::to_string(group) +
                                          " in shader '" + shaderName + "'");
-                        continue;
+                        return;
                     }
 
@@ -649,5 +649,5 @@ void Drawable::draw(PaintParameters& parameters) const {
                                      "WebGPU: invalid bindings for drawable '" + getName() +
                                          "' (group=" + std::to_string(group) + ", slot=" + std::to_string(slot) + ")");
-                        continue;
+                        return;
                     }
 
@@ -662,4 +662,7 @@ void Drawable::draw(PaintParameters& parameters) const {
 
                     WGPUBindGroup bindGroup = wgpuDeviceCreateBindGroup(deviceHandle, &descriptor);
+                    if (!bindGroup) {
+                        return;
+                    }
                     impl->bindGroups.push_back({static_cast<uint32_t>(slot), group, bindGroup});
                 }
