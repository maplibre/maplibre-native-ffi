---
title: MapLibre Native FFI
description: A C API for direct host control of the MapLibre Native map and render session.
---

import SupportMatrix from "../../components/SupportMatrix.astro";

MapLibre Native FFI is a C API for
[MapLibre Native](https://github.com/maplibre/maplibre-native), an engine that
draws vector and raster maps from a MapLibre style document. It exposes MapLibre
Native concepts directly across a C ABI and many language bindings. It's intended
for SDKs and custom host integrations that prefer to control the map and render
session directly.

The MapLibre [Android](https://maplibre.org/maplibre-native/android/api/) and
[iOS](https://maplibre.org/maplibre-native/ios/latest/documentation/maplibre/)
SDKs provide a map view that owns a window, a frame loop, and gesture handling.
This API provides the map and the render session, and the host provides the rest:
the host creates the window and the graphics context, pumps the runtime, and
decides when each frame is drawn. The same API renders into a texture target for
image export or for compositing into a scene that the host already draws.

See the [examples](https://github.com/maplibre/maplibre-native-ffi/tree/main/examples)
in the repository for complete integration examples for each language binding.

## Status

MapLibre Native FFI is pre-1.0. The C ABI is unstable while `mln_c_version()`
returns `0`, and headers, package coordinates, and behavior change between
snapshots.

The C API covers most of the MapLibre Native API surface. Custom layers and
shader registry access are not yet included, so host code cannot contribute its
own draw calls and shaders.

<SupportMatrix />
