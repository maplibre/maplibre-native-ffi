---
title: Work Offline
description: Download an offline region so the map keeps drawing without a network, and read the regions that the cache database already holds.
sidebar:
  order: 11
---

import { Code } from "@astrojs/starlight/components";
import offlineRegionSnippet from "../../../../snippets/c/offline-region.c?raw";
import offlineStorageSnippet from "../../../../snippets/c/offline-storage.c?raw";

One database holds everything that a runtime caches, and `cache_path` in the
runtime options sets that database's path. The field defaults to `:memory:`, so
pass a writable filesystem path to keep the cached resources across runs of the
host.

Two kinds of storage share the database. The ambient cache holds the resources
that the map already fetched, and MapLibre evicts from it to stay under a byte
budget. Offline regions are the storage that a host requests: MapLibre downloads
every resource that a defined area needs, and keeps those resources outside the
ambient budget. Download a region when the application must draw a known area
without a network.

Every database call is asynchronous, and they share one shape. A `_start`
function validates its arguments, returns an operation ID, and queues the work.
The runtime later queues one `MLN_RUNTIME_EVENT_OFFLINE_OPERATION_COMPLETED`
event that carries the ID and the operation's own result status. That result
status is separate from the status that the `_start` call returned. An operation
that produces a value holds that value until the matching `_take_result` call
consumes it, and every operation holds runtime state until a take or
`mln_runtime_offline_operation_discard` releases it.

<Code code={offlineRegionSnippet} lang="c" title="offline-region.c" meta={'collapse={1-44, 72-85, 114-119} mark={"MapLibre resolves this definition into every tile, glyph, and sprite that the area needs":49-61} mark={"MapLibre copies the definition and the opaque metadata bytes before this call returns":65-67} mark={"Observation enables the progress and error events for this region":88-90} mark={"An active download proceeds while the host pumps the runtime":98-100} mark={"The fraction stays an estimate until the status reports the required count precise":123-126}'} />

The runtime queues progress events for observed regions only.
`MLN_RUNTIME_EVENT_OFFLINE_REGION_STATUS_CHANGED` carries resource counts, tile
counts, and stored bytes for every change, and
`MLN_RUNTIME_EVENT_OFFLINE_REGION_RESPONSE_ERROR` reports one resource that
failed while the rest of the download continues. Drain both from the runtime
that owns the region, as described in
[Concepts](/maplibre-native-ffi/concepts/). Setting the download state to
`MLN_OFFLINE_REGION_DOWNLOAD_INACTIVE` pauses a download, and setting it active
again resumes from the resources that MapLibre already stored.

List the regions in the database to learn what an earlier run of the host
stored, because region records outlive the runtime that wrote them.

<Code code={offlineStorageSnippet} lang="c" title="offline-storage.c" meta={'collapse={1-43, 72-77} mark={"One operation lists every region in the database":48-50} mark={"Taking the result consumes the operation entry":55-57} mark={"Discard releases an operation whose result the host never took":58-61} mark={"Each entry carries the region ID, its definition, and the metadata that creation stored":68-69} mark={"Deleting a region evicts the resources that no other region requires":81-83}'} />
