---
title: Load a style
description: Load a base style from a URL or host text, then replay additions after later style loads.
sidebar:
  order: 2
---

import { Code } from "@astrojs/starlight/components";
import { region } from "../../../snippets";

import loadStyle from "../../../../snippets/c/load-style.c?raw";

A style defines the tiles, layers, and drawing order of a map. Load one before
rendering a new map.

## Choose where the style comes from

Set a style URL for a hosted base map. MapLibre fetches it through the resource
system that also loads tiles. The request therefore uses the runtime's cache,
offline regions, and resource transform.

<Code code={region(loadStyle, "url")} lang="c" title="load-style.c" />

Set style text when the host already has the document, such as a bundled base
style or one composed at runtime. The command copies the text before returning
acceptance.

<Code code={region(loadStyle, "json")} lang="c" title="load-style.c" />

Either command returns after copying and accepting its input. The native
scheduler loads the style, sources, glyphs, and first tiles. A style-loaded
event reports that the style parsed and its sources exist. A rejected or failed
command completes with diagnostic text; a later loading failure arrives as an
event. [Handle events](/maplibre-native-ffi/guides/handle-events/) covers reading
the event stream.

## Load a bundled style on Android

After
[Android platform initialization](/maplibre-native-ffi/install/#android), a
style URL can name a file in the APK `assets/` directory.
`asset://style.json` and `file:///android_asset/style.json` both read that file.
Prefix the same URL with `pmtiles://` for a PMTiles archive in that directory.
Add `pmtiles` to the app's `androidResources.noCompress` list so MapLibre can
read byte ranges from that archive. Other `file://` URLs still read the
filesystem, including files the host downloaded into app storage.

## Replay additions after a style load

Sources, layers, images, and layer properties that your host adds belong to the
style that was loaded when they were added. Loading another style discards all
of them.

A host adds its own data after the style-loaded event and repeats those changes
after every later style load. A host that switches base maps keeps the changes
that it needs to replay.

Camera state survives a style load unchanged.
