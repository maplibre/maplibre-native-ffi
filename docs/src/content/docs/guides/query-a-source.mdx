---
title: Query a source
description: Read the features that a source holds, and expand a cluster into the features under it.
sidebar:
  order: 13
---

import { Code } from "@astrojs/starlight/components";
import { region } from "../../../snippets";

import sourceFeatures from "../../../../snippets/c/query-source-features.c?raw";
import clusterLeaves from "../../../../snippets/c/cluster-leaves.c?raw";

A source query reports the loaded features in a source, including features that
no layer draws. To find features at a screen position, use
[Query a map](/maplibre-native-ffi/guides/query-a-map/).

The query belongs to the render session and is an operation callable from any
thread. The selected driver executes it after preceding render work. A caller
driver therefore needs graphics-thread service while the operation is pending.
Source queries become available after the first rendered frame.

## Run the query

A vector source needs source-layer IDs, because one vector tile holds several
layers and MapLibre reads the layers that you name. A vector query with
no source-layer ID returns zero features. A GeoJSON source holds a single layer
and ignores the field.

<Code code={region(sourceFeatures, "options")} lang="c" title="query-source-features.c" />

Name the source itself by its style source ID. That ID is the query's second
argument, and it differs from the source-layer IDs in the options.

<Code code={region(sourceFeatures, "query")} lang="c" title="query-source-features.c" />

A successful query can return no hits for an unknown source ID. Copy every
value that the host keeps.

<Code code={region(sourceFeatures, "read")} lang="c" title="query-source-features.c" />

A query covers the tiles that the source loaded for the current camera. Its
result therefore changes as the camera moves and more tiles arrive. A source
loads tiles only while a layer uses it.

## Expand a cluster

An extension query runs a named extension against one source feature. The
`supercluster` extension reads a clustered GeoJSON source. Its `leaves` field
returns the features under one cluster as a feature collection.

`supercluster` matches numbers by exact native JSON type. Encode `limit` and
`offset` as nonnegative integer literals so MapLibre reads them as unsigned
values.

<Code code={region(clusterLeaves, "arguments")} lang="c" title="cluster-leaves.c" />

The call names the source, the cluster feature, the extension, and the field
within that extension.

<Code code={region(clusterLeaves, "query")} lang="c" title="cluster-leaves.c" />

An extension result is one owned buffer. For `leaves`, parse it as a
GeoJSON FeatureCollection; scalar extension fields return their JSON value.

<Code code={region(clusterLeaves, "leaves")} lang="c" title="cluster-leaves.c" />

Pass the Feature bytes from a queried feature to the extension query.
`supercluster` also matches `cluster_id` by exact JSON type. Preserve its
nonnegative integer literal; rewriting every number as a double produces a
successful query with an empty result.
