---
title: Style layers by data
description: Use feature data in layer properties and filters.
sidebar:
  order: 10
---

import { Code } from "@astrojs/starlight/components";
import { region } from "../../../snippets";

import styleByData from "../../../../snippets/c/style-by-data.c?raw";

A layer property contains a constant or an expression. An expression reads each
feature's data, allowing one rule to style every feature. A property value
belongs to the loaded style. Set it again after every later style load, as
[Load a style](/maplibre-native-ffi/guides/load-a-style/) describes.

Style properties use style-spec JSON values. A constant is a scalar, and an
expression is an array whose first element names the operator. The remaining
elements are its arguments. This C snippet builds a descriptor tree with one
small constructor per JSON type.

<Code code={region(styleByData, "node")} lang="c" title="style-by-data.c" />

Build inner expressions before the arrays that contain them. `["get", "mag"]`
reads the feature property named `mag`, and `["linear"]` names the interpolation
curve.

<Code code={region(styleByData, "expression")} lang="c" title="style-by-data.c" />

Those two nodes become elements of the outer array, alongside the stop pairs
that scale a circle from 4 pixels at magnitude 1 to 24 pixels at magnitude 6.

<Code code={region(styleByData, "property")} lang="c" title="style-by-data.c" />

Setting a property takes one layer and one property name, and the name is the
one that the style specification defines for that layer's type.

<Code code={region(styleByData, "set")} lang="c" title="style-by-data.c" />

A filter is a boolean expression that selects the source features to draw. It
uses the same value representation as a style property. An empty filter clears
the layer's filter. The snippet reuses the `magnitude` node in this second value.

<Code code={region(styleByData, "filter")} lang="c" title="style-by-data.c" />

The host owns the descriptors, arrays, and strings, and can allocate them on the
stack. MapLibre parses and copies a property value or filter before the call
returns.

Both calls run on the map's owner thread and apply to the live style
immediately. A host that pumps the runtime and renders on update draws the new
appearance in its next frame.
