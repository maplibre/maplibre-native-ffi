[tasks.build]
depends = ["//:ensure-native-library"]
run = '''
source "$MLN_FFI_REPO_ROOT/.mise/bin/zig-build-args.sh"
zig build "${MLN_FFI_ZIG_BUILD_ARGS[@]}"
'''

[tasks.run]
depends = ["//:ensure-native-library"]
run = '''
mkdir -p zig-out
source "$MLN_FFI_REPO_ROOT/.mise/bin/zig-build-args.sh"
zig build run "${MLN_FFI_ZIG_BUILD_ARGS[@]}" -- zig-out/zig-readback.ppm
'''
