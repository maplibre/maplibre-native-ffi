[tasks.ci]
run = ["mise run :run"]

[tasks.build]
depends = ["//:ensure-native-library"]
run = "zig build"

[tasks.run]
depends = ["//:ensure-native-library"]
run = "mkdir -p zig-out && zig build run -- zig-out/zig-readback.ppm"
